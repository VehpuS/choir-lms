## 1. Shared Sort Control Extraction

**Corrected per design.md's "Sort control placement" and "Extract a shared `SortFieldChipRow`" decisions (updated after finding Tags' sort already lives behind the filter popover, not inline in `SavedTagsList`):**

- [x] 1.1 Add a `SortFieldChipRow` presentational component to `packages/mobile-rehearsal-player/src/app/library/components/`, wrapping the existing generic `FilterChipGroup<Value>` (`library/search/components/library-search-filter-groups.tsx`) plus a direction-toggle `SurfaceIconButton`, per design.md's `SortFieldChipRowProps<Field>` shape, owning its own default styles matching the values currently duplicated across the existing call sites.
- [x] 1.2 Refactor the Tags `Sort` block in `library-search-controls.tsx` and the tag-detail screen's `Sort` block in `tags/components/tag-match-list/controls-panel.tsx` to render the new shared `SortFieldChipRow` instead of independently composing `FilterChipGroup` + `SurfaceIconButton`, keeping `sortSavedTagUsage`/`SavedTagsListSortState`, `TagMatchListSortState`, and all existing behavior unchanged. Files' own `Sort` block is left untouched (non-goal).
- [x] 1.3 Add focused tests for `SortFieldChipRow` and re-run the existing Tags-list and tag-detail sort tests/behavior to confirm no regression from the refactor. (Extracted the component's one branch of logic, the direction→icon mapping, into a colocated pure `resolveSortDirectionIcon` helper in `sort-field-chip-row-model.ts` and unit-tested it with `node:test`, matching the repo's existing "test the view-model/logic, not the render tree" convention — confirmed via a throwaway smoke test that `@testing-library/react-native` cannot render RN components under this repo's `tsx --test`/esbuild harness without a Babel/jest-expo-style transform, which is out of scope per this repo's explicit no-Jest/Vitest policy. Re-ran `saved-tags-list/model.spec.ts` and `tag-match-list/model.spec.ts`, both untouched by the presentational refactor — 18/18 and 17/17 passing.)

## 2. Tracks View Sort

- [x] 2.1 Add a `sortSavedSourcesBy`-style comparator and sort state/options (`Name`/`Date added`, using `source.modifiedTime` for `Date added` to match Files' existing semantics) in a colocated model file for the Tracks browse surface.
- [x] 2.2 Extend `canShowFilterPopover` (`screens/library/library-header-search-props.ts`) to include the Tracks view, add a Tracks `Sort` block (via the shared `SortFieldChipRow`) to `library-search-controls.tsx`'s filter popover, own the sort state in `use-saved-rehearsal-library-search.ts`, and apply the sort on top of `searchState.visibleSavedLibrarySources` before rendering rows in `BrowseSourceGroup`.
- [x] 2.3 Add focused tests for the Tracks sort comparator (name case-insensitivity, date-added ordering, default state) and update/add a `browse-content`-level test if the render-branch wiring needs one, matching the existing test-coverage convention for this surface. (Comparator coverage already landed alongside 2.1's `browse-source-group-model.spec.ts` — default state, name asc/desc case-insensitivity, date asc/desc, undated fallback, no-mutation; 7/7 passing, re-verified. No `browse-content`-level test needed: sorting is applied entirely inside `useSavedRehearsalLibrarySearch`'s `visibleSavedLibrarySources` memo before the already-sorted list reaches `BrowseSourceGroup`, which — like `browse-content.tsx`/`browse-content-model.ts` — contains no sort-related branching of its own to test; this matches the surface's existing convention of testing pure filter/sort functions directly (e.g. `filterSavedLibrarySourcesByQuery`) rather than the stateful hook that composes them, which has no spec file of its own either.)

## 3. Loops View Sort

- [x] 3.1 Add a `sortSavedLoopsBy`-style comparator and sort state/options (`Name`/`Date added`, using `loop.createdAt` for `Date added`) in a colocated model file for the Loops browse surface. (Added `loops/utils/saved-loop-sort-model.ts`, mirroring `browse-source-group-model.ts`'s shape and reusing the shared `parseTimestamp` helper from `saved-rehearsal-library/library-files-model`; placed in `loops/utils/` to match this feature's existing model-file convention, e.g. `saved-loop-section-model.ts`, rather than colocating in `loops/components/` — `saved-loop-view-model.ts` is already at the 300-line guideline so a new file was needed regardless. 7/7 tests passing.)
- [x] 3.2 Extend `canShowFilterPopover` to include the Loops view, add a Loops `Sort` block (via the shared `SortFieldChipRow`) to `library-search-controls.tsx`'s filter popover, own the sort state in `use-saved-rehearsal-library-search.ts`, and apply the sort on top of `searchState.visibleSavedLoops` before rendering rows in the Loops section/list component. (Mirrored 2.2's Tracks wiring exactly: `loopsSortState`/`setLoopsSortField`/`toggleLoopsSortDirection` added to the hook, `sortSavedLoopsBy` applied inside `visibleSavedLoops`'s memo — same as Tracks, no changes needed to `SavedLoopSection`/`SavedLoopList` themselves since they already render whatever list they're given. Props threaded through `search-shell.tsx`. Full 487/487 test suite and app typecheck pass. **Known deferred item, confirmed with user:** this pushed `library-search-controls.tsx` to 305 lines and `use-saved-rehearsal-library-search.ts` to 312 lines (eslint's `max-lines` count) over the 300-line guideline; extraction is deferred until after 4.2 lands Playlists' matching increment, to do one consolidated pass instead of two. Currently a lint warning only, not an error, matching existing precedent elsewhere in the repo.)
- [x] 3.3 Add focused tests for the Loops sort comparator, matching task 2.3's coverage shape. (Comparator coverage already landed alongside 3.1's `saved-loop-sort-model.spec.ts` — default state, name asc/desc case-insensitivity, date asc/desc via `createdAt`, date-tie name tie-break, no-mutation; 7/7 passing, re-verified. Confirmed, matching 2.3's finding for Tracks, that neither `SavedLoopList` nor `SavedLoopSection`/`saved-loop-section-model.ts` contain any sort-related branching to test — sorting is applied entirely upstream in `useSavedRehearsalLibrarySearch`'s `visibleSavedLoops` memo.)

## 4. Playlists View Sort

- [x] 4.1 Add a `sortSavedPlaylistsBy`-style comparator and sort state/options (`Name`/`Date added`, using `playlist.createdAt` for `Date added`) in a colocated model file for the Playlists browse surface. (Added `browse-playlist-cards-model.ts` colocated with `browse-playlist-cards.tsx` in `components/saved-rehearsal-library-section/` — the closer precedent than Loops' `loops/utils/` choice, since `BrowsePlaylistCards` already lives in the same folder as Tracks' `browse-source-group.tsx`/`browse-source-group-model.ts` pair. Per task 4.2's wording ("apply the sort on top of `searchState.visiblePlaylistCards`"), the comparator operates on the already-resolved `SavedPlaylistCard[]` shape rather than raw `Playlist[]`, reading `.playlist.name`/`.playlist.createdAt`. 7/7 tests passing.)
- [x] 4.2 Extend `canShowFilterPopover` to include the Playlists view, add a Playlists `Sort` block (via the shared `SortFieldChipRow`) to `library-search-controls.tsx`'s filter popover, own the sort state in `use-saved-rehearsal-library-search.ts`, and apply the sort on top of `searchState.visiblePlaylistCards` before rendering cards in `BrowsePlaylistCards`. (Mirrored 2.2/3.2's wiring exactly: `playlistsSortState`/`setPlaylistsSortField`/`togglePlaylistsSortDirection`, `sortSavedPlaylistsBy` applied inside `visiblePlaylistCards`'s memo after `resolveSavedPlaylistCards`. `canShowFilterPopover` simplified to unconditionally `true` outside a detail view, since all 5 `SavedRehearsalLibraryView` values now have a filter popover — removed the now-dead `selectedView` param from `resolveLibraryHeaderSearchProps` and its one call site. **Resolved the file-length overage deferred from 3.2**, confirmed with user: extracted a shared `useFieldSortState` hook (`library/components/use-field-sort-state.ts`) collapsing 4 duplicated useState+setField+toggleDirection triples in the search hook (338→297 lines, no type-safety loss — user explicitly asked for a type-safe approach over a string-erased data-driven block); split `library-search-controls.tsx`'s entire filter-popover JSX into a new `LibrarySearchFilterPopover` component (`library-search-filter-popover.tsx`), leaving the original file as a thin search-panel/filter-popover composition (318→184 lines; new file 259 lines). Both target files now clear of the `max-lines` lint warning. **Additional cleanup, confirmed with user:** the `canShowFilterPopover`/`canShowFilters` simplification exposed that this flag was already unconditionally `true` in *every* code path — including the tag-detail branch (`TagDetailHeaderSearchActions.canShowFilters` was hardcoded `true` at its source). Removed it entirely across `LibraryHeaderSearchProps`, `SavedRehearsalLibraryHeaderProps`, `LibrarySearchControlsActions` (filter button now renders unconditionally; `visibleActionCount` calc replaced by a plain `canShowSearch` check), and `TagDetailHeaderSearchActions` — 5 files total. Full suite 494/494 passing, app typecheck and lint clean.)
- [x] 4.3 Add focused tests for the Playlists sort comparator, matching task 2.3's coverage shape. (Comparator coverage already landed alongside 4.1's `browse-playlist-cards-model.spec.ts` — default state, name asc/desc case-insensitivity, date asc/desc via `playlist.createdAt`, date-tie name tie-break, no-mutation; 7/7 passing, re-verified. Confirmed, matching 2.3/3.3's findings, that neither `BrowsePlaylistCards` nor `SavedPlaylistCardsList` contain any sort-related branching to test — sorting is applied entirely upstream in `useSavedRehearsalLibrarySearch`'s `visiblePlaylistCards` memo.)

## 5. Entity Filter Reset Fix

- [x] 5.1 Reset `entityFilter` to `'all'` when `selectedView` changes while `isLibrarySearchMode` is false, in `use-saved-rehearsal-library-search.ts` or an adjacent effects hook, per design.md's scoping (only reset when no active search, to avoid discarding an in-progress filtered search). (Added `selectedView` to `UseSavedRehearsalLibrarySearchOptions` and a `useEffect` keyed strictly on `[selectedView]` that resets `entityFilter` to `'all'` unless `activeLibrarySearchQuery !== null` — no `react-hooks/exhaustive-deps` lint rule is configured in this repo, so omitting `activeLibrarySearchQuery` from deps is safe and intentional: it keeps the effect firing only on an actual view change, not merely when search starts/stops, matching design.md's literal scoping. Wired both call sites — `screens/library/index.tsx` (external `selectedView`) and `use-saved-rehearsal-library-section-state.ts`'s internal fallback path (its own `internalSelectedView`). Full suite 494/494 passing, app typecheck and lint clean.)
- [x] 5.2 Add a focused test covering the reset-on-view-change behavior and confirming it does not fire while a search is active. (Extracted the reset decision into a pure `resolveEntityFilterOnViewChange` helper in a new `entity-filter-view-change.ts`, kept dependency-light — no react-native-touching imports — since `use-saved-rehearsal-library-search.ts` itself transitively imports `@react-native-async-storage/async-storage` via `useRecentSearchHistory` and can't be rendered/tested under this repo's `tsx --test` harness, matching the same constraint and pattern established in task 1.3. `useEffect` in the hook is now a thin wrapper calling `setEntityFilter` with the helper via the functional-updater form. 3/3 new tests passing: resets to `all` with no active search, leaves the filter untouched while a search is active, no-op when already `all`. Full suite 497/497 passing, app typecheck and lint clean.)

## 6. Final Validation

- [ ] 6.1 Manually verify in the integrated browser: each of Tracks/Loops/Playlists exposes working Name/Date-added sort (both directions), sort composes correctly with an active search query, Files and Tags sort remain visually and behaviorally unchanged after the shared-component refactor, and the Show-filter reset fix behaves as spec'd (reset when switching views with no active search, preserved mid-search).
- [ ] 6.2 Run project typecheck and lint on touched files, and the narrowest relevant Nx test targets for `mobile-rehearsal-player`, before considering this change ready to archive.
