## 1. Tag Aggregation Foundation

- [x] 1.1 Add a library-wide tag aggregation query (likely in `audio-library-runtime`) that scans saved tracks, loops, playlists, and folders and returns the deduped, normalized set of in-use tags with per-tag usage counts, reusing `normalizeLibraryEntityTags`'s trim/whitespace/case-insensitive dedupe rule rather than reimplementing it.
  - Decision (confirmed with user): `normalizeLibraryEntityTags` currently lives app-side (`tag-editor-sheet/model.ts`), but `audio-library-runtime` cannot depend on the app. Move `normalizeLibraryEntityTags` into `@org/audio-library-models` (new `src/lib/rehearsal-tags.ts`, exported from the package `index.ts`) so both the app's tag editor and the new runtime aggregation query share one implementation. `parseLibraryTagInput`/`addLibraryEntityTag`/`removeLibraryEntityTag` stay in the app's `model.ts` and import the moved function from `@org/audio-library-models`.
  - New query: `aggregateRehearsalLibraryTags` in `packages/audio-library-runtime/src/lib/rehearsal-library-tags.ts`, taking `{ entityCollections: RehearsalLibraryEntityCollections; folders: RehearsalLibraryFolderNode[] }` and returning `RehearsalLibraryTagUsage[]` (`{ tag, count }`), pre-sorted most-used-first with an alphabetical tie-break (case-insensitive) so task `2.1`'s Recents cap can simply slice the result. `count` = number of distinct entities carrying the tag (post per-entity dedupe). Exported from the package root (`index.ts`) for app consumption.
- [x] 1.2 Add focused unit tests for the aggregation query: cross-entity dedupe with mixed casing/whitespace, usage counts, and the empty-library (no tags) case.
- [x] 1.3 Add `createdAt: string` to `DriveAudioSource` (`@org/audio-library-models`) and stamp/preserve it through `AsyncStoragePracticeRepository.saveSource`: brand-new records get `new Date().toISOString()`, existing records being replaced (e.g. tag edits) keep their prior `createdAt`, mirroring how `NamedLoop`/`Playlist` already preserve theirs across edits.
  - Decision (confirmed with user): needed so the tag detail screen's "Date added" sort (task `3.2`) has a real timestamp for tracks, which currently have none. See design.md's "Sources and folders gain creation timestamps..." decision.
  - Implementation: added `createdAt: string` to `DriveAudioSource` (`rehearsal-domain.ts`). `createDriveAudioSource` now defaults it to `new Date().toISOString()` when not supplied by the caller (`Omit<..., 'createdAt'> & { createdAt?: string }`), mirroring `createPlaylist`'s `options.createdAt ?? new Date().toISOString()` precedent — this is the "stamp at construction" half for objects that never reach `saveSource` unsaved (Drive browse/search results). `AsyncStoragePracticeRepository.saveSource` is the authoritative preserve-on-edit boundary: it looks up any existing stored record by id and, if found, always keeps that stored `createdAt` (ignoring whatever the incoming object carries, protecting against a re-synced Drive object clobbering the true original date); if no prior record exists, it trusts the incoming `source.createdAt` (already stamped by the factory at construction time) rather than re-stamping at save time, so a source's "created" moment reflects when it was first discovered/authored, not when it happened to get saved.
  - Compile fallout from the new required field: added explicit `createdAt` values to `PLAYABLE_SOURCE`/`UNSUPPORTED_SOURCE` in the shared `library-test-fixtures.ts`, the inline source literal in `library-files-destination-actions.spec.ts`, and `SAVED_SOURCE` in `use-saved-rehearsal-library.spec.ts` — the only fixtures across the workspace that build a full `DriveAudioSource` object literal rather than going through `createDriveAudioSource`. All other existing fixtures/call sites use the factory and needed no changes. `browse-content.spec.ts`'s two `DriveAudioSource`-shaped literals were left as-is: that file already fails `typecheck-tests` on `main` for an unrelated pre-existing reason (missing `provider`), confirmed via a `git stash` baseline comparison.
  - Fixed a test made newly flaky by the type change: `google-drive.spec.ts`'s `mapDriveFileToAudioSource` `deepEqual` assertion previously checked the full object literal, which no longer works now that the mapped source carries a live `createdAt` timestamp; updated it to assert `createdAt` is a valid ISO date string separately and `deepEqual` the rest via destructuring.
  - Tests: `rehearsal-playback.spec.ts`'s existing `AsyncStoragePracticeRepository` suite (unchanged) already exercises `saveSource` for both the create and edit paths and continues to pass; a dedicated `createdAt` preserve/stamp regression test is added alongside `1.4`-`1.6` coverage in task `1.7`, per that task's existing scope.
  - File-length note: `rehearsal-domain.ts` (336 lines) and `google-drive.spec.ts` (701 lines) both already exceeded the repo's 300-line guideline before this slice (332 and ~697 lines respectively); this slice's net addition is small (4 and 4 lines). Flagged as a follow-up task rather than pulling an unrelated large-scale split into this slice, matching the precedent set in task `2.3.5`.
  - Validation: `nx run-many -t typecheck test -p audio-library-models audio-library-runtime google-drive mobile-rehearsal-player` all pass; `eslint` on touched files clean (only the pre-existing `google-drive.spec.ts` max-lines warning noted above, present before this change).
- [x] 1.4 Add `createdAt: string` to `RehearsalLibraryFolderNode` (`@org/audio-library-models`) and stamp/preserve it through folder creation (`library-files-folder-create-dialog`) and the file-tree persistence path, mirroring `1.3`'s stamp-on-create/preserve-on-edit rule for folders.
  - Decision (not pre-decided by artifacts, resolved from nearby code convention rather than asked): unlike `DriveAudioSource`, folders have no non-persisted "browsed but not yet saved" context and no existing factory (`createDriveAudioSource` has no folder analog), so no new factory was introduced. Instead: `createFolder` in `library-files-operations.ts` (the app-layer create flow, called from `library-files-folder-create-dialog` via `useLibraryFiles`) stamps `createdAt: new Date().toISOString()` directly on the new-folder literal it builds; `AsyncStoragePracticeRepository.saveLibraryFolderNode` is the authoritative preserve-on-edit boundary, mirroring `saveSource` (task `1.3`) — it looks up the folder's prior record in the already-loaded `tree.folders` and keeps that stored `createdAt` when present, otherwise trusts the incoming value. `moveFolder`/`renameFolder` already spread the full existing folder object before calling `saveLibraryFolderNode`, so `createdAt` passes through those edit paths with no code change needed.
  - Root-folder fix (in-scope for this task, not deferred to `1.5`'s backfill): `createRehearsalLibraryRootFolderNode()` is reconstructed fresh on every `syncRehearsalLibraryFileTree` call (the file-tree persistence path, run on every read/write), which previously discarded any `createdAt` an existing root folder had on every single sync — a "reset on every normalize pass" bug, not a "backfill a missing value" concern. Fixed by giving the factory an optional `{ createdAt?: string }` param (defaulting to `new Date().toISOString()`, mirroring `createDriveAudioSource`'s pattern) and having `syncRehearsalLibraryFileTree` look up the prior stored root folder's `createdAt` and pass it through. Regression-tested in `rehearsal-library-tree.spec.ts`'s existing "preserves hard links..." sync test, which already exercises a root-folder rename during sync — extended its fixtures with an explicit `createdAt` on the input root folder and asserted the synced output keeps that exact value.
  - Compile fallout: this repo has no shared/exported fixture for `RehearsalLibraryFolderNode` (unlike sources' `library-test-fixtures.ts`) — every test builds folder literals inline, so the newly-required field touched ~20 call sites across 9 spec files. The repetitive `folders: [...]` array literals in `library-files-model-browse.spec.ts`, `library-files-model-search.spec.ts`, `library-files-model-sort.spec.ts`, `library-files-operations.spec.ts`, and `files-view.spec.tsx` were patched with a small bracket-matching script (inserting `createdAt` into every folder object literal within `folders: [...]` blocks) rather than by hand, then diff-reviewed; the remaining single-object-literal sites (`files-row-actions.spec.ts`, `files-view-model.spec.ts`, `library-files-destination-actions.spec.ts`'s `pendingAction.row.folder`, plus `rehearsal-library-files.spec.ts`/`rehearsal-library-tags.spec.ts`/`rehearsal-library-tree.spec.ts`/`rehearsal-playback.spec.ts`/`practice-repository-storage.spec.ts` in `audio-library-runtime`) were edited directly. `library-files-destination-actions.spec.ts`'s `destinationFolders` fixtures were left untouched — that field is typed as a narrower `Pick`-style shape that doesn't include `createdAt`, confirmed by the typecheck error surface not flagging those literals.
  - A second `deepEqual`-vs-live-timestamp issue (same shape as task `1.3`'s `google-drive.spec.ts` fix) surfaced in `rehearsal-playback.spec.ts`'s "migrates canonical library entities into a root folder file tree" test: `listLibraryFileTree` on a fresh (never-synced) tree stamps the root folder with a live `new Date().toISOString()`, which a hardcoded `deepEqual` can't match — fixed by reading the actual result's `rootFolder.createdAt`, asserting it parses as a valid date, and reusing that value in the expected-shape `deepEqual`.
  - Tests: `rehearsal-library-tree.spec.ts`'s extended sync test (above) is the primary new regression coverage; all other edits are fixture updates to existing assertions, no new test files needed for this subtask specifically (backfill-focused tests are task `1.7`'s scope).
  - File-length note: `rehearsal-library-files.spec.ts`, `rehearsal-playback.spec.ts`, `library-files-operations.spec.ts`, and `library-files-operations.ts` all already exceeded the 300-line guideline before this slice's few-line-per-file additions (confirmed via `git diff --stat`); flagged rather than fixed here, consistent with `1.3`'s and `2.3.5`'s precedent.
  - Validation: `nx run-many -t typecheck test -p audio-library-models audio-library-runtime google-drive mobile-rehearsal-player` all pass (11+30+17+406 tests green); `mobile-rehearsal-player:typecheck-tests` shows exactly the same pre-existing baseline failures as before this slice (confirmed unchanged, none newly introduced); `eslint` on touched files clean except the four pre-existing max-lines warnings noted above.
- [x] 1.5 Backfill `createdAt` for already-persisted sources and folders that predate `1.3`/`1.4`: extend `AsyncStoragePracticeRepository`'s existing "normalize on read, write back if changed" idiom (already used by `listLoops()`/`listPlaylists()` via `normalizeStoredLoops`/`normalizePlaylist`) to sources and the folder tree, stamping any record missing `createdAt` with the current time on next read and rewriting storage.
  - Implementation: added `normalizeStoredSources` to `async-storage-practice-repository-helpers.ts`, following the exact `normalizeStoredLoops`/`normalizePlaylist` shape; `AsyncStoragePracticeRepository.listSources` now reads, normalizes, diffs via `JSON.stringify`, and writes back only if changed — identical idiom to `listLoops`/`listPlaylists`. For folders, no new wrapper was needed: `syncRehearsalLibraryFileTree` (the file-tree's existing sync/normalize pass, already run on every read via `persistSynchronizedLibraryFileTree`) already iterates every non-root folder, so the per-folder loop now also backfills `createdAt` there, alongside `1.4`'s root-folder fix.
  - New shared helper: `resolveBackfilledCreatedAt(createdAt: unknown): string` (stamps `new Date().toISOString()` when the value isn't a non-empty string). Placed in `rehearsal-library-files.ts` (not the helpers file) and imported into `async-storage-practice-repository-helpers.ts` — the reverse direction would have created a circular import, since the helpers file already imports `REHEARSAL_LIBRARY_ROOT_FOLDER_ID`/`removeRehearsalLibraryFileLinkNode` from `rehearsal-library-files.ts`.
  - Tests: two new focused tests in `rehearsal-playback.spec.ts` — one seeds raw pre-`1.3` source storage (via `omit(AVAILABLE_SOURCE, ['createdAt'])`, `es-toolkit/compat`) and asserts `listSources` backfills a valid ISO `createdAt` and rewrites storage with that same value; one seeds a pre-`1.4` folder tree (a folder literal with no `createdAt`) and asserts `listLibraryFileTree` backfills both the root and the non-root folder and persists the backfilled value.
  - File-length note: `rehearsal-playback.spec.ts` (652 lines) was already ~575 lines (well over the 300-line guideline) before this slice's ~80 lines of new test coverage — flagged, not fixed, consistent with prior precedent; worth folding into the existing oversized-file follow-up alongside `rehearsal-domain.ts` and `google-drive.spec.ts`.
  - Validation: `nx run-many -t typecheck test -p audio-library-models audio-library-runtime google-drive mobile-rehearsal-player` all pass (11+32+17+406 tests green, up from 30 in `audio-library-runtime` due to the 2 new backfill tests); `eslint` on touched files clean.
- [x] 1.6 Add `tagAddedAt?: Record<string, string>` (tag → ISO date) alongside `tags?: string[]` on all four entity types (`@org/audio-library-models`), maintained by the tag-write path: newly-added tags are stamped with the current time, removed tags drop their entry. Backfill missing entries (tags present in `tags[]` with no recorded add date) using the entity's own `createdAt` during the same normalize-on-read pass as `1.5`.
  - Decision (confirmed with user): a `Record` sidecar was chosen over changing `tags` to `{tag, addedAt}[]` to avoid touching the roughly dozen existing `tags: string[]` read sites across `audio-library-models`/`audio-library-runtime`/the app's tag editor, search, and loop utilities. See design.md's "Sources and folders gain creation timestamps..." decision for the full rationale.
  - Decision (not pre-decided by artifacts, resolved from design.md's own wording — "maintained by the tag-write path (`saveTagEdits`'s underlying `saveSource`/`saveLoop`/`updatePlaylist` calls...)"): the stamp/drop logic is centralized in `AsyncStoragePracticeRepository`'s four `save*` methods (`saveSource`, `saveLoop`, `savePlaylist`, `saveLibraryFolderNode`), not in the UI-level `saveTagEdits` hook — mirroring `1.3`/`1.4`'s repository-is-authoritative pattern for `createdAt`. This also transparently covers the folder tag-edit path, which currently has no live UI (`files-row-actions.ts`'s "Edit tags" menu item for folders is a permanently-disabled stub) — once that UI is wired up in a future change, it gets correct `tagAddedAt` behavior for free.
  - New shared helpers in `rehearsal-tags.ts` (`@org/audio-library-models`, exported from the package root): `resolveTagAddedAt(tags, priorTagAddedAt, fallbackAddedAt)` — pure, rebuilds the map from the current `tags[]`, reusing each tag's recorded date if present or stamping `fallbackAddedAt` if not (naturally "drops" entries for removed tags, since it only ever includes keys for tags currently present); `withResolvedTagAddedAt(entity, priorTagAddedAt, fallbackAddedAt)` — object-merge wrapper that explicitly `delete`s any stale `tagAddedAt` key before conditionally re-adding it, rather than an object-spread `...entity, tagAddedAt: maybeUndefined`, which would otherwise leave an explicit `tagAddedAt: undefined` own-property on the result — this was caught by 6 failing `deepEqual` tests in `rehearsal-playback.spec.ts` (Node's `assert/strict` `deepEqual` is strict-equal and treats a present-but-`undefined` key as different from an absent key) before being fixed.
  - Critical non-obvious constraint discovered during implementation (not spec'd, had to trace the call graph): `normalizeStoredLoops`/`normalizeStoredLoop` (loops) and `normalizePlaylist` (playlists) are each called from **both** their respective read path (`listLoops`/`listPlaylists`) **and** their write path (`saveLoop`/`savePlaylist`, to structurally validate/normalize the incoming entity before persisting). Baking `tagAddedAt` backfill (fallback = entity's own `createdAt`) directly into either function would incorrectly apply that "old data" fallback to a genuinely brand-new tag added in the current edit (which should get "now", not the entity's original creation date) — because on the write path, these functions run on the *incoming-to-save* object, not just prior stored state. Fix: left `normalizeStoredLoops`/`normalizeStoredLoop`/`normalizePlaylist` untouched (only `isNamedLoop`'s type guard was extended to tolerate a `tagAddedAt` value passing through without being stripped); backfill (createdAt fallback) is applied explicitly and only in `listLoops`/`listPlaylists`, and the stamp-with-now logic is applied explicitly and only in `saveLoop`/`savePlaylist`, each using a freshly-fetched prior record as the "already recorded" source of truth. By contrast, `normalizeStoredSources` (sources) and `syncRehearsalLibraryFileTree`'s per-folder loop (folders) are *only* ever invoked on prior/stored state (confirmed by re-reading `saveSource`/`saveLibraryFolderNode`'s call graphs) — so backfill was safely baked directly into those two functions, matching `1.5`'s pattern.
  - Type guards extended to tolerate (not require) `tagAddedAt`, mirroring how `tags` itself is already optional/permissive in both guards — so already-persisted pre-`1.6` data without the field keeps parsing successfully and gets backfilled on next read, rather than being rejected: `isNamedLoop` (`rehearsal-domain.ts`, new local `isStringRecord` helper) and `isRehearsalLibraryFolderNode` (`rehearsal-library-files.ts`, its own local `isStringRecord` — duplicated rather than shared, following each file's existing self-contained-validator convention).
  - Tests: `rehearsal-tags.spec.ts` covers `resolveTagAddedAt` (stamp-new, preserve-existing, drop-removed, empty-tags-returns-undefined) and `withResolvedTagAddedAt` (adds key when present, omits key entirely — even over a stale prior entry — when tags are empty, no-mutation guarantee). `rehearsal-playback.spec.ts` adds repository-level coverage: a combined stamp/preserve/drop sequence through three consecutive `saveSource` calls, the same sequence through `saveLibraryFolderNode`, and a backfill-on-read test seeding raw legacy-shaped loop/playlist storage (tags present, no `tagAddedAt`) and asserting `listLoops`/`listPlaylists` backfill using each entity's own `createdAt`.
  - File-length note: `rehearsal-domain.ts` and `rehearsal-playback.spec.ts` both grew further on top of the pre-existing overages already flagged in `1.3`'s and `1.5`'s notes (`rehearsal-playback.spec.ts` is now 754 lines) — still flagged, not fixed, per established precedent; the standing oversized-file follow-up task should be expanded to include `rehearsal-playback.spec.ts` alongside `rehearsal-domain.ts`/`google-drive.spec.ts`.
  - Validation: `nx run-many -t typecheck test -p audio-library-models audio-library-runtime google-drive mobile-rehearsal-player` all pass (18+35+17+406 = 476 tests green); `eslint` on touched files clean except the two pre-existing max-lines warnings noted above.
- [x] 1.7 Add focused unit tests for `1.3`-`1.6`: stamp-on-create, preserve-on-edit, and backfill-on-read for source/folder `createdAt`, and stamp/drop/backfill behavior for `tagAddedAt`.
  - Coverage audit: `1.3`-`1.6` were each implemented with focused tests added in the same slice (per this repo's deliberate-execution convention), so most of `1.7`'s scope was already satisfied incrementally: backfill-on-read for source/folder `createdAt` (`1.5`'s two tests), root-folder `createdAt` preservation across sync/rename (`1.4`'s `rehearsal-library-tree.spec.ts` extension), and stamp/drop/backfill for `tagAddedAt` across all four entity types (`1.6`'s three repository tests plus `rehearsal-tags.spec.ts`'s pure-function unit tests).
  - Real gap found and closed: no existing test distinguished "preserve the *stored* `createdAt` on edit" from "the incoming object already happened to carry the same value" — every existing fixture (`AVAILABLE_SOURCE`, etc.) reused one fixed `createdAt`, so a regression that reversed the `priorSource?.createdAt ?? source.createdAt` fallback order would not have been caught. Added two new tests to `rehearsal-playback.spec.ts`: `saveSource` and `saveLibraryFolderNode`, each saved twice with a deliberately different `createdAt` on the second (edit) call, asserting the *original* stored value wins while an unrelated field (name) does update.
  - Validation: `nx run-many -t typecheck test -p audio-library-models audio-library-runtime google-drive mobile-rehearsal-player` all pass (18+37+17+406 = 478 tests green); `eslint` on the touched file clean except the same pre-existing max-lines warning already tracked in the oversized-file follow-up.
- [x] 1.8 Extend `aggregateRehearsalLibraryTags` to include each tag's creation date (the earliest `tagAddedAt` across every entity carrying that tag, from `1.6`) in its returned `RehearsalLibraryTagUsage` shape, so tag-list sorting by creation date (task `2.6`) has real data to sort on. Add focused unit tests alongside `1.7`/existing aggregation tests.
  - Decision (not pre-decided by artifacts, resolved from nearby code convention): named the new field `createdAt: string` on `RehearsalLibraryTagUsage`, matching the naming convention already used for "when this thing came into existence" everywhere else in the domain (`DriveAudioSource.createdAt`, `NamedLoop.createdAt`, etc.), rather than inventing a new name like `addedAt`.
  - Implementation: `collectEntityTagLists` (returning `string[][]`) became `collectEntityTagInfos` (returning `{ tags, tagAddedAt }[]`), threading each entity's `tagAddedAt` sidecar map alongside its `tags` array. During aggregation, each occurrence looks up `entityTagInfo.tagAddedAt?.[tag]` (falling back to `new Date().toISOString()` if genuinely absent — mirroring `resolveBackfilledCreatedAt`'s "honest current-time approximation" precedent from `1.5`, for the defensive case where a caller passes entities that haven't been through the repository's backfill-on-read path) and keeps the minimum (lexicographic ISO-string comparison, which is chronologically correct since every value comes from `.toISOString()`) across every entity carrying that tag — not just the first-seen entity's date, since first-seen casing and earliest add-date are independent and can point at different entities.
  - Tests: extended the existing shared fixtures (`SOPRANO_SOURCE`/`ALTO_LOOP`/`PLAYLIST`/`FOLDER`) with distinct, non-matching `tagAddedAt` values per tag so the main dedupe/count test's expected `createdAt` values are a real regression guard, not incidentally-equal dates. Added two new dedicated tests: one proving `createdAt` reflects the earliest date across entities even when the first-seen entity for a tag is not the one with the earliest date (two sources with the same tag, different `tagAddedAt`, added to the collection in newest-first order); one proving the fallback produces a valid parseable timestamp when a tag has no recorded `tagAddedAt` entry at all.
  - Fallout check: grepped every consumer of `RehearsalLibraryTagUsage`/`aggregateRehearsalLibraryTags` across the app (Recents, the Library Tags view, `browse-content.tsx`, `app-router.tsx`) — all destructure only `tag`/`count`, so the new required `createdAt` field caused no compile fallout; confirmed via a full `mobile-rehearsal-player` typecheck/test pass.
  - Validation: `nx run-many -t typecheck test -p audio-library-models audio-library-runtime google-drive mobile-rehearsal-player` all pass (18+39+17+406 = 480 tests green); `eslint` on touched files clean (no warnings).
- [x] 1.9 Add folder tag editing to Library Files, mirroring the existing track/loop/playlist tag-editor wiring. **Scope change (confirmed with user):** originally out of scope per design.md's Non-Goals; added after discovering `files-row-actions.ts`'s "Edit tags" folder action was a permanently-disabled stub with no live write path, leaving `1.6`'s `tagAddedAt` stamping for folders unreachable through the UI. See design.md's "Folder tag editing is added..." decision.
  - Implementation matched the plan exactly: `saveFolderTags` operation added to `library-files-operations.ts` (mirrors `moveFolder`), exposed from `useLibraryFiles()`; `folder` variant added to `TagEditorTarget`, `openFolderTagEditor(folder)` action added, `saveTagEdits`'s folder branch wired to `saveFolderTags`; `resolveFolderMenuActions`'s "Edit tags" stub replaced with a real enabled action; `onOpenFolderTagEditor` threaded through the full chain (`files-row-actions-model.ts` → `use-library-files-row-action-flows.tsx` → `files-view.tsx` → `browse-content-types.ts`/`browse-content.tsx` → `index.tsx`, sourcing `saveFolderTags` via `libraryFiles.saveFolderTags` threaded into `use-saved-rehearsal-library-section-state.ts`).
  - Refactor for testability: extracted the hook's inline `resolveTagsAndTitle` closure into a standalone exported pure function `resolveTagEditorTagsAndTitle(target: TagEditorTarget)` in the same file — this hook had zero test coverage for any of its branches before this task (no spec file existed at all), so the extraction adds real regression coverage for all four kinds (source/loop/playlist/folder), not just the new one.
  - Tests added: `library-files-operations.spec.ts` — `saveFolderTags` success and error/recovery-absent paths, mirroring `moveFolder`'s conventions; `files-row-actions.spec.ts` — extended the existing folder-menu test to invoke the now-enabled "Edit tags" action and assert it calls through; new `use-saved-rehearsal-library-tag-editor.spec.ts` — 6 tests covering `resolveTagEditorTagsAndTitle` for all five states (source/loop/playlist/folder/none) plus the tags-defaults-to-empty-array case.
  - Fallout caught during this slice's typecheck sweep that traced back to task `1.8` (not this task): `saved-tags-list/model.spec.ts` had 12 sites constructing `RehearsalLibraryTagUsage` literals missing the `createdAt` field added in `1.8` — missed at the time because that checkpoint's validation ran `typecheck` but not `typecheck-tests` (the two are separate Nx targets; `typecheck-tests` covers `tsconfig.spec.json`, which is what actually type-checks `*.spec.ts` files against the app's own tsconfig). Fixed as part of this slice's cleanup rather than left for later.
  - Manually verified in the integrated browser (the exact flow the user's original report was about): opened the "test" folder's overflow menu — "Edit tags" is now enabled (previously permanently greyed out); tapped it, the tag editor sheet opened titled "Folder tags • test"; added "Alto, Warmup", saved; reloaded the app; confirmed both tags now appear in the Library Tags view (1 item each) and in the Recents "Popular tags" module; reopened the folder's tag editor and confirmed it was pre-filled with the persisted tags; removed both tags and saved to restore the demo environment to its prior state. No new console errors at any step (only the pre-existing Drive/auth 400/401/COOP noise already documented elsewhere in this file).
  - File-length note: `index.tsx`, `use-library-files-row-action-flows.tsx`, `library-files-operations.ts`, and `library-files-operations.spec.ts` all grew further on top of pre-existing overages (confirmed via `git diff --stat` — each was already hundreds of lines over 300 before this slice's additions); flagged via a new follow-up task scoped to `packages/mobile-rehearsal-player`'s Library Files files, separate from the existing `audio-library-models`/`audio-library-runtime` follow-up.
  - Validation: `nx run-many -t typecheck test -p audio-library-models audio-library-runtime google-drive mobile-rehearsal-player` all pass (488 tests green, up from 480); `mobile-rehearsal-player:typecheck-tests` matches the pre-existing baseline exactly (17 unique errors, all pre-existing); `eslint` on touched files clean except four pre-existing max-lines warnings (all pre-existing overages, none newly created).
  - Add focused tests: extend `library-files-operations.spec.ts` for the new `saveFolderTags` operation (success + error/recovery path, matching `moveFolder`'s existing coverage); update `files-row-actions.spec.ts`'s existing "keeps folder overflow aligned with standard Files management actions" test (which today asserts the stub `Edit tags` action stays inert) to assert it now calls through correctly; add hook-level coverage for the `folder` branch of `saveTagEdits`/`openFolderTagEditor`/title/tags derivation (new colocated spec if none exists for `use-saved-rehearsal-library-tag-editor.ts` — confirmed none does today).
  - Manual verification needed: open a folder's overflow menu in the integrated browser, confirm "Edit tags" is enabled and opens the tag editor sheet pre-filled with the folder's current tags; add/remove tags and save; confirm the folder's tags persist across a reload and that the Library Tags view / Recents tag module reflect the folder's tags. This directly re-tests the gap that prompted this task.

## 2. Recents Tag Module

- [x] 2.1 Replace the hardcoded `RECENTS_SHORTCUT_TAGS` array and the no-op `onPlayRecentShortcut` wiring in `screens/recents/` with the real aggregated tag list from `1.1`, capped and ordered most-used-first with an alphabetical tie-break.
  - Decision (confirmed with user): cap = 6.
  - Data wiring: extend `useLibraryFiles`'s return with `folders: tree?.folders ?? []` (only `destinationFolders` was previously exposed). In `app-router.tsx`, add a `useMemo` calling `aggregateRehearsalLibraryTags` over `libraryController.savedLibrary.{savedLibrarySources,savedLoops,files.folders}` + `libraryController.playlists.savedPlaylists`, matching the existing `savedSourceIds`/`savedLoopIds` memo pattern there. Thread the resulting (already-sorted) `RehearsalLibraryTagUsage[]` down through `AppRouterRecentsScreen` into `RecentsScreen` as a new `libraryTagUsage` prop (full uncapped list, so tasks `2.2`/`2.3` can reuse it for empty-state/overflow checks without another prop-shape change).
  - Display: in `screens/recents/index.tsx`, replace `RECENTS_SHORTCUT_TAGS` with a `RECENTS_SHORTCUT_TAG_CAP = 6` constant and `shortcutTags = libraryTagUsage.slice(0, RECENTS_SHORTCUT_TAG_CAP).map((usage) => usage.tag)`, used everywhere `RECENTS_SHORTCUT_TAGS` was used.
  - Out of scope for this slice (left as-is / deferred): `onPlayRecentShortcut`'s no-op resume-most-recent behavior (task `2.4` replaces it with real navigation once task `3.1`'s tag detail screen exists), and the empty/overflow UI states (tasks `2.2`/`2.3`).
  - Bug found and fixed during manual verification (pre-existing, predates this change, in code from the archived `streamline-mobile-rehearsal-ux` change): `resolveSavedRehearsalLibrarySources` (`saved-rehearsal-library/view-model.ts`) silently discarded a saved source's `tags` field whenever that source's id also appeared in the currently-cached Drive browse/search snapshot (`visibleSourcesById[source.id] ?? source` did a full replacement with the tag-less Drive-fetched object instead of a merge). This made tag edits vanish for any source Drive still had cached, and blocked verifying this entire feature. Fixed by preserving `source.tags` across the merge; added a regression test (`view-model.spec.ts`). Also renamed the Recents module title from "Popular shortcuts" to "Popular tags" per user request (not previously specified in this change's artifacts).
- [x] 2.2 Replace the always-shown placeholder chip row with concise empty-state guidance when no saved entity carries a tag yet.
  - Decision (confirmed with user): copy is "No tags yet. Tag a track, loop, playlist, or folder in Library to see it here." — matches the existing tag-editor-sheet empty-state convention.
  - Implementation: new pure `getRecentsTagModuleCopy({ hasSavedTagUsage })` in `screen-copy.ts` returning `{ body }`, unit-tested in `screen-copy.spec.ts` now (same slice), ahead of task `2.5`'s broader test sweep. In `index.tsx`, derive `hasSavedTagUsage = libraryTagUsage.length > 0`; show the body text when `!hasSavedTagUsage || !isRecentPlaybackAvailable` (empty-state guidance always shows regardless of recent-playback state; the existing "optional shortcuts" hint keeps its prior recent-playback-gated behavior when tags do exist); only render `shortcutsMeta` and the chip `tagRow` when `hasSavedTagUsage` is true — no placeholder chips.
- [x] 2.3 Add an overflow entry point to a full tag list when more tags exist than the Recents module's compact display cap.
  - Decision (confirmed with user, supersedes design.md's originally-considered "reuse Library's filter surface" alternative): build a dedicated **Tags** view as a fifth entry in Library's existing Files/Tracks/Loops/Playlists view-switcher, listing every in-use tag with its usage count, sortable by name or count (ascending/descending), and searchable. The Recents overflow action navigates there. See subtasks `2.3.1`-`2.3.7` and the updated design.md/proposal.md/spec.md.
- [x] 2.3.1 Add `'tags'` to the `SavedRehearsalLibraryView` union and wire it into the view-switcher chip row, `detail-mode.ts`'s visible-sections table, and the existing view-switcher lock machinery (`view-switcher-lock-model.ts`), matching the integration pattern already used for Files/Tracks/Loops/Playlists.
- [x] 2.3.2 Build the Tags list: render `aggregateRehearsalLibraryTags` output as rows (tag name + usage count) using the existing row shell conventions.
  - Implementation: new `SavedTagsList` component at `packages/mobile-rehearsal-player/src/app/library/tags/components/saved-tags-list/index.tsx` — a new `library/tags/` domain folder sibling to `library/loops/`, `library/playlists/`, `library/drive/`, `library/search/`, per the folder-structure convention's "avoid adding more unrelated siblings to already flat directories like `library/components/`" rule (that directory is already 48 files). Renders one `ExplorerListRow` per tag inside `ExplorerListSurface` (same row shell `BrowseSourceGroup`/`SavedLoopListRow` use), `title` = tag name, `metadata` = a pluralized usage-count label (`getSavedTagUsageMetadataLabel` in a colocated `model.ts`/`model.spec.ts`, matching the `explorer/model.ts` colocation pattern), `leadingIcon` = `tag-outline` (matching the `folder-outline`/`playlist-music-outline`/`music-note-outline` per-entity-type icon convention). Falls back to concise "No tags yet…" body text when the list is empty (not spec-required for this view, but avoids a blank screen; kept to one string constant, no new empty-state component). Row `onPress` navigation is deferred to task `2.3.5`.
  - Data wiring decision (not spec'd, decided from nearby code convention rather than asked): `browse-content.tsx` already receives `savedLibrarySources`/`savedLoops`/`savedPlaylists`/`libraryFiles` (which already exposes `.folders`, added in task `2.1`) as props, so it computes its own `useMemo(() => aggregateRehearsalLibraryTags(...))` locally rather than threading the Recents-side `libraryTagUsage` memo down from `app-router.tsx` through `LibraryScreen`. This matches the existing pattern where `LibraryScreen` derives its own state from `libraryController`/raw collections instead of consuming app-router-computed memos (which today are only threaded to `AppRouterRecentsScreen`), and both call sites share the same pure `aggregateRehearsalLibraryTags` source of truth, so there's no drift risk despite the duplicate `useMemo` call site.
  - New render branch: added `shouldRenderSavedTagsList(selectedView)` to `browse-content-model.ts` (tested in `browse-content.spec.ts`), mirroring `shouldRenderFilesExplorer`'s early-return pattern in `browse-content.tsx`.
  - Tests: `model.spec.ts` (singular/plural usage-count label) and a `browse-content.spec.ts` case for `shouldRenderSavedTagsList`. No render/interaction test — this repo's `*.spec.tsx` convention (confirmed by scanning existing row components like `BrowseSourceGroup`/`SavedLoopListRow`) tests companion pure logic/view-model files, not RN component rendering, so the pure label-formatting logic is what's covered.
  - Validation: `nx run mobile-rehearsal-player:test-pattern -- --pattern='src/app/library/tags/**/*.spec.ts'`, `nx run mobile-rehearsal-player:test-file -- --file=src/app/library/components/saved-rehearsal-library-section/browse-content.spec.ts`, `nx run mobile-rehearsal-player:typecheck`, `nx run mobile-rehearsal-player:lint` all pass; lint warnings present are pre-existing and outside touched files.
- [x] 2.3.3 Add a sort control (Name / Count, ascending/descending) for the Tags list.
  - Decision (confirmed with user): no existing ascending/descending toggle precedent existed anywhere in the codebase (Files' sort is a single fixed-direction enum), so this was a genuine UX-shape choice, not one nearby code already decided. Confirmed approach: two field chips (Name/Count, using the existing `InteractionChip`) plus a separate direction-toggle icon button (`SurfaceIconButton`, `sort-ascending`/`sort-descending` glyphs), rather than 4 combined chips or a tap-again-to-reverse interaction. Placement is inline above the list inside `SavedTagsList` itself (not behind the header filter-popover Files uses, since the Tags view only has two controls total - sort here, search in `2.3.4` - so a popover would add a click for no crowding benefit).
  - Implementation: `model.ts` gained `SavedTagsListSortField`/`SavedTagsListSortDirection`/`SavedTagsListSortState`, `DEFAULT_SAVED_TAGS_LIST_SORT_STATE` (`{ field: 'count', direction: 'desc' }`, reproducing `aggregateRehearsalLibraryTags`'s existing default order), `SAVED_TAGS_LIST_SORT_FIELD_OPTIONS`, `sortSavedTagUsage` (count field respects direction on the count itself but always tie-breaks alphabetically ascending, regardless of direction, matching the `1.1` aggregation convention rather than naively reversing the whole comparator), and `getSavedTagsListSortDirectionToggleLabel` (describes the action the button performs next, e.g. `'Sort ascending'`, matching the `getQueueToggleAction` accessibility-label convention in `playback-session-mode-card.tsx`). `index.tsx` holds local `useState(DEFAULT_SAVED_TAGS_LIST_SORT_STATE)` and a `useMemo(() => sortSavedTagUsage(tagUsage, sortState))`; the sort row is hidden along with the rest of the list content when there are no tags.
  - Tests: `model.spec.ts` covers all four field/direction combinations (asserting the tie-break stays alphabetical-ascending under both count directions), the no-mutation guarantee, and both toggle-label branches.
  - Manually verified in the integrated browser: Count/desc default renders most-used-first-then-alphabetical; switching to Name keeps direction and re-sorts descending by name; toggling direction re-sorts ascending; no console errors introduced (pre-existing Drive/auth 400/401 noise only).
  - Validation: `nx run mobile-rehearsal-player:test-pattern -- --pattern='src/app/library/tags/**/*.spec.ts'`, `nx run mobile-rehearsal-player:typecheck`, and `eslint` on touched files all pass clean.
- [x] 2.3.4 Add a search input scoped to the Tags list, reusing the shared contextual search panel scaffold.
  - First pass gave `SavedTagsList` its own local `ContextualSearchPanel` instance (own magnifier icon, own query state, `canShowRecentSearchTerms={false}`). **User feedback overrode this**: the app already has one top-level search icon in the Library header; Tags should not add a second one, and should support recent searches "like in all other search contexts" instead of opting out of them.
  - Corrected implementation: removed `SavedTagsList`'s own search UI entirely (`ContextualSearchPanel`, its `useState` query/visibility, the `noop`/`es-toolkit` import). The header's existing search icon (`LibrarySearchControlsActions`/`searchPanel.handleSearchActionPress`) and its `ContextualSearchPanel` instance (rendered by `SavedRehearsalLibrarySearchShell` → `LibrarySearchControls`, already positioned above the view content for every view) were already mounted regardless of `selectedView` - they just weren't being read for Tags. `SavedTagsList` now takes a `searchQuery: string | null` prop instead of owning its own; `browse-content.tsx` passes `searchState.activeLibrarySearchQuery` (already available there via the existing `SearchState` Pick type - no new prop threading needed) into it. Filtering (`filterSavedTagUsageByQuery`) and row highlighting (`SearchHighlightedText`) are unchanged, just fed by the shared query instead of a local one.
  - Recent-search support came "for free": the header's `ContextualSearchPanel` is already bound to `searchState.recentLibrarySearchTerms` / `commitLibrarySearchQuery` (the same `LIBRARY_RECENT_SEARCH_HISTORY_KEY`-backed history Files/Tracks/Loops/Playlists already share on this one screen). Since Tags now reads from that same `searchState`, a tag-name search submitted while on the Tags view is recorded into and suggested from that same recent-searches list, matching "all other search contexts" on this screen.
  - Visibility/gating behavior: did not special-case `'tags'` in `shouldRenderSavedLibraryBrowseContent` (`browse-content-model.ts`) - Tags now behaves exactly like Tracks/Loops/Playlists already do (browse content, including the tag list, hides while the header search bar is open with no committed query yet, and reappears once a query is active). This is zero new gating logic, just relying on the existing convention.
  - Fixed stale copy found while wiring this: `getLibrarySearchContextCopy()` (`library/drive/utils/drive-library-metadata.ts`) hard-coded "Search saved library (playlists, tracks, and loops)", which became inaccurate once the same header search bar also covers Tags. Updated to "...tracks, loops, and tags" and updated the matching assertion in `drive-library-view-model.spec.ts`.
  - Tests: `model.spec.ts` still covers `filterSavedTagUsageByQuery` (empty/whitespace query passthrough, case-insensitive substring match, query trimming, no-match empty array) - unaffected by where the query now comes from. Updated `drive-library-view-model.spec.ts`'s search-copy assertion.
  - Manually verified in the integrated browser: opening the one header search icon while on Tags shows updated helper copy mentioning tags; typing narrows the list and highlights matches; closing and reopening the search bar surfaces the typed term under "RECENT SEARCHES" and reselecting it re-filters; no second search icon anywhere on the Tags view. No new console errors (only pre-existing Drive/auth 400/401/COOP noise).
  - Validation: `nx run mobile-rehearsal-player:test-pattern -- --pattern='src/app/library/tags/**/*.spec.ts'`, `nx run mobile-rehearsal-player:test-file -- --file=src/app/library/drive/utils/drive-library-view-model.spec.ts`, `nx run mobile-rehearsal-player:typecheck` (via `tsc -b`), and `eslint` on touched files all pass clean.
- [x] 2.3.5 Wire Tags-list row taps to navigate to the tag detail screen (task `3.1`) with the selected tag.
  - Implementation: threaded a new `onSelectTag: (tag: string) => void` prop through the existing explicit-prop-passing chain from `AppRouter` (which owns `selectedTag`/`setSelectedTag` from task `3.1`) down to `SavedTagsList`: `AppRouter` → `LibraryScreen` (`library-screen-types.ts`) → `SavedRehearsalLibrarySection` (`types.ts`) → `SavedRehearsalLibraryBrowseContent` (`browse-content-types.ts`, added to its `Pick<SavedRehearsalLibrarySectionProps, ...>` list) → `SavedTagsList`, matching the exact existing pattern already used for `onOpenSourceTagEditor`/`onOpenLoopTagEditor`/`onOpenPlaylistTagEditor`. `SavedTagsList` wires `onPress={() => onSelectTag(usage.tag)}` on each `ExplorerListRow`.
  - Found and fixed a second, previously-orphaned call site: `DriveLibrarySavedLibraryPanel` (`library/drive/components/drive-library-section/saved-library-panel.tsx`) also renders `SavedRehearsalLibrarySection` (with its own internal, uncontrolled `selectedView`, so the Tags view is technically reachable there too) but has zero importers anywhere in the app (`DriveLibrarySection`, its wrapping export, is dead/unreachable code, superseded by `DriveDiscoveryPanel`). Satisfied the new required prop there with `onSelectTag={() => undefined}`, matching that file's own existing convention for other not-meaningful-here callbacks (`onDismissLibraryFilesSuccessFeedback`, `onOpenLibraryFilesSuccessFeedbackFolder`, `onShowLibraryFilesSuccessFeedback` all already do this). Did not attempt to remove the dead code itself — out of scope for this subtask.
  - Also flagged (not fixed, out of scope): `saved-rehearsal-library-section/index.tsx` was already 315 lines before this session (pre-dating this change), now 317 after this 2-line addition, over the repo's 300-line guideline. Spawned a follow-up task rather than pulling an unrelated extraction into this slice.
  - Tests: no new automated test — the added logic is a single-line pass-through (`onPress={() => onSelectTag(usage.tag)}`) with no branching, matching the established no-render-test convention for this component family (see task `2.3.2`'s note).
  - Manually verified in the integrated browser: tapping a tag row (e.g. "Soprano") opens the tag detail overlay with `TAG` eyebrow + tag name and the mini player dock/tab bar still visible underneath; tapping back returns to the Tags list in place. No new console errors (only pre-existing Drive/auth 400/401/COOP noise).
  - Validation: `nx run mobile-rehearsal-player:typecheck`, `eslint` on touched files (clean except the pre-existing file-length warning noted above), and `nx run mobile-rehearsal-player:test` (403/403 passing) all pass.
- [x] 2.3.6 Wire the Recents tag module's overflow action (from `2.3`) to navigate to the new Library Tags view.
  - Found while implementing: no overflow entry-point UI existed yet in the Recents tag module (subtasks `2.3.1`-`2.3.5` only built the Library Tags view itself and its row navigation) — this subtask builds the trigger too, not just its wiring.
  - Cross-tab navigation-with-parameter problem, same category task `3.1` solved for tag detail: `LibraryScreen.selectedView` is local state with no external control, so opening Library "already on the Tags view" from Recents (a different shell tab) needed a new signal. Mirrored the existing `requestedDestination`/`requestedDestinationRequestId` pattern already used for tab switching: `AppRouter` gained `requestedLibraryView`/`requestedLibraryViewRequestId` state plus a `requestLibraryView(view)` setter, threaded into `LibraryScreen` as `requestedView`/`requestedViewRequestId` props; `LibraryScreen` reacts via a `useEffect` calling `setSelectedView(requestedView)`, identical in shape to `MobileShell`'s existing destination-sync effect. The Recents overflow action calls both `requestDestination('library')` and `requestLibraryView('tags')`.
  - UI: added a trailing `SurfaceIconButton` (`chevron-right`, reusing the shared icon-button component already used elsewhere, e.g. the Tags-list sort toggle) in the "Popular tags" module header, shown only when `libraryTagUsage.length > RECENTS_SHORTCUT_TAG_CAP`.
  - Tests: no new automated test — the added logic is a boolean threshold (`hasOverflowTags`) already exercised implicitly by the existing `libraryTagUsage`-driven rendering, plus pass-through navigation wiring with no branching of its own.
  - Manually verified in the integrated browser: added 3 extra tags via the tag editor to exceed the cap of 6 (7 total distinct tags), confirmed the overflow chevron appears on Recents, tapping it switches to the Library tab with the Tags view pre-selected showing all 7 tags, then removed the 3 added tags to restore the demo environment to its prior state. No new console errors.
  - Validation: `nx run mobile-rehearsal-player:typecheck`, `eslint` on touched files, and `nx run mobile-rehearsal-player:test` (401/401 passing) all pass.
- [x] 2.3.7 Add focused tests for the Tags view-model: sort orders (name asc/desc, count asc/desc, tie-break) and search filtering.
  - Already satisfied by test coverage added during tasks `2.3.3`/`2.3.4`: `model.spec.ts`'s `sortSavedTagUsage` suite covers count desc (with alphabetical tie-break for equal counts), count asc (tie-break not reversed), name asc, name desc, and a no-mutation guarantee; `filterSavedTagUsageByQuery` suite covers empty/whitespace query passthrough, case-insensitive substring match, query trimming, and no-match. No new code needed — confirmed the 13/13 tests in this file still pass.
  - Validation: `nx run mobile-rehearsal-player:test-pattern -- --pattern='src/app/library/tags/**/*.spec.ts'` (13/13 passing).
- [x] 2.4 Wire tag chip taps to navigate to the new tag detail screen (task `3.1`) with the selected tag, instead of the current no-op shortcut handler.
  - Decision (confirmed with user): the pre-existing chip only had one tap target — a nested "play" icon (`CompactPlaybackAction`) disabled unless a recent-playback item existed to resume — while the `InteractionChip` itself wasn't pressable. Since tapping a tag now opens tag detail (a browse action) rather than playing anything, and that's independent of whether recent playback exists, made the whole chip pressable via `InteractionChip`'s own `onPress` and removed the nested play icon entirely, rather than repurposing it. This also retired `getRecentsShortcutPlayActionCopy` (its accessibility copy/disabled-state was written specifically for a "play" action) and its 2 tests.
  - Implementation: `RecentsScreenProps.onPlayRecentShortcut` renamed to `onSelectRecentShortcutTag` (no longer play-shaped) and threaded straight through from `AppRouter`'s `onSelectTag={(tag) => setSelectedTag(tag)}` (task `3.1`'s state) via `AppRouterRecentsScreen`'s new `onSelectTag` prop, replacing the previous no-op "resume most-recent item" handler.
  - Tests: no new automated test — the chip wiring is a single-line pass-through (`onPress={() => onSelectRecentShortcutTag(tag)}`) with no branching; removed the now-dead `getRecentsShortcutPlayActionCopy` tests instead of adding new ones.
  - Manually verified in the integrated browser: from the Recents tab (a different shell tab than Library), tapping the "Soprano" chip opens the tag detail overlay with the correct tag name; back returns to Recents with the chip row unchanged. No new console errors.
  - Validation: `nx run mobile-rehearsal-player:typecheck`, `eslint` on touched files, and `nx run mobile-rehearsal-player:test` (401/401 passing, 2 fewer than before from the retired copy tests) all pass.
- [x] 2.5 Update the existing Recents `screen-copy`/`overflow-actions` focused tests for the new tag-driven module, and add coverage for the empty-state and overflow-entry-point branches.
  - `overflow-actions.ts`/`overflow-actions.spec.ts` (the per-recent-row "Play next/Add to queue/View in library" menu) is unrelated to the tag module and untouched by this change — confirmed its 3 existing tests still pass unmodified, no update needed.
  - `screen-copy.spec.ts`'s stale `getRecentsShortcutPlayActionCopy` tests were already removed in task `2.4` when that copy helper was retired.
  - The empty-state and overflow-entry-point branches were previously inline JSX booleans in `index.tsx` (`!hasSavedTagUsage || !isRecentPlaybackAvailable` and `libraryTagUsage.length > RECENTS_SHORTCUT_TAG_CAP`) with no testable seam. Extracted both into a new pure `getRecentsTagModuleVisibility` in `screen-copy.ts` (returning `{ showGuidanceBody, showOverflowTrigger }`), alongside the exported `RECENTS_SHORTCUT_TAG_CAP` constant (moved out of `index.tsx`), matching this codebase's existing `shouldRenderX`-predicate convention (e.g. `shouldRenderSavedTagsList` in `browse-content-model.ts`). `index.tsx` now derives `tagModuleVisibility` from this function instead of inline conditions.
  - Tests: 5 new cases in `screen-copy.spec.ts` — empty-state guidance shown with no tags, guidance still shown with tags but no recent playback yet, guidance hidden once both tags and recent playback exist, overflow trigger hidden at exactly the cap (6), overflow trigger shown one past the cap (7).
  - Manually verified in the integrated browser: with tag usage exceeding the cap, the overflow chevron renders and the guidance body correctly hides once both gating conditions are satisfied — same behavior as before the refactor, confirming it's a pure extraction with no behavior change.
  - Validation: `nx run mobile-rehearsal-player:typecheck`, `eslint` on touched files, and `nx run mobile-rehearsal-player:test` (406/406 passing) all pass.
- [x] 2.6 Add "Date" as a third sort field (alongside Name/Count) to the Library Tags view's sort control (`SavedTagsListSortField` in `saved-tags-list/model.ts`), once `1.8`'s aggregation exposes each tag's creation date. Extend `sortSavedTagUsage` and `SAVED_TAGS_LIST_SORT_FIELD_OPTIONS`; add matching test cases (date asc/desc, tie-break) alongside the existing Name/Count coverage from `2.3.7`.
  - Implementation: added `'date'` to `SavedTagsListSortField` and a `{ label: 'Date added', value: 'date' }` entry to `SAVED_TAGS_LIST_SORT_FIELD_OPTIONS`; `sortSavedTagUsage` gained a `date` branch comparing `createdAt` (ISO strings, so lexicographic comparison is chronologically correct) with the same alphabetical-ascending tie-break convention already used by `count`.
  - Decision (confirmed with user): requested after `2.3.7` was marked done, once tag creation dates became available via `1.6`/`1.8` — the Tags view's sort control should offer the same date-sort capability as the tag detail screen's match-list sort (task `3.2`), not just Name/Count.
  - **Revision after user feedback on the first pass** (three points): (1) the label "Date" was unclear on its own — renamed to "Date added", matching the exact label Files' own sort control already uses for the same concept (`FILES_SORT_OPTIONS`). (2) the sorted value itself wasn't visible per row — added `getSavedTagUsageAddedDateLabel`/`getSavedTagUsageRowMetadataLabel` to `model.ts` (small-date formatter mirroring the existing `getCalendarLastPlayedLabel` convention in `screens/recents/history.ts`: day+month, plus year only when it differs from the current year); every tag row's metadata now reads e.g. "4 items · Added Jan 5" instead of just the count. (3) sort controls should be revealed by the same filter-icon affordance Files already uses, not shown inline at all times — this required lifting `SavedTagsList`'s sort state (previously local `useState`) up into `useSavedRehearsalLibrarySearch` (new `tagsSortState`/`setTagsSortField`/`toggleTagsSortDirection`, mirroring the existing `filesSortMode`/`setFilesSortMode` pattern) and adding a `selectedView === 'tags'` branch to `LibrarySearchControls`'s existing filter popover (a `FilterChipGroup`-style Sort row plus the same asc/desc `SurfaceIconButton` toggle `SavedTagsList` used to own). `SavedRehearsalLibraryHeader`'s `isShowingFilesView` prop (which gated whether the filter icon shows at all) was renamed to `canShowFilterPopover` and broadened to `selectedView === 'files' || selectedView === 'tags'`. `SavedTagsList` itself lost its inline sort row entirely and now takes `sortState` as a prop.
  - File-length fallout from lifting state into the hook: `use-saved-rehearsal-library-search.ts` crossed 300 lines (292 → 313). Extracted the pre-existing, already self-contained `resolveAvailableTagFilters`/`normalizeTagToken` helpers (unrelated to this task, but the cleanest available cut) into a new sibling `saved-rehearsal-library-tag-filters.ts` with a new colocated spec (this function had no prior test coverage at all) — brought the hook back to 264 lines.
  - Tests: `model.spec.ts` gained `getSavedTagUsageAddedDateLabel` (same-year vs. cross-year formatting) and `getSavedTagUsageRowMetadataLabel` (count+date combination) cases, alongside the original date asc/desc/tie-break cases from the first pass. New `saved-rehearsal-library-tag-filters.spec.ts` covers the extracted dedupe helper (cross-entity case-insensitive dedupe, blank-tag filtering, empty-input).
  - **Second revision after user feedback**: the direction toggle should sit inline with the "Sort" title (not wrap below the field chips as its own row), and this layout should be the shared convention for any sort control, including Files' — not a Tags-only one-off. Extended the shared `FilterChipGroup` (`library-search-filter-groups.tsx`) with an optional `trailingAction?: ReactNode` and `filterLabelRowStyle` prop: when both are supplied, the label and trailing action render on one row (`justifyContent: 'space-between'`) above the chip row; otherwise the label renders alone exactly as before. Tags' Sort group now goes through `FilterChipGroup` itself (passing the direction-toggle `SurfaceIconButton` as `trailingAction`) instead of hand-rolled markup, so it's built from the same primitive Files' Scope/Sort/Show groups use. Files' three groups pass no `trailingAction` and are visually unchanged — confirmed no regression by inspecting the popover after this change.
  - Manually verified in the integrated browser: Tags view now shows no inline sort row and each row displays "N items · Added <date>"; tapping the header filter icon (same icon Files uses) reveals a "SORT" group with the direction toggle inline on the title row and Name/Count/Date added chips wrapping cleanly below; selecting "Date added" and toggling direction both work and re-sort the list; switching to Files confirms its own Scope/Sort/Show filter popover renders identically to before (label-only rows, no trailing icon). Only pre-existing Drive/auth 400 console noise, no new errors.
  - Validation: `nx run mobile-rehearsal-player:test-pattern -- --pattern='src/app/library/tags/**/*.spec.ts'` (18/18 passing), `nx run mobile-rehearsal-player:test` (422/422 passing, up from 419), `nx run mobile-rehearsal-player:typecheck` clean, `eslint` on all touched files clean. `typecheck-tests` shows the same pre-existing baseline errors documented elsewhere in this file, none in touched files.
  - **Third revision after user feedback ("What about the files view?")**: confirmed with user via `AskUserQuestion` that Files should gain a *real* ascending/descending toggle (one shared direction across all four Files sort fields, matching Tags' shared-toggle model) rather than just inheriting the layout. This is a genuine behavior change, not cosmetic: Files previously had no direction concept — Name sorted A→Z, Type used a fixed track→loop→playlist grouping, and both Date modes were hardcoded newest-first (asserted by an existing test). Confirmed with user that the new shared default direction is `'asc'`, matching Name's prior default; this means Date added/Date opened now show oldest-first by default unless the user has touched the toggle, a deliberate, user-accepted tradeoff for one coherent mental model across all four fields.
  - Implementation: added `LibraryFilesSortDirection = 'asc' | 'desc'` and `DEFAULT_LIBRARY_FILES_SORT_DIRECTION = 'asc'` to `library-files-model` (`types.ts`/`sort.ts`). Rewrote `sort.ts`'s comparators to canonical-ascending orientation (name A→Z, type track→loop→playlist, timestamps oldest→newest) each multiplied by a `directionMultiplier`; the alphabetical tie-break stays ascending regardless of direction, matching the `SavedTagsList`/`sortSavedTagUsage` convention. Threaded `sortDirection` end-to-end: `LibraryFilesSearchOptions` → `build-default-rows.ts`/`build-search-rows.ts` → `buildLibraryFilesExplorerState`; new `filesSortDirection` state + `setFilesSortDirection`/`toggleFilesSortDirection` added to `useSavedRehearsalLibrarySearch` (mirroring `filesSortMode`); also threaded through the in-session Files-view restoration path (`library-files-search-state.ts`, `library-files-session-state.ts`, `use-library-files-session-restoration.ts`) so switching away from and back to Files preserves the chosen direction, matching how `sortMode` is already preserved there. `library-search-controls.tsx`'s Files "Sort" `FilterChipGroup` now passes the same `trailingAction` direction-toggle `SurfaceIconButton` Tags uses (reusing `getSavedTagsListSortDirectionToggleLabel`, kept in the tags model to avoid a search↔tags import cycle — noted as a minor naming compromise, not moved).
  - Tests: `library-files-model-sort.spec.ts` renamed the existing "newest-first" test to explicit `sortDirection: 'desc'` and added two new cases: default-direction (`asc`) now sorts date modes oldest-first, and `name` mode reverses under `desc`. Extracted `buildDateModeFixtures()`/`buildNameModeTree()` local helpers to deduplicate fixture setup across the now-5 tests in this file, keeping it at 297 lines (was pushed to 344 by the naive additions, over the repo's 300-line guideline) rather than pulling in `code-move-checkpointed` for what was a same-file, low-risk extraction.
  - Manually verified in the integrated browser: opened Files' filter popover, confirmed the Sort row now shows the same inline direction toggle as Tags; toggled direction with Name mode active and confirmed the file list (and the "test" folder, sorted as part of the same group) reversed correctly (`This Masquerade.m4a → No Diggity → New York Voices → Jazzmeia → Copy of Radio Video`, folder first); switched to Tags and confirmed its own Count/asc sort state was completely unaffected by the Files toggle (separate state, as designed). No new console errors (only pre-existing Drive/auth 400/401 noise).
  - Validation: `nx run mobile-rehearsal-player:test-file -- --file=src/app/library/saved-rehearsal-library/library-files-model-sort.spec.ts` (5/5 passing), `nx run mobile-rehearsal-player:test --skip-nx-cache` (424/424 passing, up from 422), `nx run mobile-rehearsal-player:typecheck` clean, `eslint` clean on all touched files, `typecheck-tests` unchanged from the pre-existing baseline (17 unique errors, none in touched files).
  - **Fourth revision after user feedback (screenshot)**: the direction-toggle button rendered visibly larger than the sort chips (`SurfaceIconButton`'s default `secondaryButton` size is a fixed 46×46, versus the chips' ~32–36px height). Added a `sortDirectionToggle: { width: 32, height: 32 }` style (matching `filterChip`'s `minHeight: 32`) passed via `SurfaceIconButton`'s existing `style` prop override, and reduced its icon `size` from 20 to 16 to stay visually proportionate inside the smaller box — applied to both the Tags and Files toggle instances (the only two call sites). Left `SurfaceIconButton`'s own default size untouched since it's used elsewhere in the app at the original 46×46 scale.
  - Manually verified in the integrated browser: both Files' and Tags' direction toggle now render at the same height as their neighboring chips, no longer oversized. No new console errors.
  - Validation: `nx run mobile-rehearsal-player:typecheck` clean, `eslint` clean on `library-search-controls.tsx`, `nx run mobile-rehearsal-player:test --skip-nx-cache` (424/424 passing, unchanged — pure styling change, no logic touched).

## 3. Tag Detail Screen

- [x] 3.1 Add a new tag detail router destination/screen shell, using the shared compact destination-header primitive for the tag name and back action.
  - Decision (confirmed with user): the tag detail screen must be reachable from both Recents (task 2.4) and the new Library Tags view (task 2.3.5), which live on different top-level shell tabs. The existing `requestedDestination`/`activeDestination` mechanism (`app-router.tsx`/`mobile-shell/index.tsx`) only switches between the 3 fixed tab panels with no per-screen parameter, and playlist/track-loop "detail" screens today use `LibraryScreen`-local state (`SavedRehearsalLibraryDetailMode`), which isn't reachable from Recents. Instead of extending `ShellDestinationKey` with a payload, followed the existing cross-tab-overlay precedent already in this file: `MobileShellPlaybackSurface` (Now Playing/Queue) is opened/closed independent of the active tab. New `selectedTag: string | null` state lives in `AppRouter` (which already owns `libraryController`/`playback`/`libraryTagUsage`, all of which the screen's content will need in tasks 3.2+); `AppRouter` builds a `tagDetailScreen` ReactNode and passes it into `MobileShell` as a new prop. `MobileShell` renders it as a 4th panel inside the existing `contentViewport` (reusing the `destinationPanel`/`destinationPanelActive` show/hide styles), hiding the 3 tab panels while it's shown, so the mini player dock, tab bar, and Now Playing/Queue overlay are all unaffected and stay reachable.
  - New component: `TagDetailScreen` (`packages/mobile-rehearsal-player/src/app/library/tags/components/tag-detail-screen/index.tsx`), taking `{ tag, onClose }`, rendering `ExplorerNavigationBar` (the shared compact header/back-action primitive already used by Files browsing and the Drive discovery panel) with `eyebrow="Tag"`, `title={tag}`, `onGoBack={onClose}`, plus an empty flex-1 body placeholder for task 3.2's match list.
  - Out of scope for this slice (left for tasks `2.3.5`/`2.4`): no caller sets `selectedTag` yet — `onSelectTag`-style wiring from the Tags-list row tap and the Recents chip tap is exactly what those two subtasks add. The overlay mechanism itself (state → prop → conditional render) is therefore not yet manually triggerable; verified instead by confirming the Library/Tags screens still render correctly with `tagDetailScreen` always `null` (no regression) via the browser preview, and that the new prop threading typechecks/lints clean.
  - Tests: no new automated test — `TagDetailScreen` is pure JSX composition around an already-tested shared primitive with zero branching logic, and `app-router.tsx`/`mobile-shell/index.tsx` have no existing spec files themselves (matching this repo's convention of testing colocated pure logic/view-model files, not top-level wiring components — same precedent noted in task `2.3.2`).
  - Validation: `nx run mobile-rehearsal-player:typecheck`, `eslint` on touched files, and `nx run mobile-rehearsal-player:test` (403/403 passing) all pass clean. Manually verified in the integrated browser that Library (including the Tags view added in `2.3.1`-`2.3.4`) still renders correctly with no new console errors (only pre-existing Drive/auth 400/401/COOP noise).
- [x] 3.2 Resolve and render the tag's direct matches (tracks, loops, playlists, folders), and add a sort control (Name / Type / Date added, each ascending/descending) over the flat, ungrouped match list.
  - Decision (confirmed with user): rows are composed directly from the shared `ExplorerListRow`/`ExplorerListSurface` primitive — the same primitive the native Tracks/Loops/Playlists/Files rows all render through underneath — with per-type icon/title/metadata matching each entity's native row appearance, rather than reusing the heavy native row components (`BrowseSourceGroup`, `SavedLoopListRow`, `SavedPlaylistCardsList`) as-is, which carry large native-screen-only prop surfaces (remove, tag editor, playlist selector, rename) that don't apply to this filtered, read-mostly lens. Mirrors the precedent set by `SavedTagsList` (task `2.3.2`).
  - Decision (confirmed with user): the match list is flat and ungrouped by default (not grouped by entity type), sortable by Name, Type, or Date added (using the new `createdAt` fields from `1.3`/`1.4`), each ascending/descending — reusing the Tags-list sort control's UX shape (task `2.3.3`: field chips + a direction-toggle icon button) rather than inventing a new interaction. Type filtering (task `3.3`) and search (task `3.4`) apply on top of this sorted list and should reuse the same top-level UI patterns where relevant (per user guidance).
  - New resolver: `resolveRehearsalLibraryTagMatches` (`packages/audio-library-runtime/src/lib/rehearsal-library-tags.ts`, alongside `aggregateRehearsalLibraryTags`), taking the tag string plus the same `{ entityCollections, folders }` shape, returning an ordered `RehearsalLibraryTagMatch[]` (`{ kind: 'track' | 'loop' | 'playlist' | 'folder'; item }`) of every entity carrying that tag (post-normalization via `normalizeLibraryEntityTags`, case-insensitive match on the trimmed queried tag), ordered tracks → loops → playlists → folders. Exported from the package root via the existing `export *`.
  - Decision (not pre-decided by artifacts, resolved from nearby code convention rather than asked): default sort state is `{ field: 'name', direction: 'asc' }`, matching Files' own `DEFAULT_LIBRARY_FILES_SORT_MODE`/`DEFAULT_LIBRARY_FILES_SORT_DIRECTION` convention (task `2.6`'s third revision) — there's no "most-used-first" concept for a flat per-tag entity list the way there is for the Tags list itself (`count`/`desc`), so Files' neutral default was the closer precedent.
  - New app component: `TagMatchList` (`packages/mobile-rehearsal-player/src/app/library/tags/components/tag-match-list/{index.tsx,model.ts,model.spec.ts}`), a sibling to `saved-tags-list`. `model.ts` provides `getTagMatchTitle`/`getTagMatchTypeLabel`/`getTagMatchIconName`/`getTagMatchCreatedAt`/`getTagMatchMetadataLabel` (per-kind: track/loop duration via the shared `formatDurationLabel`, playlist item count, static `'Folder'` for folders) and `sortTagMatches` (Type sort uses a fixed track→loop→playlist→folder order, matching Files' `NON_FOLDER_TYPE_ORDER` convention extended to include folders in the same flat ordering, since — unlike Files — the tag detail list is never grouped, so folders take a slot in the type order instead of a separate always-first group). The sort row reuses the same shared `FilterChipGroup` primitive (with `trailingAction`) Files/Tags already standardized on in task `2.6`'s second revision, rather than hand-rolling a new sort row shape.
  - `TagDetailScreen` (task `3.1`) now takes `entityCollections`/`folders` props, computes `matches` via `useMemo(() => resolveRehearsalLibraryTagMatches(tag, ...))`, and owns local sort state (`useState`, not yet lifted into a shared search hook — this screen has no header search icon of its own until task `3.4`). `app-router.tsx` threads `entityCollections`/`folders` from `libraryController` straight through (same raw-collections-as-props pattern `browse-content.tsx` already uses for `SavedTagsList`, per task `2.3.2`'s precedent, rather than threading an app-router-computed memo).
  - Tests: `rehearsal-library-tags.spec.ts` gained a `resolveRehearsalLibraryTagMatches` suite (ordered multi-kind match, case/whitespace-insensitive tag matching, empty-result case). `tag-match-list/model.spec.ts` covers per-kind title/type/icon/key/metadata derivation and `sortTagMatches` for all three fields in both directions plus a no-mutation guarantee.
  - Manually verified in the integrated browser: opened the "a" tag (from the Library Tags view) — the tag detail screen renders "TAG a" with the "test" folder as its only match, a working Sort row (Name/Type/Date added chips + ascending/descending toggle, confirmed switching fields and toggling direction re-renders without error), and back navigation returns cleanly to the Tags list. No new console errors (only the pre-existing Drive/auth 400/COOP noise documented elsewhere in this file).
  - **Revision after user feedback**: the initial pass rendered `TagDetailScreen` with only the compact `ExplorerNavigationBar` ("TAG · <name>") and no shared app header, since `MobileShell` renders `tagDetailScreen` as an independent overlay panel outside the three tab destinations (task `3.1`'s decision) — unlike Files' in-folder drilldown, which keeps Library's outer `DestinationHeader` fixed while only the inner content swaps. Confirmed with user via `AskUserQuestion` (options: reuse Library's exact header vs. show the tag name as the header title vs. a generic "Tags" title) that the tag detail screen should keep the same "Library" header (title + Drive session avatar) Library itself shows, stacked above the existing "TAG · <name>" back-navigation bar — mirroring the Files-drilldown precedent exactly rather than inventing a new header identity for this screen.
  - Implementation: `TagDetailScreen` gained an `authorization: DriveSessionMenuController` prop (threaded from `app-router.tsx`, the same `authorization` object already passed to `AppRouterRecentsScreen`/`LibraryScreen`/`AddScreen`) and local `isSessionMenuVisible` state; renders `DestinationHeader` (title from `getDestinationHeaderModel('library')`, trailing `DriveSessionMenu`) plus a tap-outside-to-dismiss backdrop, mirroring Recents' own header composition exactly (`screens/recents/index.tsx`) rather than the heavier `SavedRehearsalLibraryHeader` (which also wires Library's filter/search controls — not applicable here, since tag detail's own search/filter arrive in tasks `3.3`/`3.4` scoped to the tag's matches, not Library's search state).
  - Tests: no new automated test — this is pure JSX composition wiring together already-tested shared primitives (`DestinationHeader`, `DriveSessionMenu`), matching the same no-render-test rationale task `3.1` already documented for this component.
  - Manually re-verified in the integrated browser: opening tag "a" now shows the green "Library" header (title + session avatar) above "TAG a"; tapping the avatar opens the Drive session menu (Connected / Refresh authorization / Forget session) with the backdrop dismissing it on outside tap; back navigation still returns cleanly to the Tags list.
  - **Second revision after user feedback ("header is not positioned consistently... moves when entering / returning a tag view")**: the header still shifted by 12px between the Tags list and the tag detail screen, because `LibraryScreen` (and `screens/recents/index.tsx`) apply `marginTop: 12` to their own `DestinationHeader` instance (`styles.destinationHeader`), but `TagDetailScreen`'s newly-added header had no equivalent top margin. Fixed by adding the identical `destinationHeader: { marginTop: 12 }` style and passing it via `DestinationHeader`'s `style` prop, matching Library's/Recents' value exactly (both `MobileShell`'s tab panels and the `tagDetailScreen` overlay panel render inside the same `contentViewport`, with no panel-level top padding of its own, so matching this one value is sufficient to align both surfaces).
  - Manually re-verified in the integrated browser: the header's top edge now sits at the identical vertical position on the Tags list and on the tag detail screen, confirmed by round-tripping into and back out of tag "a" with no visible jump. No new console errors (only the pre-existing Drive/auth 400/COOP noise documented elsewhere in this file).
  - Validation: `nx run audio-library-runtime:test` (42/42 passing, up from 39), `nx run mobile-rehearsal-player:test-pattern -- --pattern='src/app/library/tags/**/*.spec.ts'` (24/24 passing), `nx run mobile-rehearsal-player:test` (430/430 passing, up from 424), `nx run mobile-rehearsal-player:typecheck` clean, `eslint` on all touched files clean, `typecheck-tests` shows the same pre-existing baseline errors documented elsewhere in this file, none in touched files.
- [x] 3.3 Add a type filter chip row (Tracks / Loops / Playlists / Folders) scoped to this tag's matches, using the existing chip family.
  - **Pulled forward together with `3.4`, prompted by user feedback**: after `3.2` shipped, the user asked for the header's "search" and "controls" icons (matching Library's own header) to appear on the tag detail screen, with "controls" toggling visibility of the sort **and** filter controls together. Confirmed scope via `AskUserQuestion` — user chose to implement both `3.3` (type filter) and `3.4` (search) now rather than defer them, since the header work already required deciding where these controls would live.
  - Decision (confirmed with user): type filtering is multi-select (a subset of Tracks/Loops/Playlists/Folders can be active at once, matching the spec's "selects a subset of entity types" wording), using the same toggle-chip pattern Library's own Tags filter row already uses (`InteractionChip`, `variant: 'selected' | 'passive'`, `onPress` toggles membership) rather than `FilterChipGroup`'s single-select radio behavior — `FilterChipGroup` stays reserved for the Sort row (still single-select).
  - Implementation: `tag-match-list/model.ts` gained `TagMatchTypeFilterValue` (= `RehearsalLibraryTagMatch['kind']`), `TAG_MATCH_TYPE_FILTER_OPTIONS` (Tracks/Loops/Playlists/Folders), and `filterTagMatchesByType(matches, selectedTypes)` (empty selection = no filter, matching "clearing the type filter restores the full match list"). New `tag-match-list/controls-panel.tsx` exports `TagMatchControlsPanel`, rendering the existing Sort `FilterChipGroup` (moved out of `TagMatchList` itself, see `3.2`'s revision below) plus a new "Type" `InteractionChip` row, mirroring the exact `filterGroup`/`filterLabel`/`filterRow` markup Library's own Tags chip filter uses in `library-search-controls.tsx`.
  - `TagMatchList` (`index.tsx`) was simplified to a pure renderer: it no longer owns sort-UI or a `sortState` setter — it now takes an already-filtered `matches` list plus `sortState` (applies `sortTagMatches` internally) and a `hasUnfilteredMatches` flag, so it can distinguish "nothing tagged with this yet" (task `3.2`'s empty state) from "no matches for the active filters" (a new `NO_TAG_MATCH_RESULTS_MESSAGE` empty state).
  - Tests: `model.spec.ts` gained a `filterTagMatchesByType` suite (no-selection passthrough, subset match, empty-result case).
  - See `3.4`'s entry below for the header wiring (the `TagMatchControlsPanel` is shown/hidden via the same header "controls" icon that also owns search).
- [x] 3.4 Add a search input scoped to this tag's matches only, using the shared contextual search panel scaffold.
  - Implemented together with `3.3` — see that entry's context.
  - Implementation: `tag-match-list/model.ts` gained `filterTagMatchesByQuery(matches, query)`, reusing the existing `normalizeSearchQuery` helper (trim + lowercase) and matching on `getTagMatchTitle(match)` substring, mirroring `filterSavedTagUsageByQuery`'s exact shape from the Library Tags list (task `2.3.4`).
  - Header wiring: `LibrarySearchControlsActions` (previously Library-Files-specific, taking `entityFilter`/`selectedTagFilters` to compute its own "has active filters" indicator) was generalized to accept a plain `hasActiveFilters: boolean` prop instead, plus optional accessibility-label overrides for its two icon buttons (`searchAccessibilityLabel`/`closeSearchAccessibilityLabel`/`showFiltersAccessibilityLabel`/`hideFiltersAccessibilityLabel`, defaulting to the existing Library wording so `search-shell.tsx`'s only other call site needed just the boolean computation inlined, no behavior change). `TagDetailScreen` reuses this same component in its `DestinationHeader`'s trailing action (alongside `DriveSessionMenu`, matching Library's exact `headerActionRow` layout), with tag-specific labels ("Search this tag's matches", "Show/Hide sort and filter controls") and `hasActiveFilters = selectedTypeFilters.length > 0`.
  - Search panel: a `ContextualSearchPanel` instance (own state, since `TagDetailScreen` is a standalone overlay destination with no existing header search context to borrow, unlike the Library Tags view's `2.3.4` revision) renders below the "TAG · <name>" navigation bar when toggled open, with tag-scoped helper/placeholder copy (`Search tracks, loops, playlists, and folders tagged "<tag>".` / `Search "<tag>" matches`) and `canShowRecentSearchTerms={false}`/`recentSearchTerms=[]` (no persisted recent-search history for this screen — a deliberate simplification, not a data-model requirement anywhere in the artifacts).
  - Filtering pipeline in `TagDetailScreen`: `visibleMatches = filterTagMatchesByQuery(filterTagMatchesByType(matches, selectedTypeFilters), searchQuery)`, recomputed via `useMemo`, fed into `TagMatchList` (which then sorts for display). The "controls" icon and the "search" icon toggle independently (matching Library's own non-mutually-exclusive `isFilterPopoverVisible`/`isSearchBarVisible` behavior) — both panels can be open at once.
  - Tests: `model.spec.ts` gained a `filterTagMatchesByQuery` suite (empty/whitespace passthrough, case-insensitive substring match, trimming, no-match case).
  - Manually verified in the integrated browser: opened tag "a" — header now shows the controls (tune) icon, search (magnify) icon, and session avatar; tapping search opens the tag-scoped input with correct helper copy, typing a non-matching query live-filters to "No matches for the selected filters."; tapping controls reveals both the Sort row and the new Type row together (matching the user's "controls should hide/show the sort and filter controls" request); selecting "Loops" while only a Folder match exists narrows to zero results, and clearing it restores the folder; confirmed Library's own Files filter popover (Scope/Sort/Show) is visually and functionally unchanged after generalizing `LibrarySearchControlsActions`. No new console errors (only the pre-existing Drive/auth 400/401/COOP noise documented elsewhere in this file).
  - Validation: `nx run mobile-rehearsal-player:test-pattern -- --pattern='src/app/library/tags/**/*.spec.ts'` (31/31 passing), `nx run mobile-rehearsal-player:test` (437/437 passing, up from 430), `nx run mobile-rehearsal-player:typecheck` clean, `eslint` on all touched files clean (one unused-import warning caught and fixed during the sweep), `typecheck-tests` shows the same pre-existing baseline, none in touched files. All touched files remain under the 300-line guideline.
- [x] 3.5 Wire folder and playlist match rows to open Files-at-that-folder and playlist detail respectively when tapped outside the play action, per the spec's non-play navigation requirement.
  - Decision (not pre-decided by artifacts, resolved from strong nearby precedent rather than asked): folder navigation reuses `libraryController.savedLibrary.files.openFolder(folderId)` directly (already centralized, non-screen-local state, callable from `app-router.tsx`) plus the existing `requestDestination('library')`/`requestLibraryView('files')` pair. Playlist navigation needed new plumbing: `playlistState.openPlaylistDetail` (which drives `SavedRehearsalLibraryDetailMode`'s `'playlist-detail'` override, confirmed via `detail-mode.ts` to be independent of `selectedView`) is local state owned inside `SavedRehearsalLibrarySection`, unreachable from `AppRouter`. Added a `requestedPlaylistId`/`requestedPlaylistIdRequestId` pair at the `AppRouter` level, mirroring the exact `requestedDestination`/`requestedLibraryView` cross-tab-navigation-with-parameter pattern already established twice in this codebase (tasks `3.1`, `2.3.6`), threaded `AppRouter` → `LibraryScreen` (pass-through props) → `SavedRehearsalLibrarySection` (new props) → `useSavedRehearsalLibrarySectionEffects` (new guarded effect calling `playlistState.openPlaylistDetail`, using a `useRef`-tracked "last handled request id" rather than relying on `openPlaylistDetail`'s referential stability, since — unlike a `useState` setter — it's a plain function recreated every render).
  - New resolver in `tag-match-list/model.ts`: `getTagMatchNavigationTarget(match)` returns `{ kind: 'folder'; folderId } | { kind: 'playlist'; playlistId } | null` (null for track/loop matches, which stay non-interactive — no tap behavior is specified for them by this task or the spec). `TagMatchList` gained `onOpenFolder`/`onOpenPlaylist` props and wires each row's `ExplorerListRow.onPress` from this resolved target. `TagDetailScreen` gained matching `onOpenFolder`/`onOpenPlaylist` props, implemented in `app-router.tsx`'s `TagDetailScreen` instantiation: both close the tag detail overlay (`setSelectedTag(null)`) and switch to the Library destination before performing the folder-open or playlist-detail-request call.
  - Tests: `model.spec.ts` gained a `getTagMatchNavigationTarget` suite (folder target, playlist target, null for track/loop). No test added for the new cross-component effect/wiring itself (`use-saved-rehearsal-library-section-effects.ts` has no existing spec file and follows this codebase's established convention of not unit-testing pure prop-threading/effect-wiring — see task `3.1`'s identical rationale); covered instead by manual verification.
  - File-length note: `saved-rehearsal-library-section/index.tsx` grew from 319 to 321 lines (crossing the lint `max-lines` warning threshold), on top of the overage already flagged repeatedly in this file's own task history (`1.9`, `2.3.5`). Flagged again, not fixed inline, consistent with that established precedent — a same-file extraction here would be a distraction from this task's actual scope.
  - Manually verified in the integrated browser: opened tag "a", tapped the "test" folder row — the tag detail overlay closed and Library opened directly inside that folder (Files view, "test" as current folder), confirmed via a fresh page load with no console errors. Created a temporary playlist ("Tag Detail Test"), tagged it "a", reopened tag "a" (now showing 2 items: the playlist and the folder), tapped the playlist row — the tag detail overlay closed and Library opened directly on that playlist's detail view (playback controls, Items (0), Add items), matching "the same as opening it from the Playlists view directly." Removed the temporary playlist afterward to restore the demo environment to its prior two-tag state. One transient `ReferenceError: requestedPlaylistId is not defined` was observed mid-edit (before all files were saved) — confirmed to be a stale Fast Refresh artifact, not a real bug, by reproducing the exact same flow cleanly on a fresh page load with zero errors. No other new console errors (only the pre-existing Drive/auth 400/401/COOP noise documented elsewhere in this file).
  - Validation: `nx run mobile-rehearsal-player:test-pattern -- --pattern='src/app/library/tags/**/*.spec.ts'` (34/34 passing), `nx run mobile-rehearsal-player:test` (440/440 passing, up from 437), `nx run mobile-rehearsal-player:typecheck` clean, `eslint` clean except the one pre-existing/growing `max-lines` warning noted above, `typecheck-tests` shows the same pre-existing baseline, none in touched files.
- [x] 3.6 Add focused view-model tests for match resolution, type filtering, and search filtering on the tag detail screen.
  - Satisfied incrementally by `3.2`'s `resolveRehearsalLibraryTagMatches` suite (match resolution: ordering, case/whitespace-insensitive matching, empty-result case) and `3.3`/`3.4`'s `filterTagMatchesByType`/`filterTagMatchesByQuery` suites (type filtering and search filtering) — no additional test code needed beyond what those subtasks already added; confirmed by re-reading this task's exact wording against the existing coverage.

## 4. Tag Queue Expansion and Playback

- [x] 4.1 Implement folder-tag expansion: recursively resolve every track/loop reachable from a tagged folder, including nested subfolders, reusing the Files explorer's existing folder-listing resolution instead of a new folder-walk.
  - Decision (not pre-decided by artifacts, resolved from proposal.md's Impact section rather than asked): proposal.md already places "a folder-content-resolution helper for queueing" in `packages/audio-library-runtime`, alongside the `1.1`/`3.2` tag-aggregation/match resolvers. Since the app-side Files explorer's existing recursive folder-subtree walk (`buildScopedFolderIds`'s `current-folder`-scope branch, `library-files-model/pathing.ts`) lives one layer up (the app can depend on the runtime package, not the reverse), literal code reuse (not just algorithmic similarity) required extracting that walk down into `audio-library-runtime` and having the app call the shared version — mirroring the exact precedent task `1.1` already established for `normalizeLibraryEntityTags`.
  - Implementation: added `resolveRehearsalLibraryFolderSubtreeIds(folderId, folders)` (the extracted walk, verbatim logic from `buildScopedFolderIds`, generalized to take a starting folder id instead of a `currentFolder`/`searchScope` option) and `resolveFolderPlayableItems(folder, folders, fileLinks, loops, sources)` to `packages/audio-library-runtime/src/lib/playback-queue.ts` — sibling to the existing `resolvePlaylistItems`/`createPlaybackQueue`, which already do the equivalent "entity references → playable items" resolution for playlists. `resolveFolderPlayableItems` computes the folder's subtree ids, then filters `fileLinks` to `track`/`loop` entries scoped to that subtree (playlist file links nested in a folder are left un-expanded, matching design.md's "every track/loop reachable from it" wording — a playlist filed inside a tagged folder is not itself recursed into by folder expansion; that's task `4.2`'s separate concern), resolving each to a `PlayableItem` via the existing `createTrackPlayableItem`/`createLoopPlayableItem` factories and the same `isSourcePlayable` availability gate `resolvePlaylistItems` already applies (a loop also requires its parent source to be available, matching that existing convention).
  - `library-files-model/pathing.ts`'s `buildScopedFolderIds` now delegates its non-`all-files`-scope branch to `resolveRehearsalLibraryFolderSubtreeIds` (imported from `@org/audio-library-runtime`, an import path already established elsewhere in this directory via `library-files-operation-helpers.ts`) instead of its own duplicated while-loop — the actual "reuse" the task text calls for, not just a parallel reimplementation.
  - Decision (not pre-decided by artifacts, resolved from nearby code convention rather than asked): the resolved items are ordered by title, case-insensitive ascending (`localeCompare` with `sensitivity: 'base'`), matching Files' own `DEFAULT_LIBRARY_FILES_SORT_MODE`/`DEFAULT_LIBRARY_FILES_SORT_DIRECTION` (`name`/`asc`, task `2.6`'s third revision) — design.md's "same order the Files explorer would list them" doesn't have a literal single-folder-listing analog once multiple folders are flattened into one queue, so the closest honest reading is Files' own default sort applied to the flattened result.
  - File-placement note: considered `rehearsal-library-files.ts` (the folder-tree domain file) as the more semantically obvious home, but that file is already over the 300-line guideline (322 lines, flagged repeatedly since `1.4`); placed the new code in `playback-queue.ts` (226 lines after this change) instead, which is both under budget and an equally good fit (folder-content → playable-item resolution, directly parallel to `resolvePlaylistItems`).
  - Tests: new colocated `playback-queue.spec.ts` (this file had no dedicated spec before — its existing coverage lived in the already-oversized `rehearsal-playback.spec.ts`, which this task avoided adding to) covers `resolveRehearsalLibraryFolderSubtreeIds` (folder + nested descendants, sibling exclusion, childless-folder case) and `resolveFolderPlayableItems` (recursive track/loop resolution ordered by title, sibling-folder exclusion, unavailable-source/missing-loop-source exclusion, playlist-file-link non-expansion, empty-subtree case).
  - Validation: `nx run audio-library-runtime:test` (50/50 passing, up from 42), `nx run mobile-rehearsal-player:test` (440/440 passing, unchanged — pure internal refactor of `buildScopedFolderIds`, no behavior change), `nx run-many -t typecheck lint -p audio-library-runtime mobile-rehearsal-player` clean (only pre-existing max-lines warnings, none in touched files), `mobile-rehearsal-player:typecheck-tests` shows the same pre-existing baseline errors, none in touched files. No touched file crosses the 300-line guideline.
- [x] 4.2 Implement playlist-tag expansion: resolve a tagged playlist's contained tracks/loops in playlist order, reusing the same item-resolution path `buildPlaylistPlaybackSession` already uses for normal playlist playback.
  - Coverage audit (mirroring task `3.6`'s "satisfied by existing code" precedent): the task's exact wording — "resolve a tagged playlist's contained tracks/loops in playlist order, reusing the same item-resolution path `buildPlaylistPlaybackSession` already uses" — describes `resolvePlaylistItems` (`playback-queue.ts`) precisely and completely. It already takes a `Playlist` (the exact shape of `RehearsalLibraryTagMatch<'playlist'>['item']`) plus the full `loops`/`sources` collections and returns an ordered, playability-filtered `PlayableItem[]` via the same resolution path `createPlaybackQueue`/`buildPlaylistPlaybackSession` already use for normal playlist playback (not a parallel/duplicate path). It is already exported at the package root (`playback-queue.ts` → `rehearsal-playback.ts` → `index.ts`) and already has focused tests (`rehearsal-playback.spec.ts`'s `resolvePlaylistItems` suite) covering playlist-order resolution, playable-only filtering, and repeated-entry handling.
  - Decision (not pre-decided by artifacts, resolved by re-reading this task's wording against existing code rather than asked): no new production code was added — a wrapper around `resolvePlaylistItems` with an identical signature would be pure duplication, contrary to this repo's "don't add abstractions beyond what the task requires" guidance. Task `4.5`'s wiring will call `resolvePlaylistItems(match.item, loops, sources)` directly for playlist matches, exactly as `4.1`'s `resolveFolderPlayableItems(match.item, folders, fileLinks, loops, sources)` will be called for folder matches.
  - Tests: none added — the existing `resolvePlaylistItems` suite already covers this task's full scope; no gap found on re-reading it against the task's wording.
  - Validation: `nx run audio-library-runtime:test` (50/50 passing, unchanged — no code touched), confirmed via re-reading `playback-queue.ts` and its existing test coverage rather than a fresh run.
- [x] 4.3 De-duplicate the final resolved playable set so a track/loop reachable through more than one matched path (e.g. tagged directly and via a tagged folder) queues exactly once; pin the dedupe key (source id for tracks, loop id for loops) called out as an open question in design.md.
  - Decision (confirmed by existing code rather than asked): design.md's open question — "the exact dedupe key (source id? playable-item id, which differs for loops vs full tracks?)" — is already answered by `PlayableItem`'s existing id convention (`createTrackPlayableItem`/`createLoopPlayableItem` in `@org/audio-library-models`): `track:${source.id}` for tracks, `loop:${loop.id}` for loops. This already **is** "source id for tracks, loop id for loops," just kind-prefixed to avoid an (impossible, since ids are namespaced) track/loop collision — no separate key needed, `item.id` itself is the pinned key.
  - Implementation: `dedupePlayableItems(items: PlayableItem[])` added to `playlist-playback-queue-state.ts` — a single `Set`-tracked filter keeping first-occurrence order, placed directly before `createTransientPlaybackSession` since task `4.4`'s multi-item session constructor will call it when assembling a tag's combined queue. Not re-exported through `saved-playlist-playback-view-model.ts`'s barrel (that file already sits exactly at the 300-line guideline; adding a re-export there for a function with no playlist-specific dependency isn't needed — task `4.5`'s wiring can import it directly from `playlist-playback-queue-state.ts`, mirroring the existing direct-import precedent `playlist-session-mode-queue-pruning.spec.ts` already uses for this same file).
  - Decision (not pre-decided by artifacts, resolved by re-reading the spec's own scenario wording rather than asked): dedup is applied to the final flattened array across all matched paths (direct match + folder expansion + playlist expansion), not scoped to exclude a single playlist's own intentionally-repeated entries (task `4.2`'s existing "keeps repeated playlist entries as distinct queue positions" behavior is specific to a playlist's *own* dedicated playback session, not tag-driven ad-hoc queues). The spec's scenario is phrased as an unconditional entity-level guarantee ("the system includes that track or loop exactly once in the resulting queue"), and a flattened multi-source tag queue showing the identical track twice back-to-back (once from a playlist's own repeat, once from elsewhere) would read as a bug from the rehearsal-queue user's perspective, not a feature — so the same uniform `item.id` key is applied everywhere, with no special-casing for intra-playlist repeats.
  - Tests: new `dedupePlayableItems` suite in `playlist-playback-queue-state.spec.ts` — cross-path duplicate collapsed to one entry (mirroring the spec's own "tagged directly and via a tagged folder" example), relative order preserved among survivors, empty-input passthrough, no-mutation guarantee.
  - Validation: `nx run mobile-rehearsal-player:test-file -- --file=src/app/library/playlists/utils/playlist-playback-queue-state.spec.ts` (13/13 passing), `nx run mobile-rehearsal-player:test --skip-nx-cache` (444/444 passing, up from 440), `nx run mobile-rehearsal-player:typecheck` clean, `eslint` on touched files clean. Both touched files stay under the 300-line guideline (282 and 266 lines).
- [x] 4.4 Add a multi-item transient session constructor (e.g. `createTransientPlaybackSessionFromItems`) alongside `createTransientPlaybackSession` in `playlist-playback-queue-state.ts`, seeding an ordered `PlayableItem[]` queue instead of a single item.
  - Implementation: matches design.md's decision exactly — `createTransientPlaybackSessionFromItems({ items, repeatMode })` added directly after `createTransientPlaybackSession` in `playlist-playback-queue-state.ts`, seeding `queue.items` with the full ordered `PlayableItem[]`, `currentIndex: 0`, `mode: 'ordered'`, and `requestedItemCount: items.length` (mirroring `buildPlaylistPlaybackSession`'s `requestedItemCount: options.playlist.items.length` convention rather than the single-item constructor's hardcoded `1`). Reuses the same `TRANSIENT_QUEUE_PLAYLIST_ID`/`TRANSIENT_QUEUE_PLAYLIST_NAME` constants, so `isTransientQueueSession` and every other transient-queue-aware code path (Up Next's "Create new playlist" vs. "Update current playlist" branching, etc.) recognizes a tag-originated session with zero additional wiring.
  - Also added to `saved-playlist-playback-view-model.ts`'s re-export barrel (alongside the existing `createTransientPlaybackSession` export) — every other file in the app that consumes this domain's functions goes through that barrel rather than importing `playlist-playback-queue-state.ts` directly (confirmed by grep: only the barrel itself and one spec file import it directly), and task `4.5`'s play-action wiring will need external access to start tag playback.
  - Tests: new `createTransientPlaybackSessionFromItems` suite in `playlist-playback-queue-state.spec.ts` — seeds the queue in order starting at index 0 with the correct `requestedItemCount`, is recognized by `isTransientQueueSession`, and handles the empty-items edge case.
  - Validation: `nx run mobile-rehearsal-player:test-file -- --file=src/app/library/playlists/utils/playlist-playback-queue-state.spec.ts` (16/16 passing), `nx run mobile-rehearsal-player:test --skip-nx-cache` (447/447 passing, up from 444), `nx run mobile-rehearsal-player:typecheck` clean, `eslint` clean on all three touched files (their raw line counts sit just over 300, but the repo's `max-lines` rule uses `skipBlankLines`/`skipComments`, so none actually trigger the warning — confirmed by the lint run reporting only pre-existing files elsewhere).
- [x] 4.5 Wire the tag detail play action to expand the currently-filtered (not full unfiltered) match list from `3.2`–`3.4` and start playback via the new multi-item transient session, matching the filter-then-play scenario in the spec.
  - UI: added the tag-level Play control (`CompactPlaybackAction`, `variant="row"`, matching the Recents "resume" row action's exact primitive/scale — design.md's "existing shared playback-action primitive... icon-first, matching Recents/playlist-card conventions") to `TagDetailScreen`'s header trailing area, placed first in `headerActionRow` ahead of the controls/search icons and session avatar (next to, not replacing, per design.md). Disabled when the currently-filtered match list resolves to zero playable items.
  - New composition util: `resolveTagQueuePlayableItems(matches, { fileLinks, folders, loops, sources })` in `library/tags/utils/tag-queue-playback.ts` — expands each visible (already type-filtered/searched) match by kind: track/loop matches resolve directly via `createTrackPlayableItem`/`createLoopPlayableItem` (gated by `isSourcePlayable`, mirroring `resolvePlaylistItems`'s existing convention); playlist matches delegate to `4.2`'s `resolvePlaylistItems`; folder matches delegate to `4.1`'s `resolveFolderPlayableItems`; the concatenated result is deduped via `4.3`'s `dedupePlayableItems`. Lives in the app layer (not `audio-library-runtime`) because it composes across both runtime resolvers and the app-owned dedupe helper. `TagDetailScreen` computes this from `visibleMatches` (the filtered list), not `matches` (the full unfiltered list), directly satisfying the spec's "queues only the filtered subset" requirement — filtering already happens upstream of this call, so no separate filtered/unfiltered branching was needed here.
  - Data wiring: `useLibraryFiles()` now also exposes `fileLinks: tree?.fileLinks ?? []` (mirroring task `2.1`'s precedent for `folders`) — folder expansion needs the file-tree's fileLinks, which no previous task had threaded out of the hook. Threaded through `libraryController.savedLibrary.files.fileLinks` → `app-router.tsx` → `TagDetailScreen`'s new `fileLinks` prop.
  - Playback wiring: new `toggleItemQueuePlayback(items: PlayableItem[])` action added to `createSavedTrackPlaybackActions` (`use-saved-track-playback/actions.ts`), backed by a new `startItemQueuePlayback` helper (`item-queue-playback-actions.ts`, sibling to `4.2`'s `playlist-playback-actions.ts`) that mirrors `startPlaylistPlayback`'s exact shape: build the session via `4.4`'s `createTransientPlaybackSessionFromItems`, load the first item through the existing `playbackController`, and set `activePlaylistContextRef.current = null` on success (matching `toggleSourcePlayback`'s established precedent for playback that isn't backed by a real `Playlist` entity), so the resulting session is correctly recognized as a transient queue by `isTransientQueueSession` and every downstream consumer (Up Next, repeat mode, "Create new playlist" from queue) with zero additional wiring.
  - Decision (not pre-decided by artifacts, resolved from nearby naming convention rather than asked): named the action `toggleItemQueuePlayback` (not `playTagQueue`) since it has no tag-specific dependency itself — it just starts playback from an arbitrary resolved `PlayableItem[]`, mirroring the existing `toggleSourcePlayback`/`togglePlaylistPlayback` naming pattern already used for "start playback of X" actions in this same file, generalized to "X = an ad-hoc item queue" rather than a saved entity.
  - Cross-tab navigation: `app-router.tsx`'s `<TagDetailScreen>` JSX/callback composition (previously ~30 inline lines) was extracted into a new `AppRouterTagDetailScreen` wrapper component (`tags/components/tag-detail-screen/app-router-tag-detail-screen.tsx`), mirroring the established `AppRouterRecentsScreen` precedent (a thin per-screen wrapper adapting router-level state into the screen's own prop shape). This was needed to keep `app-router.tsx` under the 300-line guideline once `fileLinks`/`onPlayMatches` wiring was added (it had crossed to 302 lines, a genuinely new overage introduced by this task, unlike `actions.ts`'s pre-existing one below) — not a cosmetic rename, the callback closures (`onClose`/`onOpenFolder`/`onOpenPlaylist`) moved into the wrapper too, which is what actually reduced `app-router.tsx`'s size. Caught and preserved a subtle behavior dependency in the process: `MobileShell` gates the tag-detail overlay via `!tagDetailScreen` truthiness, so the outer `selectedTag ? <AppRouterTagDetailScreen .../> : null` ternary had to stay at the `app-router.tsx` call site rather than moving the null-check inside the wrapper (a React element is always truthy even if the component internally renders `null`, which would have made the overlay permanently visible).
  - File-length note: `use-saved-track-playback/actions.ts` was already over the 300-line guideline before this task (320 lines, unflagged-but-present since before this session); this task's `toggleItemQueuePlayback` addition grows it to 335. Flagged, not extracted, consistent with this file's own established precedent elsewhere in this task history (`1.3`, `1.4`, `1.9`, `2.3.5`, `3.2`, `3.5`) of not pulling an unrelated large-scale split into an otherwise-focused slice.
  - Tests: new `tag-queue-playback.spec.ts` (8 cases) covering direct track/loop resolution (including unplayable-source/missing-parent-source exclusion), playlist-match delegation, folder-match delegation, the spec's own cross-path dedupe scenario (track tagged directly and reachable via a tagged folder queues once), and the empty-match case.
  - Manual verification (integrated browser): opened tag "a" (matching the "test" folder, which contains 1 track + 1 loop + 1 playlist) — the header Play button rendered enabled; pressing it started playback immediately (mini player: "Playing • Current queue • 1 o..."); Up Next confirmed a 2-item queue ("Copy of Radio Video Sample 1.mp3" then its loop, "Item 1 of 2") — the playlist match correctly did **not** contribute further items (folder expansion only pulls tracks/loops per design.md, not a nested playlist's own contents) — with fully functional transport, seek, repeat/shuffle controls. No new console errors (only the pre-existing Drive/auth 400/COOP noise documented elsewhere in this file).
  - Validation: `nx run mobile-rehearsal-player:test-file -- --file=src/app/library/tags/utils/tag-queue-playback.spec.ts` (8/8 passing), `nx run mobile-rehearsal-player:test --skip-nx-cache` (455/455 passing, up from 447), `nx run mobile-rehearsal-player:typecheck` clean, `eslint` clean on all touched files except the one pre-existing `actions.ts` max-lines warning noted above (confirmed `app-router.tsx` dropped back under the threshold after the extraction).
  - **Bug found and fixed during user review of this task**: the bottom nav (Library/Add/Recents) appeared inert while viewing a tag's contents — tapping a destination updated `MobileShell`'s internal `activeDestination` state but the tag detail overlay (rendered whenever `tagDetailScreen` is truthy, per `3.1`'s design) stayed on screen covering it, since nothing told `AppRouter` to clear `selectedTag`. This is a pre-existing gap from `3.1`'s original overlay mechanism, not something this task introduced, but the user hit it while reviewing this task's new Play button, so it's fixed here rather than filed separately. Fix: added an `onCloseTagDetailScreen: () => void` prop to `MobileShellProps`, wired from `app-router.tsx` as `() => setSelectedTag(null)`; `MobileShell`'s tab-bar `onSelectDestination` handler now calls it whenever `tagDetailScreen` is truthy, before updating `activeDestination`, so switching tabs from a tag view now closes the overlay and shows that destination's own last state (matching "show the default for that selected location" — Library/Recents/Add each already preserve their own independent state across tab switches, unrelated to tags).
  - No new automated test — `mobile-shell/index.tsx` has no existing spec file and follows this codebase's established convention (noted repeatedly elsewhere in this file, e.g. task `3.1`) of not unit-testing top-level wiring/render components; covered by manual verification instead.
  - Manually verified in the integrated browser: opened tag "a", tapped Recents — overlay closed, Recents screen shown correctly; reopened tag "a" from the Recents chip, tapped Add — overlay closed, Add/Drive browser shown; reopened tag "a" again, tapped Library (the already-active underlying tab) — overlay closed, Library's Tags view shown (its last-displayed state). No new console errors in any case (only the pre-existing Drive/auth 400/COOP noise documented elsewhere in this file).
  - Validation: `nx run mobile-rehearsal-player:typecheck` clean, `nx run mobile-rehearsal-player:test --skip-nx-cache` (455/455, unchanged — no new test), `eslint` clean on both touched files (`mobile-shell/index.tsx`, `app-router.tsx`).
- [x] 4.6 Add focused tests for folder expansion, playlist expansion, cross-path dedupe, and the new multi-item session constructor.
  - Coverage audit (same "satisfied incrementally" precedent as tasks `3.6`/`4.2`): re-read this task's exact wording against existing coverage added during `4.1`-`4.5` and found no gap.
    - Folder expansion: `playback-queue.spec.ts`'s `resolveRehearsalLibraryFolderSubtreeIds`/`resolveFolderPlayableItems` suites (nested descendants, sibling exclusion, availability gating, playlist-file-link non-expansion, empty-subtree case) — added in `4.1`.
    - Playlist expansion: `rehearsal-playback.spec.ts`'s pre-existing `resolvePlaylistItems` suite (playlist-order resolution, playable-only filtering, repeated-entry handling) — confirmed sufficient in `4.2`, no new code/tests needed there.
    - Cross-path dedupe: covered at two levels — the general-purpose `dedupePlayableItems` suite in `playlist-playback-queue-state.spec.ts` (`4.3`: cross-path duplicate collapse, order preservation, empty input, no-mutation), and `tag-queue-playback.spec.ts`'s dedicated end-to-end case "queues a track exactly once when reachable both directly and via a tagged folder" (`4.5`) — the exact scenario named by both this task and the spec.
    - Multi-item session constructor: `createTransientPlaybackSessionFromItems` suite in `playlist-playback-queue-state.spec.ts` (`4.4`: seeds queue in order from index 0, recognized by `isTransientQueueSession`, empty-items case).
  - No new code or test files added — verified by re-running the relevant suites rather than assuming the prior notes were accurate.
  - Validation: `nx run audio-library-runtime:test` (50/50 passing), `nx run mobile-rehearsal-player:test-pattern -- --pattern='src/app/library/{tags,playlists}/**/*.spec.ts'` (73/73 passing).

## 5. Tag Editor Suggestions

- [x] 5.1 Add `getActiveTagEditorInputSegment` and `resolveTagEditorSuggestions` (pure functions, `tag-editor-sheet/model.ts`):
  - `getActiveTagEditorInputSegment(tagInput: string)` returns the trimmed text after the last comma in `tagInput` (or the whole trimmed input when there's no comma yet) — i.e. the tag currently being typed, ignoring any complete tags already typed earlier in the same comma-separated input.
  - `resolveTagEditorSuggestions(availableTagUsage: RehearsalLibraryTagUsage[], currentTags: string[], activeSegment: string)` returns up to 12 suggested tag strings — most-used-first when `activeSegment` is empty, case-insensitive substring-matched against `activeSegment` and still usage-ranked when non-empty — always excluding any tag already present in `currentTags` (case-insensitive comparison, matching `normalizeLibraryEntityTags`'s own dedupe rule).
  - Implementation: `getActiveTagEditorInputSegment` splits `tagInput` on `,` and trims the last segment — this naturally covers both the no-comma case (single-element split, the whole trimmed input) and the trailing-comma case (empty last segment) with no branching. `resolveTagEditorSuggestions` filters `availableTagUsage` (already most-used-first with an alphabetical tie-break, per `aggregateRehearsalLibraryTags`) by excluding any tag case-insensitively present in `currentTags`, then a case-insensitive substring match against `normalizeSearchQuery(activeSegment)` (reusing the same normalization helper `filterSavedTagUsageByQuery`/`filterTagMatchesByQuery` already use, per the design's stated convention) when non-empty, then slices to `TAG_EDITOR_SUGGESTION_CAP = 12` — no re-sort needed since filtering a pre-sorted array preserves its order.
  - No unresolved decisions — task wording already pinned the exact signatures, cap value, and exclusion/ranking rules; nothing to confirm with the user.
  - Tests: deferred to task `5.2`, which is this file's own next numbered subtask and exists specifically to add this coverage (mirroring the `1.1`/`1.2` split earlier in this task list).
  - Validation: `nx run mobile-rehearsal-player:typecheck` clean, `eslint` on the touched file clean, file stays well under the 300-line guideline (53 lines).
- [x] 5.2 Add focused unit tests: `getActiveTagEditorInputSegment` (no comma, trailing comma, multiple commas, surrounding whitespace) and `resolveTagEditorSuggestions` (empty-segment popularity ranking, substring matching, excluding already-applied tags case-insensitively, cap enforcement at 12, empty-result case).
  - Implementation: added two new `describe` blocks to the existing `tag-editor-sheet/model.spec.ts`, matching the file's existing fixture-and-assertion style. `getActiveTagEditorInputSegment` covers all four named cases (no comma, trailing comma → empty segment, multiple commas → text after the last one, surrounding whitespace trimmed). `resolveTagEditorSuggestions` covers all five named cases against a shared 5-tag usage fixture (empty-segment popularity ranking, case-insensitive substring narrowing, case-insensitive exclusion of already-applied tags, cap enforcement at exactly `TAG_EDITOR_SUGGESTION_CAP` using a 20-tag fixture, and the empty-result case for a non-matching segment).
  - No unresolved decisions — test scenarios were fully enumerated by the task itself.
  - Validation: `nx run mobile-rehearsal-player:test-file -- --file=src/app/library/components/tag-editor-sheet/model.spec.ts` (12/12 passing), `eslint` on both touched files clean, `typecheck-tests` shows only the same pre-existing baseline errors documented elsewhere in this file (`search-input-value.spec.ts`, `library-files-playlist-create.spec.ts`), neither in a touched file. File stays under the 300-line guideline (118 lines).
- [x] 5.3 Wire `availableTagUsage` data: add a `useMemo(() => aggregateRehearsalLibraryTags(...))` in `SavedRehearsalLibrarySection`'s `index.tsx`, computed from the same `savedLibrarySources`/`savedLoops`/`savedPlaylists`/`libraryFiles.folders` props it already receives (mirroring the existing `app-router.tsx`/`browse-content.tsx` pattern from `2.1`/`2.3.2`), threaded through `SavedRehearsalLibraryTagEditorSheet` into `TagEditorSheet` as a new prop.
  - Implementation matched the plan exactly: `saved-rehearsal-library-section/index.tsx` gained an `availableTagUsage` `useMemo`, byte-for-byte the same shape `browse-content.tsx` (`2.3.2`) already uses (`entityCollections: { loops: savedLoops, playlists: savedPlaylists, sources: savedLibrarySources }, folders: libraryFiles.folders`, deps `[libraryFiles.folders, savedLibrarySources, savedLoops, savedPlaylists]`), passed to `<SavedRehearsalLibraryTagEditorSheet availableTagUsage={availableTagUsage} .../>`. The wrapper (`tag-editor-sheet.tsx`) and the shared `TagEditorSheet` (`tag-editor-sheet/index.tsx`) both gained a matching `availableTagUsage: RehearsalLibraryTagUsage[]` prop, threaded straight through.
  - No unresolved decisions — task wording and the design's "Data wiring" note already pinned the exact `useMemo` shape and prop-threading path.
  - Deliberately not consumed yet: `TagEditorSheet` destructures `availableTagUsage` but doesn't render anything with it — task `5.4` is the next numbered subtask and is exactly "render the suggestion row using this prop." This produces one expected `@typescript-eslint/no-unused-vars` warning (not an error) on the prop, matching the same prop-threaded-ahead-of-consumption pattern already present elsewhere in this codebase (`drive-library-root-selector.tsx`'s `isSearchMode`) — confirmed via a full `nx run mobile-rehearsal-player:lint` that this doesn't fail the build (0 errors, warnings only).
  - File-length note: `saved-rehearsal-library-section/index.tsx` grew from 351 to 366 lines, on top of the overage already flagged repeatedly in this file's own task history (`1.9`, `2.3.5`, `3.5`). Flagged again, not extracted, consistent with that established precedent — an unrelated large-scale split would be a distraction from this task's small data-wiring scope.
  - Tests: none added — pure prop-threading with no branching logic, matching this codebase's established no-render-test convention for this component family (same rationale documented at tasks `3.1`/`2.3.5`).
  - Validation: `nx run mobile-rehearsal-player:typecheck` clean, `nx run mobile-rehearsal-player:test --skip-nx-cache` (466/466 passing, unchanged), `eslint` on all three touched files shows only the two expected warnings above (0 errors).
- [x] 5.4 Render the suggestion row in `TagEditorSheet`, directly below the tag input: `activeSegment = getActiveTagEditorInputSegment(tagInput)`, chips built from `resolveTagEditorSuggestions(availableTagUsage, draftTags, activeSegment)`. Tapping a chip replaces **only** the active segment within `tagInput` with the selected tag's text, preserving any complete tags already typed earlier in that same input (before the last comma) — it does not commit anything to `draftTags` and does not clear the whole field; committing still happens only through the existing submit action (`handleAddTag`, which already runs the full input through `parseLibraryTagInput`/`addLibraryEntityTag`), unchanged. The row renders nothing when there are no suggestions. Extract into a colocated subcomponent if adding this pushes `tag-editor-sheet/index.tsx` over the 300-line guideline.
  - Implementation: added `applyTagEditorSuggestion(tagInput, suggestion)` to `tag-editor-sheet/model.ts` (a small pure helper for the segment-replacement logic: replaces everything after the last comma with `suggestion`, or the whole input when there's no comma yet), and wired `TagEditorSheet` to compute `activeSegment`/`suggestions` on every render (cheap filter over an already-small array, no `useMemo` needed) and render a suggestion row that calls `setTagInput((current) => applyTagEditorSuggestion(current, suggestion))` on tap — never touching `draftTags`.
  - File-length extraction (as pre-authorized by the task text): adding the suggestion row inline pushed `index.tsx` to 313 lines, over the 300-line guideline. Extracted a new colocated `tag-suggestion-row.tsx` (`TagSuggestionRow`, 59 lines: label + `InteractionChip` row + its own styles, returns `null` when `suggestions` is empty) and had `TagEditorSheet` delegate to it. `index.tsx` settled at 286 lines.
  - **Bug found and fixed during manual verification, not spec'd but required by spec.md's own scenario**: spec.md's "Selecting a suggested tag completes only the tag currently being typed" scenario asserts "the tapped tag no longer appears in the suggestion row" after a tap. The initial `resolveTagEditorSuggestions` (task `5.1`) only excluded tags already in `currentTags`/`draftTags` and did substring matching — after tapping, the active segment becomes the exact tag text, but a plain substring check still matches it, so the tapped chip kept reappearing (confirmed live in the browser: typing "alto, war" + tapping "Warmup" left the input at "alto, Warmup" with a "Warmup" chip still shown). Fixed by adding a same-function exact-match exclusion: `normalizedTag.includes(normalizedSegment) && normalizedTag !== normalizedSegment`. Added a regression test (`excludes a tag that exactly matches the active segment, case-insensitively`) to `model.spec.ts` (now 17/17 passing, up from 16).
  - No unresolved UX decisions — task wording pinned the exact interaction; the one open call (whether to add a "Suggested tags" label above the chip row) was already effectively pre-decided by design.md's own wording naming it "a single 'Suggested tags' chip row."
  - Manually verified in the integrated browser: opened the folder tag editor for "miao" (empty library, no suggestions shown — correct empty state); added "Alto, Warmup" and saved; reopened the track's tag editor and confirmed "SUGGESTED TAGS" showed both chips, most-used order; typed "al" and confirmed live narrowing to just "Alto"; typed "alto, war" and confirmed the suggestion row narrowed to only "Warmup" (not matching the full string); tapped "Warmup" and confirmed the input became "alto, Warmup" (preserving "alto") with the suggestion row now empty (exact-match exclusion working); confirmed tapping never touched the chips/draft-tags row (only the existing "+"/submit action commits). Restored the demo library to its original untagged state afterward (removed the folder's tags; the track's tags were never actually committed since suggestion taps don't auto-submit, confirming that requirement too). No new console errors (only the pre-existing Drive/auth 400/401/COOP noise documented elsewhere in this file).
  - Validation: `nx run mobile-rehearsal-player:test-file -- --file=src/app/library/components/tag-editor-sheet/model.spec.ts` (17/17 passing), `nx run mobile-rehearsal-player:test --skip-nx-cache` (471/471 passing, up from 466), `nx run mobile-rehearsal-player:typecheck` clean, `eslint` on all four touched files clean (0 errors, 0 warnings — including the previously-flagged `availableTagUsage` unused-var warning from `5.3`, now resolved). All touched files stay well under the 300-line guideline (286, 59, 70, 150 lines).
  - **Revision after user feedback ("Pressing a suggested tag should add it and clear that text (up to the previous comma) from the textbox")**: this reverses the "complete the segment's text, commit only via the existing submit action" behavior above, which itself was an approved spec scenario (`spec.md`'s "Selecting a suggested tag completes only the tag currently being typed", asserting "the tag is not added to the entity's draft tags until the user submits the input"). Design.md documented an even earlier, explicitly-rejected first draft that did commit-on-tap but cleared the *whole* input — rejected because it would silently discard other not-yet-submitted tags typed earlier in the same input (e.g. typing `Alto, Sop` and tapping `Soprano` must not lose `Alto`). Before implementing, confirmed via `AskUserQuestion` that the new behavior should NOT repeat that mistake: only the tapped tag commits, and only its own segment clears — any earlier segment (e.g. `Alto`) stays in the input, still uncommitted, unaffected by the tap. Updated `spec.md`'s scenario (renamed to "Selecting a suggested tag commits only the tag currently being typed") and `design.md`'s decision section (added a "Resolution" paragraph) to reflect this before changing code, per this repo's deliberate-execution convention for spec-contradicting changes.
  - Implementation: replaced `applyTagEditorSuggestion(tagInput, suggestion): string` (which returned the input with the segment replaced by the suggestion's text) with `removeTagEditorInputActiveSegment(tagInput): string` (which returns the input with the active segment — and its preceding comma, if any — removed entirely; empty string when there's no comma). `TagSuggestionRow`'s `onSelectSuggestion` handler in `TagEditorSheet` now does both in one tap: `setDraftTags((currentTags) => addLibraryEntityTag(currentTags, suggestion))` (the same commit path the existing "+" submit action already uses) and `setTagInput((currentInput) => removeTagEditorInputActiveSegment(currentInput))`. The exact-match suggestion-exclusion added in the prior revision stays in `resolveTagEditorSuggestions` — no longer load-bearing for the tap flow itself (since committing to `draftTags` now excludes the tag via the existing `currentTags` check), but still correct defensive behavior if a user manually types a tag's exact name character-by-character without tapping the suggestion chip.
  - Tests: replaced the `applyTagEditorSuggestion` suite in `model.spec.ts` with a `removeTagEditorInputActiveSegment` suite covering the same four shapes (no comma → clears entirely, one earlier tag preserved, multiple earlier tags preserved, trailing-comma/empty-segment case) — still 17/17 tests passing (one test swapped for its equivalent, not a net addition).
  - Manually re-verified in the integrated browser (with real, pre-existing Drive demo data this time — "This Masquerade.m4a" already tagged "test", the "miao" folder already tagged "miao"/"test"): opened the track's tag editor, tapped the "miao" suggestion — it was added as a chip immediately (no "+" press needed) and the suggestion row correctly emptied (no more eligible tags); typed "Alto, mi" after removing the "miao" chip again — the suggestion row narrowed to just "miao"; tapped it — the input became exactly "Alto" (uncommitted, unaffected) while "miao" appeared as a new chip alongside "test". Cancelled to discard the test changes and confirmed the demo library's real tags were unaffected. No new console errors (only the pre-existing Drive/auth 400/401/COOP noise documented elsewhere in this file).
  - Validation: `nx run mobile-rehearsal-player:test-file -- --file=src/app/library/components/tag-editor-sheet/model.spec.ts` (17/17 passing), `nx run mobile-rehearsal-player:test --skip-nx-cache` (471/471 passing, unchanged), `nx run mobile-rehearsal-player:typecheck` clean, `eslint` on all three touched files clean (0 errors, 0 warnings).
- [x] 5.5 Manually verify in the integrated browser: opening the tag editor for a track/loop/playlist/folder shows popular tags by default; typing narrows suggestions live; typing `Alto, Sop` shows suggestions matching only `Sop`, not the full string; tapping a suggestion immediately adds it to the entity's draft tags and clears just that segment's text (e.g. `Alto, Sop` + tapping a `Soprano` suggestion → draft tags gain `Soprano`, input becomes `Alto`) without discarding or committing the already-typed `Alto`; submitting still adds every comma-separated tag currently in the input at once, unchanged from today; tags already on the entity never appear as suggestions; the row disappears once no eligible tag remains to suggest.
  - Manually verified in the integrated browser against the existing demo library (tags in use: `miao`, `test`, `time`), opening the tag editor for the "This Masquerade.m4a" track (existing tag: `test`): default suggestions showed `miao`/`time` (most-used-first, `test` correctly excluded as already-applied); typing `mi` live-narrowed the row to just `miao`; typing the rest of the input to `miao, ti` (the exact `Alto, Sop`-shaped scenario) narrowed the row to just `time`, matching only the active segment after the comma; tapping `time` added it to the draft-tag chips immediately (now `test`, `time`) and cleared the input back to `miao` (the earlier, not-yet-submitted segment), with the suggestion row correctly disappearing (exact-match exclusion against the now-single-tag `miao` input); pressing the existing `+` submit action then added `miao` too (now `test`, `time`, `miao`), confirming submit-adds-everything-in-the-input is unchanged; with all three library tags now applied to the entity, the suggestion row rendered nothing (no eligible tags left). Cancelled afterward (no "Save tags" press) to leave the demo library's tag state unchanged. No console errors observed (only the pre-existing Drive/auth 400/401/COOP noise documented elsewhere in this file).

## 6. Final Validation

- [x] 6.1 Manually verify the full flow in the integrated browser: Recents tag list reflects real tags (including empty-state and overflow-entry-point branches) → tag detail lists correct matches → type filter and search narrow correctly → playing filtered vs. unfiltered queues the right set → Up Next reflects that queue with working transport/repeat/reorder → folder/playlist row taps navigate correctly outside of play → tag editor suggestions surface popular and matching tags correctly.
  - **Bug found and fixed during this verification**: opening any tag detail screen (a single tap on a Recents tag chip or a Library Tags row) threw a React "Maximum update depth exceeded" error loop in the console. Root cause: `TagDetailSection` (`tags/components/tag-detail-section/index.tsx`) passes `onToggleFilterPopover`/`onToggleSearchBar` into `useTagDetailHeaderSearchActions` as new inline arrow functions on every render; that hook's `useEffect` listed them directly in its dependency array (unlike its sibling `useTagDetailHeaderPlayback`, which already guards against exactly this with a ref + stable `useCallback`), so the effect re-fired every render and pushed a freshly-constructed actions object up into `LibraryScreen`'s `setDetailSearchActions` state on every single render — triggering a parent re-render that recreated the inline callbacks again, ad infinitum. Fixed by applying the same ref + stable-`useCallback` pattern already established in `use-tag-detail-header-playback.ts` to `use-tag-detail-header-search-actions.ts` (`onToggleFilterPopoverRef`/`onToggleSearchBarRef`, `handleFilterActionPress`/`handleSearchActionPress`), so the effect now only re-fires when `hasActiveFilters`/`isFilterPopoverVisible`/`isSearchBarVisible`/`onDetailSearchActionsChange` genuinely change.
  - Diagnosis note: isolating this took a `console.warn`-based render/effect trace (temporarily added and removed) plus testing in a from-scratch browser tab, since the Browser pane's console-log reader accumulates history across reloads within one tab — an early false negative ("still broken after the fix") turned out to be stale log entries from earlier repro attempts bleeding into the read, not a real ongoing loop. A fresh tab confirmed the fix eliminates the error entirely, reproduced cleanly across two different tags (`miao`, `test`).
  - No new automated test added for this hook — consistent with this codebase's established convention (documented throughout this file, e.g. tasks `3.1`, `3.5`) of not unit-testing pure effect-wiring/prop-threading hooks with no branching logic, and no `renderHook`-style testing exists anywhere in this codebase to test the ref-stabilization behavior directly. Covered by the manual verification below instead.
  - Manually verified in the integrated browser (fresh tab, demo library tags: `miao` (2 items: 1 track + 1 folder), `test` (2 items: same folder + a different track), `time` (1 item)): Recents "Popular tags" module shows all 3 real tags with no placeholder chips and no overflow chevron (3 tags, under the cap of 6 — the empty-state and overflow branches were already dedicated-task-verified in `2.2`/`2.3.6` and are structurally unchanged since); tapping a tag chip opens tag detail with the correct match list and no console errors; the Sort/Type controls panel opens and the "Tracks" type filter narrows 2 items → 1 correctly; pressing Play with the type filter active queues only the filtered 1-item subset (mini player: "1 of 1"); clearing the filter and pressing Play unfiltered queues the folder-expanded 2-item set (mini player: "1 of 2", matching the folder's contained track + loop, with the sibling playlist inside that folder correctly not expanded, and the directly-tagged track not double-counted against the folder's own contents); Now Playing shows full working transport (seek, ±15s, waveform) and repeat-mode controls; Up Next shows "Active rehearsal queue" with a working "Create new playlist" action, confirming the transient-queue session is recognized correctly; tapping the folder row (outside the play action) navigates to Library Files at that folder while playback continues uninterrupted in the background; switching tabs (Recents ↔ Library) preserves the active playback session; the tag-scoped search panel opens with tag-specific helper/placeholder copy and narrows the match list live. Playlist-row navigation (`onOpenPlaylist`) was not re-exercised with a fresh temporary playlist this session — it uses the same `getTagMatchNavigationTarget` code path already dedicated-task-verified in `3.5` and untouched by this session's fix, so that prior coverage stands. Tag editor suggestions were already fully verified in task `5.5`, immediately prior in this same session. No console errors beyond the pre-existing Drive/auth 400/401 and asset 404 noise documented throughout this file.
  - Validation: `git diff --stat` after removing all temporary debug instrumentation confirms only `use-tag-detail-header-search-actions.ts` has a real diff — the fix itself, with no leftover debug code in any of the three other files touched during diagnosis.
- [x] 6.2 Run project typecheck and lint on touched files, and the narrowest relevant Nx test targets (`audio-library-runtime`, `mobile-rehearsal-player`), before considering this change ready to archive.
  - `npm exec -- nx run-many -t typecheck lint test -p audio-library-runtime mobile-rehearsal-player`: typecheck clean on both projects; tests all green (`audio-library-runtime` and `mobile-rehearsal-player` both report `# fail 0`, 471/471 passing in `mobile-rehearsal-player`); lint reports 0 errors, 10 warnings — all pre-existing `max-lines` file-length overages and one pre-existing unused-var warning already documented throughout this file's task history, none newly introduced and none in the file touched by task `6.1`'s bug fix (`use-tag-detail-header-search-actions.ts`).
