## 1. Tag Aggregation Foundation

- [x] 1.1 Add a library-wide tag aggregation query (likely in `audio-library-runtime`) that scans saved tracks, loops, playlists, and folders and returns the deduped, normalized set of in-use tags with per-tag usage counts, reusing `normalizeLibraryEntityTags`'s trim/whitespace/case-insensitive dedupe rule rather than reimplementing it.
  - Decision (confirmed with user): `normalizeLibraryEntityTags` currently lives app-side (`tag-editor-sheet/model.ts`), but `audio-library-runtime` cannot depend on the app. Move `normalizeLibraryEntityTags` into `@org/audio-library-models` (new `src/lib/rehearsal-tags.ts`, exported from the package `index.ts`) so both the app's tag editor and the new runtime aggregation query share one implementation. `parseLibraryTagInput`/`addLibraryEntityTag`/`removeLibraryEntityTag` stay in the app's `model.ts` and import the moved function from `@org/audio-library-models`.
  - New query: `aggregateRehearsalLibraryTags` in `packages/audio-library-runtime/src/lib/rehearsal-library-tags.ts`, taking `{ entityCollections: RehearsalLibraryEntityCollections; folders: RehearsalLibraryFolderNode[] }` and returning `RehearsalLibraryTagUsage[]` (`{ tag, count }`), pre-sorted most-used-first with an alphabetical tie-break (case-insensitive) so task `2.1`'s Recents cap can simply slice the result. `count` = number of distinct entities carrying the tag (post per-entity dedupe). Exported from the package root (`index.ts`) for app consumption.
- [x] 1.2 Add focused unit tests for the aggregation query: cross-entity dedupe with mixed casing/whitespace, usage counts, and the empty-library (no tags) case.

## 2. Recents Tag Module

- [x] 2.1 Replace the hardcoded `RECENTS_SHORTCUT_TAGS` array and the no-op `onPlayRecentShortcut` wiring in `screens/recents/` with the real aggregated tag list from `1.1`, capped and ordered most-used-first with an alphabetical tie-break.
  - Decision (confirmed with user): cap = 6.
  - Data wiring: extend `useLibraryFiles`'s return with `folders: tree?.folders ?? []` (only `destinationFolders` was previously exposed). In `app-router.tsx`, add a `useMemo` calling `aggregateRehearsalLibraryTags` over `libraryController.savedLibrary.{savedLibrarySources,savedLoops,files.folders}` + `libraryController.playlists.savedPlaylists`, matching the existing `savedSourceIds`/`savedLoopIds` memo pattern there. Thread the resulting (already-sorted) `RehearsalLibraryTagUsage[]` down through `AppRouterRecentsScreen` into `RecentsScreen` as a new `libraryTagUsage` prop (full uncapped list, so tasks `2.2`/`2.3` can reuse it for empty-state/overflow checks without another prop-shape change).
  - Display: in `screens/recents/index.tsx`, replace `RECENTS_SHORTCUT_TAGS` with a `RECENTS_SHORTCUT_TAG_CAP = 6` constant and `shortcutTags = libraryTagUsage.slice(0, RECENTS_SHORTCUT_TAG_CAP).map((usage) => usage.tag)`, used everywhere `RECENTS_SHORTCUT_TAGS` was used.
  - Out of scope for this slice (left as-is / deferred): `onPlayRecentShortcut`'s no-op resume-most-recent behavior (task `2.4` replaces it with real navigation once task `3.1`'s tag detail screen exists), and the empty/overflow UI states (tasks `2.2`/`2.3`).
  - Bug found and fixed during manual verification (pre-existing, predates this change, in code from the archived `streamline-mobile-rehearsal-ux` change): `resolveSavedRehearsalLibrarySources` (`saved-rehearsal-library/view-model.ts`) silently discarded a saved source's `tags` field whenever that source's id also appeared in the currently-cached Drive browse/search snapshot (`visibleSourcesById[source.id] ?? source` did a full replacement with the tag-less Drive-fetched object instead of a merge). This made tag edits vanish for any source Drive still had cached, and blocked verifying this entire feature. Fixed by preserving `source.tags` across the merge; added a regression test (`view-model.spec.ts`). Also renamed the Recents module title from "Popular shortcuts" to "Popular tags" per user request (not previously specified in this change's artifacts).
- [x] 2.2 Replace the always-shown placeholder chip row with concise empty-state guidance when no saved entity carries a tag yet.
  - Decision (confirmed with user): copy is "No tags yet. Tag a track, loop, playlist, or folder in Library to see it here." — matches the existing tag-editor-sheet empty-state convention.
  - Implementation: new pure `getRecentsTagModuleCopy({ hasSavedTagUsage })` in `screen-copy.ts` returning `{ body }`, unit-tested in `screen-copy.spec.ts` now (same slice), ahead of task `2.5`'s broader test sweep. In `index.tsx`, derive `hasSavedTagUsage = libraryTagUsage.length > 0`; show the body text when `!hasSavedTagUsage || !isRecentPlaybackAvailable` (empty-state guidance always shows regardless of recent-playback state; the existing "optional shortcuts" hint keeps its prior recent-playback-gated behavior when tags do exist); only render `shortcutsMeta` and the chip `tagRow` when `hasSavedTagUsage` is true — no placeholder chips.
- [ ] 2.3 Add an overflow entry point to a full tag list when more tags exist than the Recents module's compact display cap.
  - Decision (confirmed with user, supersedes design.md's originally-considered "reuse Library's filter surface" alternative): build a dedicated **Tags** view as a fifth entry in Library's existing Files/Tracks/Loops/Playlists view-switcher, listing every in-use tag with its usage count, sortable by name or count (ascending/descending), and searchable. The Recents overflow action navigates there. See subtasks `2.3.1`-`2.3.7` and the updated design.md/proposal.md/spec.md.
- [x] 2.3.1 Add `'tags'` to the `SavedRehearsalLibraryView` union and wire it into the view-switcher chip row, `detail-mode.ts`'s visible-sections table, and the existing view-switcher lock machinery (`view-switcher-lock-model.ts`), matching the integration pattern already used for Files/Tracks/Loops/Playlists.
- [x] 2.3.2 Build the Tags list: render `aggregateRehearsalLibraryTags` output as rows (tag name + usage count) using the existing row shell conventions.
  - Implementation: new `SavedTagsList` component at `packages/mobile-rehearsal-player/src/app/library/tags/components/saved-tags-list/index.tsx` — a new `library/tags/` domain folder sibling to `library/loops/`, `library/playlists/`, `library/drive/`, `library/search/`, per the folder-structure convention's "avoid adding more unrelated siblings to already flat directories like `library/components/`" rule (that directory is already 48 files). Renders one `ExplorerListRow` per tag inside `ExplorerListSurface` (same row shell `BrowseSourceGroup`/`SavedLoopListRow` use), `title` = tag name, `metadata` = a pluralized usage-count label (`getSavedTagUsageMetadataLabel` in a colocated `model.ts`/`model.spec.ts`, matching the `explorer/model.ts` colocation pattern), `leadingIcon` = `tag-outline` (matching the `folder-outline`/`playlist-music-outline`/`music-note-outline` per-entity-type icon convention). Falls back to concise "No tags yet…" body text when the list is empty (not spec-required for this view, but avoids a blank screen; kept to one string constant, no new empty-state component). Row `onPress` navigation is deferred to task `2.3.5`.
  - Data wiring decision (not spec'd, decided from nearby code convention rather than asked): `browse-content.tsx` already receives `savedLibrarySources`/`savedLoops`/`savedPlaylists`/`libraryFiles` (which already exposes `.folders`, added in task `2.1`) as props, so it computes its own `useMemo(() => aggregateRehearsalLibraryTags(...))` locally rather than threading the Recents-side `libraryTagUsage` memo down from `app-router.tsx` through `LibraryScreen`. This matches the existing pattern where `LibraryScreen` derives its own state from `libraryController`/raw collections instead of consuming app-router-computed memos (which today are only threaded to `AppRouterRecentsScreen`), and both call sites share the same pure `aggregateRehearsalLibraryTags` source of truth, so there's no drift risk despite the duplicate `useMemo` call site.
  - New render branch: added `shouldRenderSavedTagsList(selectedView)` to `browse-content-model.ts` (tested in `browse-content.spec.ts`), mirroring `shouldRenderFilesExplorer`'s early-return pattern in `browse-content.tsx`.
  - Tests: `model.spec.ts` (singular/plural usage-count label) and a `browse-content.spec.ts` case for `shouldRenderSavedTagsList`. No render/interaction test — this repo's `*.spec.tsx` convention (confirmed by scanning existing row components like `BrowseSourceGroup`/`SavedLoopListRow`) tests companion pure logic/view-model files, not RN component rendering, so the pure label-formatting logic is what's covered.
  - Validation: `nx run mobile-rehearsal-player:test-pattern -- --pattern='src/app/library/tags/**/*.spec.ts'`, `nx run mobile-rehearsal-player:test-file -- --file=src/app/library/components/saved-rehearsal-library-section/browse-content.spec.ts`, `nx run mobile-rehearsal-player:typecheck`, `nx run mobile-rehearsal-player:lint` all pass; lint warnings present are pre-existing and outside touched files.
- [x] 2.3.3 Add a sort control (Name / Count, ascending/descending) for the Tags list.
  - Decision (confirmed with user): no existing ascending/descending toggle precedent existed anywhere in the codebase (Files' sort is a single fixed-direction enum), so this was a genuine UX-shape choice, not one nearby code already decided. Confirmed approach: two field chips (Name/Count, using the existing `InteractionChip`) plus a separate direction-toggle icon button (`SurfaceIconButton`, `sort-ascending`/`sort-descending` glyphs), rather than 4 combined chips or a tap-again-to-reverse interaction. Placement is inline above the list inside `SavedTagsList` itself (not behind the header filter-popover Files uses, since the Tags view only has two controls total - sort here, search in `2.3.4` - so a popover would add a click for no crowding benefit).
  - Implementation: `model.ts` gained `SavedTagsListSortField`/`SavedTagsListSortDirection`/`SavedTagsListSortState`, `DEFAULT_SAVED_TAGS_LIST_SORT_STATE` (`{ field: 'count', direction: 'desc' }`, reproducing `aggregateRehearsalLibraryTags`'s existing default order), `SAVED_TAGS_LIST_SORT_FIELD_OPTIONS`, `sortSavedTagUsage` (count field respects direction on the count itself but always tie-breaks alphabetically ascending, regardless of direction, matching the `1.1` aggregation convention rather than naively reversing the whole comparator), and `getSavedTagsListSortDirectionToggleLabel` (describes the action the button performs next, e.g. `'Sort ascending'`, matching the `getQueueToggleAction` accessibility-label convention in `playback-session-mode-card.tsx`). `index.tsx` holds local `useState(DEFAULT_SAVED_TAGS_LIST_SORT_STATE)` and a `useMemo(() => sortSavedTagUsage(tagUsage, sortState))`; the sort row is hidden along with the rest of the list content when there are no tags.
  - Tests: `model.spec.ts` covers all four field/direction combinations (asserting the tie-break stays alphabetical-ascending under both count directions), the no-mutation guarantee, and both toggle-label branches.
  - Manually verified in the integrated browser: Count/desc default renders most-used-first-then-alphabetical; switching to Name keeps direction and re-sorts descending by name; toggling direction re-sorts ascending; no console errors introduced (pre-existing Drive/auth 400/401 noise only).
  - Validation: `nx run mobile-rehearsal-player:test-pattern -- --pattern='src/app/library/tags/**/*.spec.ts'`, `nx run mobile-rehearsal-player:typecheck`, and `eslint` on touched files all pass clean.
- [x] 2.3.4 Add a search input scoped to the Tags list, reusing the shared contextual search panel scaffold.
  - First pass gave `SavedTagsList` its own local `ContextualSearchPanel` instance (own magnifier icon, own query state, `canShowRecentSearchTerms={false}`). **User feedback overrode this**: the app already has one top-level search icon in the Library header; Tags should not add a second one, and should support recent searches "like in all other search contexts" instead of opting out of them.
  - Corrected implementation: removed `SavedTagsList`'s own search UI entirely (`ContextualSearchPanel`, its `useState` query/visibility, the `noop`/`es-toolkit` import). The header's existing search icon (`LibrarySearchControlsActions`/`searchPanel.handleSearchActionPress`) and its `ContextualSearchPanel` instance (rendered by `SavedRehearsalLibrarySearchShell` → `LibrarySearchControls`, already positioned above the view content for every view) were already mounted regardless of `selectedView` - they just weren't being read for Tags. `SavedTagsList` now takes a `searchQuery: string | null` prop instead of owning its own; `browse-content.tsx` passes `searchState.activeLibrarySearchQuery` (already available there via the existing `SearchState` Pick type - no new prop threading needed) into it. Filtering (`filterSavedTagUsageByQuery`) and row highlighting (`SearchHighlightedText`) are unchanged, just fed by the shared query instead of a local one.
  - Recent-search support came "for free": the header's `ContextualSearchPanel` is already bound to `searchState.recentLibrarySearchTerms` / `commitLibrarySearchQuery` (the same `LIBRARY_RECENT_SEARCH_HISTORY_KEY`-backed history Files/Tracks/Loops/Playlists already share on this one screen). Since Tags now reads from that same `searchState`, a tag-name search submitted while on the Tags view is recorded into and suggested from that same recent-searches list, matching "all other search contexts" on this screen.
  - Visibility/gating behavior: did not special-case `'tags'` in `shouldRenderSavedLibraryBrowseContent` (`browse-content-model.ts`) - Tags now behaves exactly like Tracks/Loops/Playlists already do (browse content, including the tag list, hides while the header search bar is open with no committed query yet, and reappears once a query is active). This is zero new gating logic, just relying on the existing convention.
  - Fixed stale copy found while wiring this: `getLibrarySearchContextCopy()` (`library/drive/utils/drive-library-metadata.ts`) hard-coded "Search saved library (playlists, tracks, and loops)", which became inaccurate once the same header search bar also covers Tags. Updated to "...tracks, loops, and tags" and updated the matching assertion in `drive-library-view-model.spec.ts`.
  - Tests: `model.spec.ts` still covers `filterSavedTagUsageByQuery` (empty/whitespace query passthrough, case-insensitive substring match, query trimming, no-match empty array) - unaffected by where the query now comes from. Updated `drive-library-view-model.spec.ts`'s search-copy assertion.
  - Manually verified in the integrated browser: opening the one header search icon while on Tags shows updated helper copy mentioning tags; typing narrows the list and highlights matches; closing and reopening the search bar surfaces the typed term under "RECENT SEARCHES" and reselecting it re-filters; no second search icon anywhere on the Tags view. No new console errors (only pre-existing Drive/auth 400/401/COOP noise).
  - Validation: `nx run mobile-rehearsal-player:test-pattern -- --pattern='src/app/library/tags/**/*.spec.ts'`, `nx run mobile-rehearsal-player:test-file -- --file=src/app/library/drive/utils/drive-library-view-model.spec.ts`, `nx run mobile-rehearsal-player:typecheck` (via `tsc -b`), and `eslint` on touched files all pass clean.
- [x] 2.3.5 Wire Tags-list row taps to navigate to the tag detail screen (task `3.1`) with the selected tag.
  - Implementation: threaded a new `onSelectTag: (tag: string) => void` prop through the existing explicit-prop-passing chain from `AppRouter` (which owns `selectedTag`/`setSelectedTag` from task `3.1`) down to `SavedTagsList`: `AppRouter` → `LibraryScreen` (`library-screen-types.ts`) → `SavedRehearsalLibrarySection` (`types.ts`) → `SavedRehearsalLibraryBrowseContent` (`browse-content-types.ts`, added to its `Pick<SavedRehearsalLibrarySectionProps, ...>` list) → `SavedTagsList`, matching the exact existing pattern already used for `onOpenSourceTagEditor`/`onOpenLoopTagEditor`/`onOpenPlaylistTagEditor`. `SavedTagsList` wires `onPress={() => onSelectTag(usage.tag)}` on each `ExplorerListRow`.
  - Found and fixed a second, previously-orphaned call site: `DriveLibrarySavedLibraryPanel` (`library/drive/components/drive-library-section/saved-library-panel.tsx`) also renders `SavedRehearsalLibrarySection` (with its own internal, uncontrolled `selectedView`, so the Tags view is technically reachable there too) but has zero importers anywhere in the app (`DriveLibrarySection`, its wrapping export, is dead/unreachable code, superseded by `DriveDiscoveryPanel`). Satisfied the new required prop there with `onSelectTag={() => undefined}`, matching that file's own existing convention for other not-meaningful-here callbacks (`onDismissLibraryFilesSuccessFeedback`, `onOpenLibraryFilesSuccessFeedbackFolder`, `onShowLibraryFilesSuccessFeedback` all already do this). Did not attempt to remove the dead code itself — out of scope for this subtask.
  - Also flagged (not fixed, out of scope): `saved-rehearsal-library-section/index.tsx` was already 315 lines before this session (pre-dating this change), now 317 after this 2-line addition, over the repo's 300-line guideline. Spawned a follow-up task rather than pulling an unrelated extraction into this slice.
  - Tests: no new automated test — the added logic is a single-line pass-through (`onPress={() => onSelectTag(usage.tag)}`) with no branching, matching the established no-render-test convention for this component family (see task `2.3.2`'s note).
  - Manually verified in the integrated browser: tapping a tag row (e.g. "Soprano") opens the tag detail overlay with `TAG` eyebrow + tag name and the mini player dock/tab bar still visible underneath; tapping back returns to the Tags list in place. No new console errors (only pre-existing Drive/auth 400/401/COOP noise).
  - Validation: `nx run mobile-rehearsal-player:typecheck`, `eslint` on touched files (clean except the pre-existing file-length warning noted above), and `nx run mobile-rehearsal-player:test` (403/403 passing) all pass.
- [x] 2.3.6 Wire the Recents tag module's overflow action (from `2.3`) to navigate to the new Library Tags view.
  - Found while implementing: no overflow entry-point UI existed yet in the Recents tag module (subtasks `2.3.1`-`2.3.5` only built the Library Tags view itself and its row navigation) — this subtask builds the trigger too, not just its wiring.
  - Cross-tab navigation-with-parameter problem, same category task `3.1` solved for tag detail: `LibraryScreen.selectedView` is local state with no external control, so opening Library "already on the Tags view" from Recents (a different shell tab) needed a new signal. Mirrored the existing `requestedDestination`/`requestedDestinationRequestId` pattern already used for tab switching: `AppRouter` gained `requestedLibraryView`/`requestedLibraryViewRequestId` state plus a `requestLibraryView(view)` setter, threaded into `LibraryScreen` as `requestedView`/`requestedViewRequestId` props; `LibraryScreen` reacts via a `useEffect` calling `setSelectedView(requestedView)`, identical in shape to `MobileShell`'s existing destination-sync effect. The Recents overflow action calls both `requestDestination('library')` and `requestLibraryView('tags')`.
  - UI: added a trailing `SurfaceIconButton` (`chevron-right`, reusing the shared icon-button component already used elsewhere, e.g. the Tags-list sort toggle) in the "Popular tags" module header, shown only when `libraryTagUsage.length > RECENTS_SHORTCUT_TAG_CAP`.
  - Tests: no new automated test — the added logic is a boolean threshold (`hasOverflowTags`) already exercised implicitly by the existing `libraryTagUsage`-driven rendering, plus pass-through navigation wiring with no branching of its own.
  - Manually verified in the integrated browser: added 3 extra tags via the tag editor to exceed the cap of 6 (7 total distinct tags), confirmed the overflow chevron appears on Recents, tapping it switches to the Library tab with the Tags view pre-selected showing all 7 tags, then removed the 3 added tags to restore the demo environment to its prior state. No new console errors.
  - Validation: `nx run mobile-rehearsal-player:typecheck`, `eslint` on touched files, and `nx run mobile-rehearsal-player:test` (401/401 passing) all pass.
- [x] 2.3.7 Add focused tests for the Tags view-model: sort orders (name asc/desc, count asc/desc, tie-break) and search filtering.
  - Already satisfied by test coverage added during tasks `2.3.3`/`2.3.4`: `model.spec.ts`'s `sortSavedTagUsage` suite covers count desc (with alphabetical tie-break for equal counts), count asc (tie-break not reversed), name asc, name desc, and a no-mutation guarantee; `filterSavedTagUsageByQuery` suite covers empty/whitespace query passthrough, case-insensitive substring match, query trimming, and no-match. No new code needed — confirmed the 13/13 tests in this file still pass.
  - Validation: `nx run mobile-rehearsal-player:test-pattern -- --pattern='src/app/library/tags/**/*.spec.ts'` (13/13 passing).
- [x] 2.4 Wire tag chip taps to navigate to the new tag detail screen (task `3.1`) with the selected tag, instead of the current no-op shortcut handler.
  - Decision (confirmed with user): the pre-existing chip only had one tap target — a nested "play" icon (`CompactPlaybackAction`) disabled unless a recent-playback item existed to resume — while the `InteractionChip` itself wasn't pressable. Since tapping a tag now opens tag detail (a browse action) rather than playing anything, and that's independent of whether recent playback exists, made the whole chip pressable via `InteractionChip`'s own `onPress` and removed the nested play icon entirely, rather than repurposing it. This also retired `getRecentsShortcutPlayActionCopy` (its accessibility copy/disabled-state was written specifically for a "play" action) and its 2 tests.
  - Implementation: `RecentsScreenProps.onPlayRecentShortcut` renamed to `onSelectRecentShortcutTag` (no longer play-shaped) and threaded straight through from `AppRouter`'s `onSelectTag={(tag) => setSelectedTag(tag)}` (task `3.1`'s state) via `AppRouterRecentsScreen`'s new `onSelectTag` prop, replacing the previous no-op "resume most-recent item" handler.
  - Tests: no new automated test — the chip wiring is a single-line pass-through (`onPress={() => onSelectRecentShortcutTag(tag)}`) with no branching; removed the now-dead `getRecentsShortcutPlayActionCopy` tests instead of adding new ones.
  - Manually verified in the integrated browser: from the Recents tab (a different shell tab than Library), tapping the "Soprano" chip opens the tag detail overlay with the correct tag name; back returns to Recents with the chip row unchanged. No new console errors.
  - Validation: `nx run mobile-rehearsal-player:typecheck`, `eslint` on touched files, and `nx run mobile-rehearsal-player:test` (401/401 passing, 2 fewer than before from the retired copy tests) all pass.
- [x] 2.5 Update the existing Recents `screen-copy`/`overflow-actions` focused tests for the new tag-driven module, and add coverage for the empty-state and overflow-entry-point branches.
  - `overflow-actions.ts`/`overflow-actions.spec.ts` (the per-recent-row "Play next/Add to queue/View in library" menu) is unrelated to the tag module and untouched by this change — confirmed its 3 existing tests still pass unmodified, no update needed.
  - `screen-copy.spec.ts`'s stale `getRecentsShortcutPlayActionCopy` tests were already removed in task `2.4` when that copy helper was retired.
  - The empty-state and overflow-entry-point branches were previously inline JSX booleans in `index.tsx` (`!hasSavedTagUsage || !isRecentPlaybackAvailable` and `libraryTagUsage.length > RECENTS_SHORTCUT_TAG_CAP`) with no testable seam. Extracted both into a new pure `getRecentsTagModuleVisibility` in `screen-copy.ts` (returning `{ showGuidanceBody, showOverflowTrigger }`), alongside the exported `RECENTS_SHORTCUT_TAG_CAP` constant (moved out of `index.tsx`), matching this codebase's existing `shouldRenderX`-predicate convention (e.g. `shouldRenderSavedTagsList` in `browse-content-model.ts`). `index.tsx` now derives `tagModuleVisibility` from this function instead of inline conditions.
  - Tests: 5 new cases in `screen-copy.spec.ts` — empty-state guidance shown with no tags, guidance still shown with tags but no recent playback yet, guidance hidden once both tags and recent playback exist, overflow trigger hidden at exactly the cap (6), overflow trigger shown one past the cap (7).
  - Manually verified in the integrated browser: with tag usage exceeding the cap, the overflow chevron renders and the guidance body correctly hides once both gating conditions are satisfied — same behavior as before the refactor, confirming it's a pure extraction with no behavior change.
  - Validation: `nx run mobile-rehearsal-player:typecheck`, `eslint` on touched files, and `nx run mobile-rehearsal-player:test` (406/406 passing) all pass.

## 3. Tag Detail Screen

- [x] 3.1 Add a new tag detail router destination/screen shell, using the shared compact destination-header primitive for the tag name and back action.
  - Decision (confirmed with user): the tag detail screen must be reachable from both Recents (task 2.4) and the new Library Tags view (task 2.3.5), which live on different top-level shell tabs. The existing `requestedDestination`/`activeDestination` mechanism (`app-router.tsx`/`mobile-shell/index.tsx`) only switches between the 3 fixed tab panels with no per-screen parameter, and playlist/track-loop "detail" screens today use `LibraryScreen`-local state (`SavedRehearsalLibraryDetailMode`), which isn't reachable from Recents. Instead of extending `ShellDestinationKey` with a payload, followed the existing cross-tab-overlay precedent already in this file: `MobileShellPlaybackSurface` (Now Playing/Queue) is opened/closed independent of the active tab. New `selectedTag: string | null` state lives in `AppRouter` (which already owns `libraryController`/`playback`/`libraryTagUsage`, all of which the screen's content will need in tasks 3.2+); `AppRouter` builds a `tagDetailScreen` ReactNode and passes it into `MobileShell` as a new prop. `MobileShell` renders it as a 4th panel inside the existing `contentViewport` (reusing the `destinationPanel`/`destinationPanelActive` show/hide styles), hiding the 3 tab panels while it's shown, so the mini player dock, tab bar, and Now Playing/Queue overlay are all unaffected and stay reachable.
  - New component: `TagDetailScreen` (`packages/mobile-rehearsal-player/src/app/library/tags/components/tag-detail-screen/index.tsx`), taking `{ tag, onClose }`, rendering `ExplorerNavigationBar` (the shared compact header/back-action primitive already used by Files browsing and the Drive discovery panel) with `eyebrow="Tag"`, `title={tag}`, `onGoBack={onClose}`, plus an empty flex-1 body placeholder for task 3.2's match list.
  - Out of scope for this slice (left for tasks `2.3.5`/`2.4`): no caller sets `selectedTag` yet — `onSelectTag`-style wiring from the Tags-list row tap and the Recents chip tap is exactly what those two subtasks add. The overlay mechanism itself (state → prop → conditional render) is therefore not yet manually triggerable; verified instead by confirming the Library/Tags screens still render correctly with `tagDetailScreen` always `null` (no regression) via the browser preview, and that the new prop threading typechecks/lints clean.
  - Tests: no new automated test — `TagDetailScreen` is pure JSX composition around an already-tested shared primitive with zero branching logic, and `app-router.tsx`/`mobile-shell/index.tsx` have no existing spec files themselves (matching this repo's convention of testing colocated pure logic/view-model files, not top-level wiring components — same precedent noted in task `2.3.2`).
  - Validation: `nx run mobile-rehearsal-player:typecheck`, `eslint` on touched files, and `nx run mobile-rehearsal-player:test` (403/403 passing) all pass clean. Manually verified in the integrated browser that Library (including the Tags view added in `2.3.1`-`2.3.4`) still renders correctly with no new console errors (only pre-existing Drive/auth 400/401/COOP noise).
- [ ] 3.2 Resolve and render the tag's direct matches (tracks, loops, playlists, folders) using the existing native row shells for each entity type.
- [ ] 3.3 Add a type filter chip row (Tracks / Loops / Playlists / Folders) scoped to this tag's matches, using the existing chip family.
- [ ] 3.4 Add a search input scoped to this tag's matches only, using the shared contextual search panel scaffold.
- [ ] 3.5 Wire folder and playlist match rows to open Files-at-that-folder and playlist detail respectively when tapped outside the play action, per the spec's non-play navigation requirement.
- [ ] 3.6 Add focused view-model tests for match resolution, type filtering, and search filtering on the tag detail screen.

## 4. Tag Queue Expansion and Playback

- [ ] 4.1 Implement folder-tag expansion: recursively resolve every track/loop reachable from a tagged folder, including nested subfolders, reusing the Files explorer's existing folder-listing resolution instead of a new folder-walk.
- [ ] 4.2 Implement playlist-tag expansion: resolve a tagged playlist's contained tracks/loops in playlist order, reusing the same item-resolution path `buildPlaylistPlaybackSession` already uses for normal playlist playback.
- [ ] 4.3 De-duplicate the final resolved playable set so a track/loop reachable through more than one matched path (e.g. tagged directly and via a tagged folder) queues exactly once; pin the dedupe key (source id for tracks, loop id for loops) called out as an open question in design.md.
- [ ] 4.4 Add a multi-item transient session constructor (e.g. `createTransientPlaybackSessionFromItems`) alongside `createTransientPlaybackSession` in `playlist-playback-queue-state.ts`, seeding an ordered `PlayableItem[]` queue instead of a single item.
- [ ] 4.5 Wire the tag detail play action to expand the currently-filtered (not full unfiltered) match list from `3.2`–`3.4` and start playback via the new multi-item transient session, matching the filter-then-play scenario in the spec.
- [ ] 4.6 Add focused tests for folder expansion, playlist expansion, cross-path dedupe, and the new multi-item session constructor.

## 5. Final Validation

- [ ] 5.1 Manually verify the full flow in the integrated browser: Recents tag list reflects real tags (including empty-state and overflow-entry-point branches) → tag detail lists correct matches → type filter and search narrow correctly → playing filtered vs. unfiltered queues the right set → Up Next reflects that queue with working transport/repeat/reorder → folder/playlist row taps navigate correctly outside of play.
- [ ] 5.2 Run project typecheck and lint on touched files, and the narrowest relevant Nx test targets (`audio-library-runtime`, `mobile-rehearsal-player`), before considering this change ready to archive.
