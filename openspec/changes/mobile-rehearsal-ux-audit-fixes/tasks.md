## 1. Phase 0 — Bugs (tag persistence, FAB overlap, stale banners)

- [x] 1.1 Add stateful test coverage for `saveTagEdits` in `use-saved-rehearsal-library-tag-editor.ts` (currently only the pure `resolveTagEditorTagsAndTitle` helper is tested) asserting that calling it persists tags and that a subsequent read reflects them.
- [x] 1.2 Add stateful test coverage for `saveSource`/`persistSource` in `use-saved-rehearsal-library.ts` (currently only two pure exported helpers are tested) asserting the hook actually calls the repository and updates React state with the new tags.
- [x] 1.3 Use the now-failing test(s) from 1.1/1.2 to pin the exact defect in the tag-save write path and fix it, for tracks, loops, and playlists alike. (Findings: 1.1/1.2's tests did not fail — the write/state/merge layer they cover is correct, confirmed by a new composed integration test. The actual defect is in Files-view row wiring: `onOpenSourceTagEditor` passed the Files explorer tree's stale cached row object straight to the tag editor instead of resolving the entity fresh by id from the reactive `savedLibrarySources`, unlike the adjacent loop handler which already did this. Playlists were already safe — `openPlaylistTagEditor` resolves fresh by id internally. Fixed by extracting `resolveFreshEntityForTagEditor` and using it for both the source and loop lookups in `browse-content.tsx`. Folder tag staleness in Files view remains — it is owned by task 1.4's `canonicalIdsKey` fix, since folders have no independent canonical list to look up against.)
- [x] 1.4 Fix the adjacent staleness bug in `use-library-files.ts`: `canonicalIdsKey` is built only from entity IDs, so the Files-tree `refresh()` effect never re-fires on a tags-only change. Add a regression test. (`buildCanonicalIdsKey` now folds each entity's tags into its key segment. Live-browser check found no currently-observable symptom from this gap — Files rows already build from the fresh `savedSources`/`savedLoops`/`savedPlaylists` lists rather than the tree's own copies, and `saveFolderTags` already does an optimistic `setTree` update — but the key was still tag-blind by construction, which is fragile against future changes to that row-building path. Fixed as pure-hygiene coverage per the task text.)
- [x] 1.5 Manually verify: tag a track, loop, and playlist; reopen each tag editor and confirm saved tags show; confirm Library > Tags and Recents > Popular tags reflect the new tag without navigating away and back. (Verified live in the signed-in browser session: tagged a track, loop, and playlist via Files view; each reopened tag editor showed the saved tag; Library > Tags showed all three; Recents > Popular tags showed the loop/playlist tags immediately after a single tab switch, no restart needed. Also verified a folder tag (adjacent to 1.4) and confirmed all tags survive a full page reload. All test data removed afterward.)
- [x] 1.6 Fix the Files-view (and other Library views') floating `+` create button overlapping the last visible row's overflow trigger — reserve bottom scroll padding for the control's footprint and verify hit-testing is unambiguous at the boundary. (Investigated, no code change: `screens/library/index.tsx` already reserves 188px of bottom scroll padding whenever the FAB is visible — predates this change, confirmed via `git log`. The FAB has no `hitSlop`/expanded touch region beyond its own bounds. Tested live in the browser at desktop size and a short 390x550 viewport, with genuinely short lists (2-item Playlists, near-empty Files folders) — no overlap reproduced in any condition. User confirmed marking done without a speculative fix.)
- [x] 1.7 Add/verify a test or manual check that a short folder listing's last row overflow menu opens reliably regardless of exact tap position near the FAB. (Manually verified live in the browser across this session's testing: the Files root list's last row ("Warm-Ups") sits well clear of the FAB with the existing 188px bottom padding, and its overflow trigger opened correctly every time it was tapped — including immediately adjacent to the FAB in a short 2-item Playlists view and a short 390x550 viewport. No ambiguous-tap behavior observed.)
- [x] 1.8 Scope success-acknowledgment banner state (starting with "Loop saved") to the action/entity that produced it instead of a bare boolean, so navigating away and back does not resurface a stale banner, and an explicitly dismissed banner stays dismissed. (Built a generic, reusable `useScopedSuccessAcknowledgment` hook in `src/app/components/scoped-success-acknowledgment/` — stamps a monotonic token per `show()`, clears on `isActive` going false so it never resurfaces after navigating away and back, and supports an accessibility-aware auto-dismiss: skipped entirely when a screen reader is active, paused via onFocus/onBlur while the banner has focus, otherwise fires after 5s. `MobileShell` now passes `libraryScreen` as a render prop `(isActive) => ReactNode` so `LibraryScreen` knows when it stops being the active tab; combined with `selectedView === 'files'` this covers both the cross-tab and internal-view-switch staleness cases. Verified live in the browser: saved a loop, confirmed the banner appears, cleared via Library→Recents→Library switch, and separately via Files→Tracks→Files switch; both correctly clear the stale banner without needing a dismiss.)

## 2. Phase 1 — Hierarchy & discoverability

- [x] 2.1 Replace the "Repeat off" icon in the Now Playing and Up Next/Queue repeat control with a dedicated repeat-family glyph (dimmed/inactive treatment) so it no longer reuses the shuffle crossing-arrows glyph; keep "Repeat one"/"Repeat all" as loop-icon variants. (First attempt swapped the 3-segment control's "off" glyph from `repeat-off` to `repeat-variant` — fixed the shuffle collision, but the user caught a new problem live: `repeat-variant` and `repeat` (used for "all") read as the same loop shape at a glance, so "off" vs "all" became indistinguishable. MaterialCommunityIcons' repeat family only has 4 members (`repeat`, `repeat-off`, `repeat-once`, `repeat-variant`) and no combination of 3 gives full pairwise distinctness, so pure glyph-swapping within the existing 3-segment control couldn't fully solve it. Per user direction ("most apps use a single cycling button," referencing Spotify/YouTube Music screenshots), redesigned the control as a single cycling toggle button, matching the existing shuffle `ModeButton` pattern already in the file: tapping cycles through the visible repeat modes (off→one→all→off, or off→one→off when standalone); off/all share the plain `repeat` glyph and are told apart by the button's existing selected/unselected styling (filled green vs outline), while "one" keeps its own dedicated `repeat-once` glyph. This reading is also a closer fit to the existing `mobile-rehearsal-player-ui` spec wording — "each control's active/selected state is conveyed through styling ... on that control's own dedicated glyph" — than 3 simultaneously-visible segments ever were. Replaced `REPEAT_MODE_OPTIONS`/`RepeatModeSegmentedControl` with `resolveRepeatToggleModel` in `playback-session-mode-options.ts` (pure function: current mode + visible modes → icon/selected/nextMode/accessibility label+hint), removed the now-dead segmented-control styles from `playback-session-mode-card.tsx`, and rewrote `playback-session-mode-options.spec.ts` to cover the icon mapping, off/all sharing one glyph without colliding with shuffle, both cycle orders, and per-state accessibility labels. Now Playing and Up Next/Queue share one `PlaybackSessionModeCard` implementation, so the fix applies to both automatically; verified live in the browser on the standalone player and a queued playlist.)
- [x] 2.2 Add a shuffle-start action to playlist detail alongside the existing ordered-start action, as an icon-first control row per the existing `mobile-rehearsal-player-usability` requirement. Implemented together with 2.3 (design.md called these out as one fix). Added `playShufflePlaylist` to `use-saved-playlist-detail-actions.ts` (mirrors `playOrderedPlaylist`, `togglePlaylistPlayback`'s `mode` already supported `'shuffle'` — no new domain logic needed). Added a pure `getPlaylistDetailModeActions` view-model function (`saved-playlist-detail-mode-actions.ts`, a new file rather than appending to the already-307-line `saved-playlist-playback-view-model.ts`) returning `{icon, label, selected, disabled, accessibilityLabel}` per mode — deliberately does NOT reuse `getPlaylistPlaybackActionCopy`'s `label` text ("Play ordered"/"Shuffle play") as visible copy, since the spec explicitly forbids rendering that text in this row; reuses `getPlaylistQueueModeLabel` ("Ordered"/"Shuffle") as the adjacent visible label instead. Widened the shared `CompactPlaybackAction` component's icon union to add `'shuffle'` and built a new `PlaylistDetailModeRow` component reusing it (icon + adjacent label per action, selected state shown via fill color using the existing `PLAYLIST_PRIMARY_ACTION_BACKGROUND`/`_TEXT` tokens already used elsewhere in this feature). Wired into `SavedPlaylistDetailCard` via `SavedLibraryDetailCardShell`'s existing `playbackControls` slot.
- [x] 2.3 Move the playlist-wide ordered/shuffle play controls out of the shared top app-bar (where ordered-play currently occupies the Filters icon slot) into the control row inside playlist detail from 2.2. Discovered the header's "Filters icon slot" (`headerPlaybackAction` in `search-shell.tsx`) is shared infrastructure also used by Tag detail's own header play action (`use-tag-detail-header-playback.ts`) — removed only playlist detail's contribution to it (deleted the now-dead `use-playlist-detail-header-playback.ts` hook and its call/prop threading in `saved-playlist-section.tsx` and `detail-sections.tsx`'s playlist-section-content branch), leaving Tag detail's use of the same shared header slot untouched. Verified live in the browser: playlist detail's header no longer shows a play icon (just search/profile), the new row's Shuffle button starts shuffle playback and shows selected (filled) styling with the bold "Shuffle" label, and the full 556-test suite plus typecheck pass, confirming Tag detail's unrelated wiring wasn't broken.
- [x] 2.4 Find the shared Library-shell/view-switcher layout rule that reserves full remaining viewport height for the active view's content (e.g. a `flex: 1` or fixed-height wrapper) and fix it to size to actual content instead, so short lists stop padding themselves with empty space and longer lists show more rows before scrolling. Fix once at the shared layout level rather than patching each of Files/Tracks/Loops/Playlists/Tags independently; do not substitute filler content (e.g. contextual guidance) for the fix. (Investigated, no code change: the rule is `scrollView: { flex: 1 }` in `screens/library/index.tsx` (mirrored one level up by `contentViewport`/`destinationPanel` in `mobile-shell-styles.ts`). Live-patched it via devtools to shrink-to-content and screenshotted the result: zero visible change, because the floating `+` (`filesCreateDock`, `position: absolute`) and the tab bar/mini-player dock are positioned independently of the ScrollView — they're anchored to the full-height `screen`/`contentViewport`, not to where the content happens to end. Per user direction, kept the FAB/tab-bar pinned to the device bottom (standard convention, matches Spotify/Apple Music) rather than letting them ride up with short content, and searched instead for other wasteful *fixed* spacing that could be trimmed independent of that: swept every style file in the Library rendering chain (explorer, search-shell, status-cards, tags list, breadcrumb bar, row/card styles) for `flex: 1`/`minHeight` — found none causing unnecessary vertical reservation; every card/row already sizes to its own content. Measured the header (78px, tight — title/subtitle/padding all visible) and footer/tab-bar (66px, only 6px margin to the true screen edge) live in the browser — both tight, no excess. The one sizable reservation, the 188px FAB-clearance bottom padding, is necessary and was already deliberately sized and verified correct earlier in this same change (tasks 1.6/1.7); reducing it risks reintroducing the overlap bug those tasks fixed. Conclusion: given the constraint to keep the FAB/tab-bar pinned, the visible "gap" over short content is the ordinary, unavoidable consequence of that pinning, not a fixable reservation bug — no further code change is warranted. User confirmed closing without a speculative fix.)

## 3. Phase 2 — Refinement

- [x] 3.1 Add a persistent, explicitly-dismissible success card for Add's track-save action, matching the existing loop-save acknowledgment pattern; reuse the scoping fix from 1.8 so it doesn't go stale. (`AddScreen` had no `isActive` signal at all (unlike `LibraryScreen`), so gave `MobileShell`'s `addScreen` prop the same `(isActive: boolean) => ReactNode` render-prop treatment as `libraryScreen`, threaded through `app-router.tsx`. Rather than plumbing a save-success callback through the shared `useRehearsalLibraryController` (instantiated once, shared by both screens), reused the same reactive `savedLibrarySources` list the row's existing Save->management-icon swap already depends on: a new `resolveTrackSaveDetection` pure function (`drive-track-saved-feedback.ts`) diffs a baseline-ids ref against the current list to detect a just-completed save, entirely local to `AddScreen`. Live-browser testing caught a real bug in the first version: `savedLibrarySources` starts empty and populates asynchronously, so establishing the baseline on the very first effect run captured that empty pre-load state, making every pre-existing saved track look newly-saved the moment data loaded — reproduced deterministically on every fresh page load. Fixed by gating baseline establishment on `savedLibrary.isLoading` going false first; added a regression test for this exact sequence. New `DriveTrackSavedFeedbackCard` mirrors `LibraryFilesSuccessFeedbackCard`'s structure (built on the shared `FeedbackCard`) minus the folder-navigation action, which doesn't apply here. Extracted `DriveDiscoveryActionButton` into its own file to keep `screens/add/index.tsx` under the 300-line guideline. Verified live end-to-end: save shows the card naming the track, Dismiss clears it, switching to Library and back to Add does not resurface it, and Remove correctly reverses the save — all with zero regressions across the 564-test suite.)
- [x] 3.2 Implement the tag editor's suggestion row per the existing `recents-tag-navigation` requirement: popular tags when the input is empty, matching existing tags while typing, comma-aware scoping for multi-tag input, excluding tags already applied to the entity. (Investigated, no code change: this was already fully implemented — `tag-editor-sheet/tag-suggestion-row.tsx`, `tag-editor-sheet/model.ts`'s `resolveTagEditorSuggestions`/`getActiveTagEditorInputSegment`/`removeTagEditorInputActiveSegment`, wired into `tag-editor-sheet/index.tsx`, reusing the same `aggregateRehearsalLibraryTags` aggregation the Tags view and Recents' Popular tags module use — all committed via `e870834`/`7702e86`/`f61dcbe`, merged into `main` before this OpenSpec change began (predates this change's own `488c245` starting commit), with existing passing unit tests in `model.spec.ts`. The original audit finding that this "never renders" no longer reflects the current branch. Verified live in the browser: tagged a track with "alto", opened the tag editor for a different, untagged track — "SUGGESTED TAGS: alto" appeared; typing "al" kept it narrowed correctly; tapping the chip committed it as a chip and cleared the input; the suggestion then correctly disappeared since it's now already-applied. All scenarios in the spec requirement confirmed working as-is.)
- [x] 3.3 Converge the playlist-detail and Up Next/queue drag-handle icon and edge placement on one consistent treatment. (Extracted a new shared `DragHandle` component (`src/app/components/drag-handle.tsx`) used by both surfaces, rather than just visually matching duplicated code — guarantees genuine consistency, not just similar-looking duplication. Icon converged on `drag-vertical` (was already used by playlist detail; queue previously used the different `drag` glyph) since both surfaces reorder vertical lists. Edge: converged on trailing (queue's existing side — immediately before the overflow trigger) rather than moving to leading, since that only required restructuring playlist-detail's own row (a self-contained file) instead of touching the shared `CompactPlayableRowShell` queue relies on, which could have had a wider blast radius. Also unified the visual treatment (46×46 circular bordered chip, `appTheme.colors.secondaryText`/`border` tokens — confirmed identical to the playlist-feature-local color constants they replace) and bumped playlist detail's touch target from 20×40 to 46×46, meeting the 44pt accessibility minimum it previously missed. Removed the now-dead `playlistRowDragHandle(Disabled)` and `queueRowDragHandle(Disabled)` styles. No automated test added for the shared component itself: it has no extractable pure logic beyond a one-line conditional (matches this repo's established convention of not writing rendered-component tests, confirmed by the absence of such tests for either row previously). Typecheck, full 564-test suite, lint, and format all clean. Live-browser verification was attempted but blocked partway through by unrelated session-level browser-automation flakiness (clicks silently not registering across the whole app, not specific to these files) after successfully confirming no regressions via static analysis and a careful line-by-line review of both call sites' final JSX.)
- [ ] 3.4 Add a scroll-affordance hint (e.g. fade edge) to the horizontally-scrolling Library view-switcher pill row.

## 4. Phase 3 — Native verification

- [ ] 4.1 Check whether the native iOS build's Now Playing volume control shares the same custom slider component observed on web, or already uses a native volume view.
- [ ] 4.2 If the native build shares the web fallback, swap it to `MPVolumeView` (or the RN equivalent) per `apple-hig-ios`; if it already differs, close this task with no code change and note the finding.

## 5. Validation

- [ ] 5.1 Run `npm exec -- nx run mobile-rehearsal-player:test` and `npm exec -- nx run mobile-rehearsal-player:typecheck` after each phase.
- [ ] 5.2 Manual regression pass covering the flows exercised in the original audit: search + save from Drive, loop creation, playlist creation/playback (ordered and shuffle), folder creation, tagging, queue/Up Next, waveform scrub/jump, Recents.
