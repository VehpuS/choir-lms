## 1. Complete Drive Pagination and Traversal

- [x] 1.1 Add a shared abort-aware Drive files paginator that follows `nextPageToken`, deduplicates items by Drive id, fails rather than returning a silently partial set, and cover first-page, multi-page, duplicate, abort, and later-page-failure behavior with focused `google-drive` tests. (Added the dependency-injected `paginateDriveFiles` package API in its own focused module rather than expanding the already oversized discovery file. It preserves first-seen Drive order, forwards and checks the caller's abort signal before each page, rejects page-request failures without returning accumulated partial data, and guards against repeated page tokens. Added a colocated five-case spec and expanded the Google Drive Nx test target to include every `src/lib/*.spec.ts`; 22/22 tests, forced/uncached typecheck, lint with zero errors, formatting, clean install, and strict OpenSpec validation pass.)
- [x] 1.2 Route Drive browse and unscoped audio search through the paginator, preserving current sorting and availability partitioning, and add regression tests proving results beyond item 100 are returned. (Threaded `pageToken` through the existing Drive files request/fallback path and added a shared discovery adapter that returns the paginator's complete deduplicated file set to the unchanged browse/search sorting and availability partitioning logic. Added focused public-API regressions with 100 first-page audio items plus page-two folders and supported/unsupported audio; 24/24 Google Drive tests, uncached typecheck and lint, formatting, whitespace checks, and touched-file Problems all pass.)
- [x] 1.3 Route descendant-folder discovery and folder-scoped audio search through complete pagination, retaining bounded Drive query batches, fallback-to-direct-folder behavior only for descendant discovery failure, and focused deep-tree/multi-page tests. (Replaced each single-page descendant/search batch request with the shared all-pages Drive adapter while preserving the existing 20-parent query batching and descendant-only catch boundary. Added public-API regressions proving later-page folders participate in deeper traversal, later-page folder-scoped audio is returned, generated queries remain bounded, and later search-page failures reject rather than falling back; 26/26 Google Drive tests, uncached typecheck and lint, formatting, whitespace checks, file-length checks, and touched-file Problems all pass.)
- [x] 1.4 Add a cancellable recursive folder-content enumerator that returns accessible folders plus supported and unsupported audio metadata across all pages, caches visited folder ids, guards against cycles, and has focused deep-tree, shared-folder, unsupported-file, abort, and partial-failure tests. (Added the standalone public `enumerateDriveFolderContents` API, which breadth-first lists each folder through the shared all-pages request adapter, records parent folder ids for later structure-preserving planning, deduplicates audio by Drive identity, and uses a visited-folder cache to avoid duplicate requests and cycles. Reused existing folder/audio mapping and availability partitioning, preserved shared-root semantics, and rejected incomplete traversal; focused tests cover paginated deep traversal, shared folders, unsupported files, cancellation, cycles, and descendant failures. All 31 Google Drive tests, uncached typecheck and lint, formatting, whitespace checks, file-length checks, and touched-file Problems pass.)

## 2. Mixed Folder and Audio Search

- [x] 2.1 Introduce discriminated folder/audio discovery result types and update Drive search queries/parsing to return matching folders plus supported audio in the active root or folder subtree, with package tests for type partitioning and root/subtree exclusion. (Added exported `folder`/`audio` discovery result variants and a sorted mixed `results` collection while retaining legacy source partitions until the 2.3 mobile consumer migration. Search queries now request folders or audio across unscoped, My Drive, shared-root, and bounded descendant-folder scopes; mixed parsing excludes unsupported audio while preserving it in the transitional unavailable partition. Added a focused mixed-search spec and retained the existing root/subtree query regressions. All 33 Google Drive tests and 569 mobile tests pass; Google Drive and mobile app typechecks pass; both project lint targets report zero errors; formatting, strict OpenSpec validation, file-length checks, and touched-file Problems pass. Mobile test typechecking retains only two pre-existing `AbortSignal.throwIfAborted` compatibility errors in prior pagination files.)
- [x] 2.2 Add a bounded-concurrency, operation-cached Drive path resolver using `parents` metadata that returns root kind and the deepest accessible folder path, with tests for My Drive, shared folders, inaccessible ancestors, repeated ancestors, and deterministic handling of multiple parents. (Added the public `resolveDriveFilePaths` batch API with a four-worker default, deterministic input-order output, per-operation promise-cached ancestor metadata, first-accessible-parent traversal, deepest-accessible partial paths, and cycle protection. Drive list and metadata requests now include `parents`. Added five focused tests for My Drive paths and requested fields, partial shared ancestry, bounded concurrency plus repeated-ancestor caching, multiple-parent fallback order, and cycles. All 38 Google Drive tests, uncached typecheck, lint with zero errors, formatting, strict OpenSpec validation, whitespace checks, file-length checks, and touched-file Problems pass; lint retains one pre-existing max-lines warning in untouched `google-drive.spec.ts`.)
- [x] 2.3 Attach resolved path metadata to browse and search results and update `useDriveLibrary` search state to consume mixed results without changing browse navigation, with focused model/hook tests for path labels, scope changes, and stale-request cancellation. (Resolved each complete browse/search file set inside the Google Drive package with the existing bounded, operation-cached resolver and attached structured root/path metadata plus readable root-to-parent labels to browse audio and mixed search results while preserving browse folder navigation objects. Exposed mixed results through `useDriveLibrary` and the rehearsal-library controller, made root changes deactivate search, and added a request lifecycle helper so disposed requests abort and cannot apply stale results. Added public package integration tests for My Drive browse paths, shared mixed-result paths, and cached ancestry, plus focused mobile tests for mixed-result clearing on scope deactivation and stale-request disposal. All 40 Google Drive tests and 571 mobile tests pass; both projects' production typechecks and lint pass with zero errors; formatting, strict OpenSpec validation, whitespace checks, file-length checks, and touched-file Problems pass. Mobile test typechecking retains only the two pre-existing `AbortSignal.throwIfAborted` compatibility errors in untouched pagination files, and lint warnings remain only in untouched files.)

## 3. Saved Source Provenance

- [x] 3.1 Add optional structured Drive source-location provenance to `DriveAudioSource`, update runtime parsing/fixtures without requiring authorization-time backfill, and test that legacy persisted sources still load unchanged. (Defined exported Drive root/path/location types in a focused model module and added optional `sourceLocation` to `DriveAudioSource`. Runtime source normalization now retains valid stored provenance, omits malformed optional provenance, and leaves legacy records without provenance unchanged without Drive authorization or backfill. Added focused model and storage specs, expanded the model test target to discover all colocated specs, and extracted adjacent source-availability types to keep the touched model within configured lint limits. A clean `npm ci`, all 19 model tests, all 52 runtime tests, both production typechecks, both lint targets with zero errors, formatting, strict OpenSpec validation, whitespace checks, marker cleanup, and touched-file Problems pass; lint retains two pre-existing max-lines warnings in untouched runtime specs.)
- [x] 3.2 Persist provenance when saving a newly discovered source and refresh Drive-owned metadata/provenance when resaving an existing source while preserving tags, tag timestamps, `createdAt`, and Library links; add repository and controller regression tests. (Converted path-aware discovered sources into persistent `DriveAudioSource` values at the saved-library boundary, deriving structured provenance from the deepest accessible folder and dropping transient path labels. Repository upserts now refresh incoming Drive-owned fields while omitted tags preserve prior app-owned tags/timestamps, explicit tag arrays remain editable, `createdAt` remains stable, and an input without a resolved folder retains last-known provenance. Added focused repository coverage for metadata/provenance refresh plus tag, creation-time, and file-link preservation, and composed controller coverage proving rediscovery persists current provenance without losing tag edits. All 53 runtime tests and 572 mobile tests pass; both production typechecks and lint targets complete with zero errors; formatting, strict OpenSpec validation, whitespace, file-length, and touched-file Problems checks pass. Eight pre-existing max-lines warnings remain outside the touched slice.)
- [x] 3.3 Add pure view models for readable original-path presentation and original-location action availability, including legacy and deepest-accessible-path states, with focused tests before wiring UI actions. (Plan: add a feature-owned pure saved-track original-location model that formats stored provenance with the established `My Drive`/`Shared with you` root labels, exposes separate `Show in Add` and `Open in Google Drive` availability flags, and returns explicit unavailable presentation for legacy sources without provenance. Cover full My Drive ancestry, deepest-accessible shared ancestry, and legacy states in a colocated unit spec before any UI wiring. Validate with the focused mobile test, full mobile tests, production typecheck, lint, formatting, file-length, strict OpenSpec, whitespace, and touched-file Problems checks.)

## 4. Recursive Import Planning

- [x] 4.1 Add a dependency-light import-selection normalizer that collapses nested selected folders, removes separately selected descendant tracks, deduplicates by Drive file id, and reports overlap counts; cover mixed and deeply overlapping selections with focused tests. (Added a feature-owned pure normalizer beside the saved-library boundary that accepts path-aware mixed Drive discovery results, preserves first-selected order, retains only highest selected folder roots, removes audio covered by those roots, and deduplicates repeated Drive identities while reporting nested-folder, covered-audio, duplicate-selection, and total overlap counts. Added three focused tests for independent mixed selections, deeply overlapping folders and audio, and repeated folder/audio ids. The focused 3/3 tests, uncached full mobile tests, production/config typechecks, lint with zero errors, formatting, strict OpenSpec validation, whitespace checks, file-length checks, and touched-file Problems all pass.)
- [x] 4.2 Add a pure import planner for `Flatten` and `Preserve structure` that maps recursively enumerated Drive contents into the chosen Library destination, creates the selected folder as each preserved import root, excludes unsupported files with summary counts, and test both modes across multiple selected roots. (Added a pure planner beside the 4.1 normalizer that accepts normalized selection, complete enumerations keyed by selected root, destination id, and import mode. It emits destination-relative folder and track-link intents without persistence ids: individual tracks always target the Library destination; `Flatten` emits no folders and targets all descendant audio there; `Preserve structure` emits each selected root under the destination, topologically orders descendants parent before child, and targets audio to reproduced parents. Playable and unsupported sources deduplicate by Drive identity, unsupported items remain available with summary counts, and missing or incomplete folder enumeration fails rather than producing a partial plan. Added three focused tests covering multiple roots, both modes, individual and nonmatching descendant audio, deep out-of-order hierarchy, unsupported files, cross-root duplicates, and missing enumeration; extracted reusable fixtures through the checkpointed move workflow to keep every file below 300 lines. The focused 3/3 tests, uncached full mobile tests, production/config typechecks, lint with zero errors, formatting, strict OpenSpec validation, whitespace checks, file-length and marker checks, and touched-file Problems all pass.)
- [x] 4.3 Extend the planner to classify new, reusable, and already-linked sources by Drive identity and allocate deterministic keep-both folder/link names in Drive path/id order; test case-insensitive conflicts and replanning against partially imported state. (Extended the planner with current Library state and a focused classification module that orders folders and tracks by Drive path/id, scopes deterministic folder ids to their resolved Library parent, and emits stable file-link ids. Existing canonical sources are classified as reusable, target links as already present, and unseen Drive identities as new; partial replanning reuses deterministic folders and links. Extracted a shared runtime available-node-name allocator so planner reservations and existing copy behavior share the case-insensitive `Copy`/`Copy N` convention, and extracted pure ordering helpers to keep implementation files below 300 lines. Added focused runtime and planner coverage for allocator sequences, case-insensitive folder/link conflicts, path/id ordering, canonical reuse, already-present links, and partial-state replanning. The focused 5/5 planner tests, 56/56 runtime tests, uncached full runtime/mobile tests, production/config typechecks, lint, formatting, strict OpenSpec validation, whitespace, file-length/marker checks, and touched-file Problems all pass.)

## 5. Recoverable Batch Execution

- [x] 5.1 Define typed import progress phases and itemized outcomes for created, reused, already-present, unsupported, overlap-collapsed, cancelled, and failed work, with pure aggregation tests for review and completion summaries. (Plan: add a feature-owned pure import-status model beside the existing planner, with discriminated preparing/execution progress phases, itemized outcomes carrying stable item identity and folder/track/selection kind, and explicit failure details. Aggregate review counts from the classified plan plus the normalizer's overlap report, and aggregate completion counts and overall completed/cancelled/partial-failure status from outcomes so later executor and UI tasks share one typed contract. Cover every outcome category, unknown preparing totals, mixed partial failure/cancellation, and successful review/completion summaries in a colocated unit spec. Validate with the focused mobile test, full mobile tests, production and test typechecks, lint, formatting, file-length, strict OpenSpec, whitespace, and touched-file Problems checks.)
- [x] 5.2 Implement the Library batch executor using repository-safe writes and bounded Drive reads, updating progress while retaining independent successes after item-level failures; add integration tests covering source reuse, folder/link creation, and branch-local failure continuation. (Added a feature-owned, dependency-injected executor that refreshes planned tracks through a configurable four-worker Drive-read pool, serializes repository writes, emits counted folder/source/link progress, and records itemized outcomes while allowing independent branches to continue after local failures. Consolidated source persistence behind one `saveSource` repository operation: ordinary calls preserve existing links and synthesize a root link only when needed, while an optional explicit file link atomically places a new canonical source in its planned destination without creating a transient root link. Reusable sources still refresh Drive-owned metadata before missing links are added. Failed folder branches now block only dependent descendants and links, while unsaved new sources and links receive explicit failure outcomes. Extracted executor and repository helpers through the checkpointed move workflow so touched files satisfy lint limits. Focused executor tests pass 2/2, all 56 runtime tests and 589 mobile tests pass, production typecheck and both lint targets pass with zero touched-file warnings, and formatting, strict OpenSpec validation, whitespace, marker, effective file-length, and touched-file Problems checks pass. Test typechecking retains only two pre-existing `AbortSignal.throwIfAborted` compatibility errors in untouched Google Drive files.)
- [x] 5.3 Add cancellation that stops scheduling new work without rollback and retry planning that consumes only failed/cancelled remainder against current Library state; add integration tests proving partial completion and idempotent retry. (Threaded an optional `AbortSignal` through the executor and bounded worker pool, checking each folder, Drive-read, and link scheduling boundary while allowing in-flight work to settle and preserving committed writes. Unscheduled folders, sources, and links now receive explicit cancelled outcomes. Added a pure retry planner that reclassifies failed/cancelled remainder against a fresh Library snapshot, converts dependencies on completed folders to concrete Library targets, and omits unsupported and successful work. Focused integration coverage proves mid-read cancellation, retained partial writes, cancelled-work retry, one-time failure retry, and an empty second retry after completion. Extracted outcome builders through the checkpointed move workflow so touched files satisfy lint limits. The focused recovery tests pass 3/3, all 150 mobile suites pass, production typecheck and lint pass with no touched-file warnings, formatting, strict OpenSpec validation, whitespace checks, marker cleanup, and touched-file Problems pass. Test typechecking retains only the two pre-existing `AbortSignal.throwIfAborted` compatibility errors in untouched Google Drive files.)
- [x] 5.4 Expose plan/execute/cancel/retry operations through the saved-library/Drive controller boundary while preserving the existing single-track save flow, and add focused controller tests for state transitions and error recovery. (Added a feature-owned, dependency-injected `useDriveImportController` with explicit idle, preparing, review, executing, completed, and error states plus plan, execute, cancel, retry, and reset operations. Production dependencies reuse the existing selection normalizer, recursive Drive enumerator, planner, executor, retry planner, local repository, authenticated source refresh, and post-write source/tree refresh callbacks; the composed rehearsal-library controller now exposes this API as `driveImport`. Extracted and regression-tested the pre-existing single-track save-then-optional-link operation without changing its behavior. Focused controller and save-flow tests pass 4/4, all 596 mobile tests pass across 164 suites, production typecheck and lint pass with no touched-file warnings, and formatting, strict OpenSpec validation, whitespace, file-length, and touched-file Problems checks pass. Test typechecking is clean for all touched files and retains only the two pre-existing `AbortSignal.throwIfAborted` compatibility errors in untouched Google Drive pagination/traversal files; eight pre-existing lint warnings remain in untouched files.)

## 6. Add Search Selection and Import UI

- [x] 6.1 Render mixed Drive search rows with folder/audio identity, matched-text highlighting, full path context, folder browse behavior, existing audio preview/save behavior, and focused row view-model tests. (Retained the validated mixed-row presentation and added awaited paginator progress, cumulative path-aware snapshots in 12-result presentation batches, and one operation-wide ancestry cache while preserving complete final Promise semantics for future complete-set selection. Progressive rows remain explicitly incomplete during discovery, stale requests cannot publish, and selecting a folder reconstructs its root plus every accessible ancestor from structured path metadata. Folder browsing now suspends rather than clears the originating search and exposes a distinct `Search results` navigation action that restores the preserved query, scope, and latest mixed results; explicit query/root context changes discard that return state. Added focused paginator, progressive-search, loading-status, navigation-stack, search suspend/restore, and return-action coverage. All 44 Google Drive tests and the full mobile test target pass; both production typechecks and lint targets pass with no errors; scoped formatting, strict OpenSpec validation, whitespace, file-length, marker, and touched-file Problems checks pass. Mobile test typechecking retains only the two known `AbortSignal.throwIfAborted` compatibility errors. Authenticated web verification showed 12 fully path-aware rows under `Loading more results`, reconstructed a deep `My Drive / Musical Projects / Hellscore / Shows / ... / Backups / 20260612_1922_GMT+3.fcpbundle / __AsyncCopying` breadcrumb after opening a search result, and restored the originating `Music` search in My Drive from a browsed result folder with its progressive mixed result set intact.)
- [x] 6.2 Add query-bound selection mode with visible row state, selected count, cancel, individual deselection, and `Select all matching` that prepares every result page; add state-model tests proving query/root/folder changes clear stale selection. (Added a feature-owned selection model and `useDriveSearchSelection` hook keyed by the normalized active query plus Drive root/folder scope, exposing selected mixed results in result order, merging progressive result batches while complete-set selection is preparing, and clearing immediately when the query input or Drive scope changes or when complete-set discovery fails to finish. Wired an accessible `DriveSearchSelectionToolbar` and selected/unselected row treatment (checkmark iconography, `accessibilityState.selected`) into the mixed explorer, suppressed browse/playback actions on rows while selecting, and exposed a stable `Continue` handoff for the 6.3 review flow without starting import planning in this slice. Extracted the row/discovery-result selection wiring (`resolveDriveDiscoveryResultFromRow`, `getDriveExplorerRowSelectionState`) and the toolbar's result-count derivation into existing pure row/view models so the explorer and panel wiring stay covered by focused tests. Covered individual toggle/deselection, cancellation with search context retained, complete progressive selection, and query/root/folder invalidation in model and hook specs, plus presentation-level specs for row selection state, row-to-result round-tripping, and selection-aware result counting. All 614 mobile tests pass; production typecheck and lint pass with zero touched-file warnings (test typecheck retains only the two pre-existing `AbortSignal.throwIfAborted` compatibility errors in untouched Google Drive files); formatting, strict OpenSpec validation, whitespace, and file-length checks pass. Authenticated web verification confirmed individual row toggle/deselect, Cancel restoring normal browse/preview behavior, Select All Matching progressing from a partial count through a spinner to a stable complete count (111 of a large My Drive result set), the review-ready freeze rejecting further row taps, and both a changed query and reverting the query correctly clearing and re-entering selection.)
- [x] 6.3 Add the import review flow with Library destination selection, conditional `Preserve structure`/`Flatten` choice, and effective-plan counts for folders, new/reused/already-present tracks, unsupported files, and collapsed overlaps; test review-state transitions and folder-free selections. (Added a full-screen `DriveImportReviewScreen` under `src/app/screens/drive-import-review/` that `AddScreen` renders in place of the normal Drive panels once the 6.2 selection toolbar's `Continue` flips `isReviewReady`, matching the user's choice of a dedicated screen over a cramped sheet for the destination tree, mode choice, and counts. A `useDriveImportReviewState` hook holds the chosen destination folder id (defaulting to the Library root) and mode, forces `flatten` for a folder-free selection while defaulting to `preserve-structure` once any folder is selected, and automatically recomputes the plan through the existing `driveImport.plan(...)` (5.4) whenever the destination or mode changes, per design.md's "recomputed if the destination or mode changes." The screen renders a flat, label-sorted destination picker from the existing `savedLibrary.files.destinationFolders`, a conditional Preserve/Flatten toggle only when the selection contains a folder, and a summary section reading `driveImport.state` directly (idle prompt, preparing spinner, review counts via a new `buildDriveImportReviewSummaryRows` over the already-computed `DriveImportReviewSummary`, or an error message) with no new count computation of its own. Per explicit user decision, this slice stops at review: `Back to selection`/`Cancel` reset `driveImport` and hand back to the 6.2 selection state, and no `execute()` call or Confirm action is wired yet — that is left for 6.4, which owns the preparing/import progress and completion UI a Confirm action would lead into. Added focused specs for the pure model (`hasFolderInSelection`, `resolveDriveImportReviewMode`, `sortDriveImportDestinationFolders`, `buildDriveImportReviewSummaryRows`), the copy helpers, and the hook's review-state transitions including a folder-free selection that skips the mode toggle and a folder selection that replans on both destination and mode changes. All 631 mobile tests pass, production typecheck and lint pass with zero touched-file errors (lint's 8 `max-lines`/unused-var warnings remain in untouched files; test typecheck retains only the two pre-existing `AbortSignal.throwIfAborted` compatibility errors in untouched Google Drive files), formatting and strict OpenSpec validation pass, and every touched file is under the 300-line limit. Live authenticated verification of the review screen itself (destination picking, mode toggle, and real counts against Drive data) is deferred to 8.2 alongside the rest of the manual Drive verification pass; a pre-auth smoke check in the browser preview confirmed the Add screen still renders normally and no new console errors appeared. Manual testing of this slice surfaced a pre-existing race in the 5.4 `useDriveImportController.plan()`: pressing `Flatten` while a `preserve-structure` plan for the same folder was still preparing superseded it as intended, but if the superseded call's abort-triggered `catch` settled after the new call's success, its unconditional `setState({status:'idle'})` clobbered the fresh `'review'` state, and nothing re-triggered planning since destination/mode hadn't changed again, leaving the review screen stuck. Fixed by gating every state-setting branch in `plan()` on `activeOperation.current === controller` so a superseded operation can no longer overwrite a newer one's result; added a regression test that reproduces the exact settle-order race (resolves the second plan, then rejects the first) and verified it fails without the fix (`'idle' !== 'review'`) and passes with it. Re-ran the full mobile suite (632/632), typecheck, and lint (zero errors, only the same pre-existing warnings in untouched files) after the fix. Further manual testing then surfaced a second, related issue: toggling between `Flatten` and `Preserve structure` after the review had already loaded re-ran the full Drive folder enumeration from scratch every time instead of restoring the previous result, since `plan()`'s folder-content enumeration doesn't depend on destination or mode at all. Fixed by adding a `folderContentsCache` (keyed by Drive folder id) inside `useDriveImportController`, populated the first time a folder's contents are fetched and reused on later `plan()` calls for the same folder regardless of mode/destination changes, cleared on `reset()` so a fresh review session starts clean. Split the growing `use-drive-import-controller.spec.ts` into the original controller spec plus a new `use-drive-import-controller-plan-refresh.spec.ts` for the two plan-refresh regression tests, sharing harness helpers (`renderController`, `requireController`, `requireControllerState`, `createFolderImport`) via `drive-import-controller-test-fixtures.ts`, keeping every file under the 300-line limit. Added a regression test proving `enumerateFolderContents` is called exactly once across three `plan()` calls that alternate modes for the same folder, and verified it fails without the cache and passes with it. All 633 mobile tests pass, typecheck and lint remain clean on every touched file, and formatting passes. Both fixes were then verified live against a real authenticated Google Drive account in the browser preview: selecting a folder and pressing `Flatten` mid-load correctly settled on Flatten's counts instead of getting stuck, and toggling Preserve structure/Flatten repeatedly after the first load resolved near-instantly each time instead of re-preparing.)
- [x] 6.4 Add preparing/import progress and completion UI with cancellation, categorized outcomes, dismiss, and retry-failed actions; test the presentation models for active, cancelled, partial-failure, successful, and retried states. (Extended `DriveImportReviewScreen` to render the existing `executing`/`completed` controller states in place instead of returning null: a new pure `drive-import-progress-model.ts` maps execution phases to labels, completion status to headline/description copy, and completion counts to labeled rows, plus a `resolveDriveImportReviewHeaderMode` mapping used to swap the header's actions per phase. New `ImportProgressSection` shows the active phase and completed/total counts with a spinner; new `ImportCompletionSection` shows the completion headline, all seven itemized outcome counts, and a `Retry failed` action (calling the existing 5.4 `retry()`) whenever the summary status isn't a full success. Per explicit user decisions: while executing, the header's `Back to selection`/`Cancel` are replaced by a single `Cancel import` action wired to `driveImport.cancel()` (stops scheduling without resetting/exiting, so the resulting completion summary still renders); once completed, they're replaced by a single `Dismiss` action that resets `driveImport` and cancels the search selection, returning to normal Add browsing exactly like today's pre-confirmation `Cancel`. Added the `Confirm import` action (calls the existing `execute()`) below the summary while in `review` status, closing the gap 6.3 left open. Destination/mode pickers are now hidden once executing or completed since they no longer apply. Added a colocated `drive-import-progress-model.spec.ts` covering header-mode resolution, phase/status copy, retry eligibility, and the fixed completion row order. All 641 mobile tests pass, production typecheck and lint pass with zero touched-file warnings (lint's 8 pre-existing `max-lines` warnings remain in untouched files), and formatting passes on every touched file. A pre-auth smoke check in the browser preview confirmed the Add/Library screens still render with no new console errors; live authenticated verification of the executing/completed/cancel/retry flow against real Drive data is deferred to 8.2 alongside the rest of the manual Drive verification pass, since exercising it for real would mutate the user's actual Drive-backed library.)

## 7. Follow-ups From 6.4 Manual Verification

Found during a real-account, real-Drive manual pass of the 6.4 progress/completion UI (audio-only import, small folder preserve-structure import, and a 271-track flatten import that was genuinely cancelled mid-execution then retried to completion). The core confirm/executing/cancel/completed/retry/dismiss flow worked correctly end-to-end, including correct keep-both naming for real case-only-duplicate Drive filenames during a Flatten import. These are the gaps/anomalies observed:

- [x] 7.1 Surface per-item failure reasons for a bulk import's `failed` outcomes instead of only an aggregate count. During the 271-track flatten test, the completion summary correctly showed `Failed: 4`, but there is no way in the UI to see which items failed or why, even though each `DriveImportOutcome` with `status: 'failed'` already carries an `errorMessage` (`packages/mobile-rehearsal-player/src/app/library/saved-rehearsal-library/drive-import-status.ts`). (Per explicit user decision (collapsible list, recommended over always-visible or a separate modal/screen), added a pure `getFailedDriveImportOutcomes` selector to `drive-import-progress-model.ts` that filters/narrows a `DriveImportOutcome[]` to the `failed` ones in order. Threaded `outcomes` from `DriveImportControllerState`'s `completed` case through `SummaryCountsSection` into `ImportCompletionSection`, which gained a local collapsed-by-default "Show failed items (N)" toggle (`isFailedListExpanded` local state, consistent with existing patterns like `AddScreen`'s `isSessionMenuVisible`) that reveals each failed item's name and `errorMessage` when expanded; the section renders nothing extra when there are no failures. Added focused unit tests for the selector (order-preserving filter, empty-array case). All 643 mobile tests pass, production typecheck and lint pass with zero touched-file warnings (lint's 8 pre-existing `max-lines`/unused-var warnings remain in untouched files), formatting passes on every touched file, and every touched file is well under the 300-line limit. The app was restarted mid-session, clearing the disposable web preview's local storage (no saved tracks remained), so live verification was limited to a pre-auth smoke check confirming no new console errors; reproducing a genuine `failed` outcome to visually exercise the toggle would need a contrived Drive failure and was judged not worth forcing given the change is a straightforward, fully unit-tested selector plus a conventional collapsible-list pattern already used elsewhere in the app.)
- [x] 7.2 Show meaningful counts during the pre-review "preparing" phase (recursive folder-content enumeration before the plan is ready), matching the "completed and total work when totals are known" requirement in the `mobile-rehearsal-player-usability` spec's "Import exposes meaningful progress" scenario. `useDriveImportController.plan()` already tracks `completedItems`/`totalItems` per folder scanned (`packages/mobile-rehearsal-player/src/app/library/saved-rehearsal-library/use-drive-import-controller.ts`), but `SummaryCountsSection`'s `'preparing'` branch (`packages/mobile-rehearsal-player/src/app/screens/drive-import-review/summary-counts-section.tsx`) only renders a generic "Preparing import review…" spinner instead of e.g. "Scanning folder 3 of 5", unlike the detailed phase+count display added for `'executing'` in 6.4. (The `preparing` and `executing` `DriveImportControllerState` variants already carry the same `DriveImportProgress` shape, and the existing `ImportProgressSection`/`getDriveImportProgressCopy` (added in 6.4) already render phase label plus "completed of total" (or "N items done" when no total is known) generically for any `DriveImportProgress`. Reused that existing component for `preparing` instead of the static spinner text, collapsing the `'preparing'`/`'executing'` cases in `SummaryCountsSection` into one arm since they now render identically. Removed the now-unused `'preparing'` branch (and its only test) from `getDriveImportReviewSummaryStatusCopy`, which is left covering only `idle`/`error`. No new pure-model code was needed since `getDriveImportProgressCopy` already had colocated coverage for the no-total preparing case; this was a presentation-wiring fix, consistent with the repo's existing pattern of not unit-testing `.tsx` presentation components directly. All 642 mobile tests pass (down from 643 only because the removed dead-copy test was deleted), production typecheck and lint pass with zero touched-file warnings (lint's same 8 pre-existing warnings remain in untouched files; test typecheck retains only the two pre-existing `AbortSignal.throwIfAborted` compatibility errors in untouched Google Drive files), formatting, whitespace, and strict OpenSpec validation pass. Live authenticated verification in the browser preview against a real, large My Drive account: selecting 3 folders from a "Music" search and continuing to review showed "Preparing Drive contents" with a live "0 of 3" → "1 of 3" count while one large shared folder enumerated, before landing on a real review summary (36 folders, 16 new tracks); cancelled out without importing anything, keeping the disposable preview library unchanged.)
- [x] 7.3 (Low confidence — needs reproduction) Investigate a possible one-step lag in the Add screen's Drive browser "current location" header title versus its content banner when navigating back up through folders (e.g., the header briefly read "Downloads" while the "Drive browser ready" banner underneath already said a nested folder name was loaded). It resolved correctly once navigation settled and may just be a transient render during the async folder load rather than a real bug — confirm with a deliberate repro (e.g. rapid back-navigation clicks) before treating as a defect, likely in `packages/mobile-rehearsal-player/src/app/library/drive/hooks/use-drive-library.ts` or the destination header model. (Confirmed as a real, root-caused defect, not a transient render: in `use-drive-library.ts`, `currentLocation` is derived synchronously from `navigationStack` and updates the header title (`currentTitle` in `drive-discovery-panel-model.ts`) the instant a navigation action fires. The content banner, however, comes from `getDriveLibraryStatusCopy` (`drive-library-view-model.ts`) reading the still-stale `browseSnapshot`, which is only replaced once the async `browseDriveLocation` fetch for the new folder resolves. The bug: that function's browse-loading branch only returned "Loading Drive browser" copy when `isLoading && browseFolderCount + browseTotalSourceCount === 0` — so whenever the _previous_ (stale) folder's snapshot had any folders or sources (the common case, e.g. navigating up from a populated folder to "Downloads"), the zero-count guard failed, the function fell through to the "ready" branches, and it rendered a `tone: 'ready'` banner describing the _old_ folder using `browseSnapshot.location.name`, contradicting the header's already-updated title and the genuinely in-flight `isLoading: true` fetch — exactly the reported "header says Downloads, banner already looks loaded" mismatch. Fixed by making the loading branch fire on `isLoading` alone (no count gate) and by threading an accurate `currentBrowseLocation` (from `driveLibrary.currentLocation`, the same synchronous source the header title uses) through `getDriveLibraryStatusCopy` and its only caller (`use-rehearsal-library-controller.ts`) so the loading message names the folder actually being fetched instead of the stale one; removed the now-dead `getTotalBrowseSourceCount` helper. Added a regression test reproducing the exact scenario (stale nonzero `BROWSE_SNAPSHOT` plus a distinct in-flight `currentBrowseLocation: 'Downloads'`) that fails against the old zero-count-gated logic and passes with the fix. All 643 mobile tests pass, production and test typechecks pass (test typecheck retains only the two pre-existing `AbortSignal.throwIfAborted` errors in untouched Google Drive files), lint passes with zero touched-file warnings (same 8 pre-existing warnings remain in untouched files), and formatting, whitespace, and strict OpenSpec validation pass. Attempted a live browser-preview repro against a real, deeply nested My Drive folder tree (`Backup / Downloads Archive / Crayon Physics`) by navigating in and clicking back, but the dev/API round trip in this environment resolved too quickly to catch the stale-banner frame in a screenshot; the fix's correctness rests on the code-path analysis above and the unit test, which fails without the fix and passes with it.)
- [x] 7.4 (Low priority, verification only) Confirm `createDriveImportRetryPlan` (`packages/mobile-rehearsal-player/src/app/library/saved-rehearsal-library/drive-import-retry-planner.ts`) excludes already-`reused`/`already-present` tracks from its rebuilt plan and only re-processes failed/cancelled remainder. Manual observation during the 271-track test showed the retry's progress total ("of 271") was close to the original plan's total tracks count rather than visibly smaller, which is plausible given how few items had resolved before cancellation but was not precisely confirmed; a targeted unit test asserting `retryPlan.tracks.length` excludes non-failed/cancelled outcomes would close this out with certainty. (Confirmed correct by code analysis and a new targeted test, with no code change needed: `createDriveImportRetryPlan`'s track filter (`retryableItemIds.has(canonicalSourceId) || retryableItemIds.has(libraryFileLinkId)`, where `retryableItemIds` only contains ids from `'cancelled'`/`'failed'` outcomes) is generic over outcome status and was already correctly excluding any track whose source/link outcome was `'reused'` or `'already-present'`, exactly like it already excluded `'created'` in the pre-existing `'replans only a failed track after its sibling completes'` test. Added a new `drive-import-retry-classification.spec.ts` test that pre-seeds one already-saved source with an existing link at the import destination (`already-present`), one already-saved source with a link only in a different folder (`reusable`, needs just a new link), and one brand-new source whose save is injected to fail; asserts the classified plan and the real executed outcomes (`reused`/`already-present`/`created`/`failed` at the expected source and link ids) match this setup precisely, then asserts the resulting `createDriveImportRetryPlan(...).tracks` contains only the failed new track. `drive-import-recovery.spec.ts` exceeded the 300-line lint limit once this test was added, so per the file-length guardrail it was split via the `code-move-checkpointed` workflow: the new test moved into `drive-import-retry-classification.spec.ts`, and shared setup (owner id, fixed clock, `AsyncStorage` mocking, `createPlanState`, `asDiscoveredAudio`/`asEnumeratedAudio`) was extracted into a new `drive-import-recovery-test-fixtures.ts` used by both spec files, keeping every file well under 300 lines. All 644 mobile tests pass, production typecheck and lint pass with zero touched-file warnings (test typecheck retains only the two pre-existing `AbortSignal.throwIfAborted` compatibility errors in untouched Google Drive files; lint's same 8 pre-existing warnings remain in untouched files), and formatting, whitespace, and strict OpenSpec validation pass.)
- [x] 7.5 Stop `DriveImportReviewScreen` from replacing the app's shared header UI, and keep `Confirm import` always reachable. The screen (since 6.3, extended in 6.4) rendered its own bespoke header — a plain cream block with a bottom border and green text-link actions, no Drive session indicator — instead of the `DestinationHeader` component every other screen (Library/Add/Recents) uses (the dark-green rounded card with the `DriveSessionMenu` avatar, per `packages/mobile-rehearsal-player/src/app/components/destination-header.tsx`), and buried `Confirm import` as the last item inside the scrollable body where it could be scrolled out of view. This went through two rounds of user feedback within the same session: (1) an initial fix reused `DestinationHeader` but put the flow actions (`Back to selection`/`Cancel`/etc.) in its `trailingAction` slot as text buttons, which squeezed the title into truncation ("Review i...") and still didn't restore the session avatar; (2) the user then pointed out the header still looked non-standard (no session indicator, non-standard trailing content) and asked for `Confirm import` to be persistently visible rather than scrollable. Final implementation: extended `DestinationHeader` with an optional `subtitle` prop (passed through to `SectionHeading`'s `body` slot with a translucent-cream `bodyStyle` override for legibility on the dark background) so the review screen's helper copy still renders. `DriveImportReviewScreen` now takes an `authorization: DriveSessionMenuController` prop (threaded from `AddScreen`, which already holds it) and renders `<DestinationHeader title="Review import" subtitle={helper} trailingAction={<DriveSessionMenu .../>} />` — the header's trailing slot now contains _only_ the same session menu every other screen shows, with its own local `isSessionMenuVisible` state and backdrop, matching `AddScreen`'s exact pattern. All flow actions (`Back`, `Cancel`, `Cancel import`, `Dismiss`) and the primary `Confirm import` CTA moved out of the header entirely into a new bottom action bar rendered as a sibling _outside_ the `ScrollView` (not inside its scrollable content), so `Confirm import` stays visible above the tab bar regardless of scroll position, addressing the "not hidable by overflow" requirement directly. `Back`/`Cancel` render as compact secondary (outlined) buttons and `Confirm import`/`Cancel import`/`Dismiss` as the primary (filled) button, reusing the same primary-button visual language as elsewhere in the screen. The short-lived `ReviewHeaderActionButton` icon-button component from the first round was deleted as no longer needed. Also removed the screen's own redundant `paddingHorizontal: 20` (the shell wrapper around every tab panel already applies `14px` horizontal padding to all screens; the review screen was previously double-padded to `34px`, wider than every other screen). `destination-header-model.spec.ts` (the only existing test for this area) is unaffected since it only covers `getDestinationHeaderModel`, not the component; no behavioral logic changed, only presentation/layout, so no new tests were added beyond re-running the full suite. All 643 mobile tests pass, production typecheck and lint pass with zero touched-file warnings, formatting passes, and every touched file stays well under 300 lines. Verified live in the browser preview against the real Drive account: header now shows the full untruncated title, subtitle, and a working session-menu avatar (opens/closes the same "Drive connected" panel used elsewhere) identical to Library/Add; the bottom action bar (Back/Cancel/Confirm import, then Dismiss after a real completed import) stayed pinned above the tab bar throughout.

## 8. Original Drive Folder Actions

- [ ] 8.1 Show the last-known original Drive path and provenance-dependent actions in saved-track detail/management surfaces without conflating it with the track's Library folder placement, with focused presentation tests.
- [ ] 8.2 Add a shared source-location resolver that fetches current metadata by saved `driveFileId`, resolves the current accessible parent/path, detects a moved file, and persists refreshed provenance while preserving app-owned fields; add focused tests for unchanged and moved files, root changes, renamed ancestors, inaccessible parents, deleted files, authorization failure, and lookup failure without stale-folder fallback.
- [ ] 8.3 Add `Show in Add` routing that uses the shared live resolver before switching roots or reconstructing the breadcrumb stack, opens the resolved current parent after a move, and preserves the user's current context plus last-known path when resolution fails; add router/hook tests proving it never opens a still-existing stale parent folder.
- [ ] 8.4 Add `Open in Google Drive` through the same live resolver plus a tested current-folder URL builder and React Native Linking adapter, including moved-file routing, resolution failure without stale-folder fallback, unsupported-link, and open failure feedback, without changing the existing source-file `webViewLink` behavior.

## 9. Integrated Validation

- [ ] 9.1 Run focused and full Nx test, typecheck, and lint targets for `google-drive`, `audio-library-models`, `audio-library-runtime`, and `mobile-rehearsal-player`, and clear Problems in every touched file.
- [ ] 9.2 With an authenticated Drive account, manually verify My Drive and shared-folder search, more than 100 results, mixed selection and deselection, overlapping selections, flat and preserved imports, cancellation/retry, source reuse, a source file moved to a different folder after save, inaccessible/deleted source files and folders, `Show in Add`, and external `Open in Google Drive` on web and at least one native mobile platform. (Partially covered by the 6.4 manual web pass below: real-account audio-only import, small preserve-structure folder import, a large 271-track flatten import with a genuine mid-execution cancel, and a full retry-to-completion were exercised on web against a real Drive account. Still outstanding for this task: `Show in Add` / `Open in Google Drive` (blocked on 7.x), a moved/inaccessible source file, and any native iOS/Android pass.)
