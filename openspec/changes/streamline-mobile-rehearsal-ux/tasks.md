## 1. Completed Baseline

These items already reflect the current app state. Treat them as the regression-sensitive baseline for the remaining work rather than as future implementation steps.

### IA, Shell, and Search Context Baseline

- [x] 1.1 Document the current top-level IA (Home/Search/Library baseline with Home -> Recents and Search -> Add rename intent) and map every existing user-visible feature to a destination or entry point before reordering work begins.
- [x] 1.2 Define the next IA arrangement for top-level tabs and within-tab section order, including explicit placement for Google Drive browse/navigation, Drive search, and app-library search.
- [x] 1.3 Add non-regression acceptance criteria for critical existing capabilities (Drive root switching, folder navigation, breadcrumbs, source save/remove, track playback, loop creation, playlist playback).
- [x] 1.4 Add UI-level context labels and helper copy rules that prevent Drive features from being hidden or mislabeled after IA changes.
- [x] 1.5 Validate proposed IA against representative flows (discover in Drive -> save -> add to playlist -> play) and refine before component implementation.
- [x] 1.6 Build compact Recents mockups that include multiple shortcut modules (for example recents and popular tags) and capture design confirmation before implementation.
- [x] 3.1 Reorder top-level tab composition and within-tab sections according to the IA plan while preserving current feature coverage.
- [x] 3.1.1 Rename the middle destination from `Search` to `Add` across shell labels, file/component/constant identifiers, and repo guidance or skill docs so `Search` remains an operation rather than a destination name.
- [x] 3.2 Rebalance Recents hierarchy so Recents stays optional and non-blocking while still supporting recents and shortcut use cases.
- [x] 3.3 Add recent rehearsal entry points on Recents with concise fallback guidance for first-use or empty-history states, and optional shortcut metadata (for example popular tags).
- [x] 3.3.1 Use icon-first playback entry on Recents resume and shortcut cards, replacing any text-labeled `Play` button with a standard play icon while keeping descriptive copy outside the control.
- [x] 3.3.2 Rename remaining legacy `Home` file/component/constant identifiers to `Recents` equivalents while preserving behavior and destination order.
- [x] 3.3.3 Expand Recents from single-item resume to session-persisted compact multi-item history (tracks/loops/playlists) with explicit per-item play buttons and clear `Last played` labeling semantics.
- [x] 3.3.4 Add a vertical-ellipsis overflow menu on Recents recent-item rows with `Play next`, `Add to queue`, and `View in library` actions while keeping inline icon-only `Play` as the primary action.
- [x] 3.4 Implement explicit dual search contexts: Google Drive discovery search (including folder scoping) and dedicated app-library search.
- [x] 3.5 Add clear active-context and scope indicators so users always know which corpus (Google Drive in Add vs library in Library) is being searched.
- [x] 3.6 Scope Add Drive search to navigation context after folder drill-down: keep root-level search at selected-root scope, and default to current-folder-path scope after users enter folders.
- [x] 3.7 Add recent-search suggestion interactions for the active search context that allow tap-to-run query execution.
- [x] 3.8 Verify Google Drive navigation remains first-class after tab/section reorder inside Add: root selector, folder drill-down, breadcrumbs, visible search-scope continuity, and unavailable/source status visibility.
- [x] 3.9 Add Drive search-result preview playback so playable Google Drive sources can be auditioned without saving first, while keeping Save available as a separate action.
- [x] 3.10 Highlight matched query substrings in visible search-result text for both Add (Google Drive) and Library contexts, using the same normalized match semantics as result filtering.
- [x] 5.1 Add dedicated app-library search behavior over saved entities (tracks, loops, playlists, folders, tags) independent of Drive discovery search.
- [x] 5.2 Add tag assignment and tag-filter interactions for app-owned library entities without changing playback semantics.
- [x] 5.2.1 Ensure loops remain independently taggable even when default Library browsing reaches them through `View track loops` parent-track navigation.

### Playlist, Queue, and Playback Baseline

- [x] 2.1 Refine playlist detail hierarchy so playback actions stay primary and non-critical management actions move to lighter affordances.
- [x] 2.1.1 Standardize playlist overflow affordances by pinning vertical-ellipsis triggers to top-right in playlist list cards and playlist detail cards.
- [x] 2.1.2 Reuse a shared options-sheet surface for playlist management and track-context menus, replacing bespoke per-surface menu sheets.
- [x] 2.2 Implement low-friction playlist item removal interaction in default playlist detail mode while preserving undo snackbar recovery.
- [x] 2.3 Remove the dedicated playlist edit mode and expose default-visible playlist row reorder controls that follow Apple-style drag affordances, keep play/pause inline, and group up/down controls beside the overflow trigger.
- [x] 2.3.1 Route playlist-row `Move to position` and `Remove` into the shared overflow menu while reusing the same bounded slider modal and confirmation flow as the active queue control.
- [x] 2.4 Add `Play next` queue quick-action support for saved track and loop rows without interrupting active playback.
- [x] 2.5 Add `Add to queue` queue quick-action support for saved track and loop rows without interrupting active playback.
- [x] 2.6 Update queue and now-playing control presentation so mode-aware controls remain explicit and queue-only controls stay hidden in standalone playback.
- [x] 2.7 Standardize saved loop cards with saved track cards for add-to-playlist affordances, including consistent action placement, labels, and feedback.
- [x] 2.8 Add a top-level play icon on playlist cards while preserving `Open playlist` navigation and existing overflow management actions. Do not use a text-labeled `Play` button.
- [x] 2.8.1 Add `Remove playlist` to the shared playlist-card overflow menu so saved playlists can be deleted without opening playlist detail.
- [x] 2.8.2 Keep playlist-card rename in Library context by opening the rename flow in place instead of routing through playlist detail.
- [x] 4.2.7 Extend queue-action availability rules to every queue-capable item surface so users can create or grow a transient queue from standalone playback without first opening a playlist. Implement transient queue promotion when `Play next` or `Add to queue` is invoked during standalone single-track playback, keeping the current track as queue head and preserving uninterrupted playback.
- [x] 4.2.8 Surface queue-management affordances across queue-capable item surfaces once a transient or playlist-backed queue exists, while keeping queue-only controls hidden during true single-item playback with no queued follow-up.
- [x] 4.2.9 Add a `Create new playlist` action in the Up Next queue summary area to save the current queue order as a new playlist while preserving uninterrupted playback, then immediately treat the active queue as that new playlist for follow-up updates.
- [x] 4.2.10 Add an `Update current playlist` action in the Up Next queue summary area for playlist-backed queue sessions, confirm before replacing that playlist's saved items and order with the current queue order, and keep the active queue session unchanged.
- [x] 4.2.11 Constrain the active rehearsal queue list to a scrollable maximum height so the queue summary, queue-mode controls, and transport remain visible for long queues.
- [x] 4.2.12 Add active queue row controls for direct playback and reordering: per-row play buttons, drag handles, and overflow actions for remove, move to start, move to end, and move to a specific queue position.
- [x] 4.2.13 Add a move-to-position modal for queue rows with a slider bounded from queue position `1` through the last queue position, and apply the selected position without restarting playback.
- [x] 4.2.14 Remove redundant `Up next` / `Now playing` row text from the active queue once direct play controls exist, and keep current-item state legible through row styling and control state.
- [x] 4.2.15 Add previous and next track transport controls directly to the active rehearsal queue view.

### Row-Action, Loop, and Shared UI Baseline

- [x] 4.1 Add context-aware default loop naming in loop creation while preserving user override behavior before save.
- [x] 4.2 Align row-level action menus to include new queue-acceleration actions and maintain consistent icon semantics across Library and Add, while keeping saved track and saved loop rows behaviorally identical except for track-only loop creation.
- [x] 4.2.1 Apply the shared overflow menu UI to saved source rows (`DriveLibrarySourceGroup`) and route secondary/destructive actions into that menu.
- [x] 4.2.2 Introduce explicit row-action placement metadata (`inline` vs `menu`) with backward-compatible fallback logic and migrate current source/playlist callers.
- [x] 4.2.3 Replace heuristic action grouping rules with explicit placement-only behavior after caller migration and validation.
- [x] 4.2.4 Expand the shared overflow menu pattern to remaining row-action surfaces (for example loop rows and future queue quick-action entry points) so saved track and saved loop rows converge on the same top-level menu structure and icon semantics.
- [x] 4.2.5 Extract a shared overflow-trigger primitive (top-right vertical-ellipsis affordance) and adopt it in playlist, source, and future row-action surfaces.
- [x] 4.2.6 Converge playable row surfaces in Library and Add on one inline icon-only `Play` action plus a shared overflow trigger, while keeping `Make loop` as a saved-track-only overflow action.
- [x] 4.2.6.1 After row-action placement and quick-action semantics stabilize, extract a shared compact playable-row shell and adopt it in Add, Library, loop, and Recents row-style surfaces without collapsing surface-specific metadata, badges, or availability messaging.
- [x] 4.3 Ensure loop management remains parent-track-first while supporting optional promotion of loops to first-class organization surfaces.
- [x] 4.4 Add track-context loop management entry points (create, view, edit, remove) and preserve visible parent-track linkage in all loop surfaces, with `Make loop` living in saved track overflow only.
- [x] 4.4.1 Keep the default top-level Saved loops Library section and add a `View track loops` saved-track overflow action that opens a full-screen Library detail view for that track's loops when loops exist, while preserving loop action parity for playback, add to playlist, queue, and remove flows.
- [x] 4.4.2 Keep the track-scoped loop surface visibly tied to its parent track, make it replace the main Library browse UI with a back path while active, add ordered loop-series playback plus a `Make new loop` action for that track, and reuse the same shared loop row actions and metadata needed for fast playback and playlist workflows.
- [x] 4.5 Audit shell, playback, and queue surfaces for remaining text-labeled `Play` buttons, replace them with standard playback icons where appropriate, and update icon-only control labels, selected/disabled states, and touch-target sizing.
- [x] 4.5.1 Extract a shared icon-only playback-action primitive for repeated compact playback entry points (for example Add source rows, Recents rows and shortcut chips, and playlist cards) while keeping larger transport controls on their existing surface-specific components.
- [x] 4.5.2 Redesign playlist-detail fresh-start playback controls into icon-first ordered/shuffle actions, keep the active queue mode legible, and preserve a playlist-specific larger control surface instead of collapsing those actions into the compact row/card playback primitive.
- [x] 4.6 Extract shared interaction style tokens for card shells, action buttons, chips, and pressed/disabled states used by playlist/source/menu surfaces.
- [x] 4.6.1 Extract a shared contextual search panel scaffold for Add and Library search surfaces, reusing the input row, submit affordance, recent-search suggestions, and helper or clear-action slots while preserving each search context's copy and disabled rules.
- [x] 4.6.2 Extract a shared section-heading primitive with eyebrow, title, body copy, and optional trailing action, and adopt it in in-content Drive, Library, playlist, and modal entry surfaces where that structure already repeats. Do not use it for top-level compact destination headers.
- [x] 4.6.3 Extract a shared feedback-card family for status, issue, and empty states, and adopt it in Drive status, playlist creation and rename issues, and empty playlist-selector flows without changing workflow semantics.
- [x] 4.6.4 Extract a shared chip family for recent searches, Drive root selection, Recents shortcut tags, and future tag or filter surfaces with passive, selected, and action variants.
- [x] 4.7 Extract a shared centered dialog-card shell for rename/create/select flows and migrate existing playlist dialogs without changing workflow behavior.
- [x] 4.7.1 Move create-playlist entry into the Library Playlists section header with a right-aligned `+` trigger that opens the shared create modal with playlist name input, replacing any persistent bottom-of-Library creation component.
- [x] 4.7.2 Extract a shared modal-surface base beneath bottom-sheet and centered-dialog variants, then migrate playlist, selector, and loop-builder flows onto it without forcing one modal presentation across all cases.

## 2. Checkpointed Implementation Plan

Complete these steps in order. Each `2.x` group is one checkpoint, each `2.x.y` checkbox is the unit of execution to complete individually, and the original spec-task references are preserved in parentheses for traceability.

### 2.1 Shared Compact Destination Header

- [x] 2.1.1 Implement the remaining shared compact destination-header rollout across Recents, Add, and Library: remove large descriptive headers, keep destination titles leading, keep `Search` adjacent to the Drive session menu in Add, keep Add `Refresh` as the leading header action when the visible Drive context can refresh, keep `Filters` and `Search` adjacent in Library, place the Drive session menu trigger to the right of search when present, and preserve surface-specific trailing actions where search is not relevant (`3.8.1`).
- [x] 2.1.2 Add or finish UI coverage for compact destination-header and contextual-search adoption where this rollout changes behavior (`6.1.4` scoped to header and search-shell adoption).
- [x] 2.1.3 Manually verify Add refresh/search/session-menu ordering, Library filters/search/session-menu ordering, Drive session trigger placement, Recents title alignment, and shell-level non-regression for the touched surfaces (`6.2.5` and `6.2.6` scoped to header rollout).

### 2.2 Stabilize the Already-Shipped Queue, Playlist, and Shared-Action Behavior

- [x] 2.2.1 Backfill focused automated coverage for row-action placement, shared overflow trigger behavior, search-result highlighting, active queue behavior, and default playlist row controls (`6.1.1`, `6.1.2`, `6.1.3`, `6.1.5`, and `6.1.5.1`).
- [x] 2.2.2 Run manual regression for playlist row-start playback, playlist-card play icon behavior, Recents shortcut playback, transient queue promotion, queue-to-playlist capture, active queue controls, playlist-detail playback controls, and search highlight updates (`6.2`, `6.2.1`, `6.2.2`, `6.2.3`, `6.2.3.1`, `6.2.3.2`, and `6.2.4` scoped to the already-implemented non-Files surfaces).
- [x] 2.2.3 Record pass/fail against the non-regression baseline from task `1.1` for the capabilities touched in this step (`6.3` scoped to queue, playlist, search, and Recents behavior).
  - PASS: Recents live verification showed persisted recent rows, `Last played` labeling, inline icon-only play affordances, and the `Play next` / `Add to queue` / `View in library` overflow actions. The `View in library` handoff kept playback running.
  - PASS: Library search live verification showed the explicit `Search saved library` context, recent-query re-entry, and visible substring highlighting (`light` highlighted in `03 Lightning Strikes Twice.mp3`).
  - PASS: Playlist live verification showed icon-only playlist-card playback, playlist-detail ordered/shuffle playback controls, active-session detail copy, and default row-level reorder/playback controls.
  - PASS: Queue live verification showed the Up Next `Create new playlist` / `Update current playlist` actions, previous/next transport, and row-level play / drag / overflow affordances. Focused automated coverage also passed via `npx tsx --test` for Recents, playlist, queue, and library-search specs.

### 2.3 Introduce the File-Tree Storage Foundation

- [x] 2.3.1 Replace the current single-folder entity metadata approach with a file-tree storage model and migration path: folder nodes, file-link nodes, hard-link support, pointer-local visible names, canonical entity persistence, and last-link lifecycle semantics (`5.3` and `5.3.1`).
- [x] 2.3.2 Implement the Files mutation guardrails in the storage and helper layer: case-insensitive same-parent naming, duplicate-name conflict handling, case-insensitively unique `Copy` suffix defaults for same-folder copies, and folder-move prevention for self or descendant targets (`5.3.1.1`).
- [x] 2.3.3 Preserve loop parent-track context in the underlying model so loops remain identifiable across Files, search, tags, and folder results as later UI work lands (`5.4` and `5.4.1` foundation work).
- [x] 2.3.4 Add focused model and helper coverage for tree mutations, hard-link behavior, naming guardrails, and parent-track context wiring (`6.1` and `6.1.6` scoped to the data layer foundation).

### 2.4 Rebuild the Explorer Shell and Navigation Surfaces

- [x] 2.4.1 Rebuild Library Files around one mixed-list explorer shell with current-folder navigation chrome, horizontally scrollable breadcrumbs, leading type icons, row-body primary-action semantics, and a persistent Google Drive-style floating Files `+` create control (`5.3.2`).
- [x] 2.4.2 Align Add Drive browsing with the same explorer chrome and shared list primitives where the spec calls for parity, while preserving Drive-specific save and preview behavior (`5.3.2` on the Add surface).
- [x] 2.4.3 Ensure folder rows push onto a standard explorer stack, track and loop rows play in place, playlist rows preserve a back path to the originating Files folder, and dedicated Tracks, Loops, and Playlists views remain coherent beside Files (`5.3.2.1` and `5.3.4`).
- [x] 2.4.4 Extend UI coverage for the shared explorer and list primitives, then manually verify Add and Files explorer navigation, breadcrumb behavior, and playlist return-path behavior (`6.1.4`, `6.2.5`, and the navigation subset of `6.2.7`).

### 2.5 Implement Files Actions, Copy Flows, and Availability Recovery

- [x] 2.5.1 Implement Files overflow operations for folders and saved-entity links, keeping queue actions in the first menu level and reusing the existing tag editor, playlist selector, and loop-builder flows where applicable (`5.3.3`).
- [x] 2.5.2 Standardize per-entity explorer menu contents and ordering, including action-sheet dismissal through `Cancel`, destructive ordering, clear pointer-versus-entity deletion messaging for pointer-level `Delete from folder`, and explicit track-level `Remove from library` placement (`5.3.3.1`).
- [x] 2.5.3 Add `Create a copy`, rename, move, `Delete from folder`, folder-delete impact summaries, and connection-first broken-source recovery through `Reconnect` and `Remove from library` (`5.3.3` and `5.3.3.2`).
  - Wire existing Files menu placeholders to file-tree mutations for copy, pointer-local rename, move, and delete confirmations.
  - Add folder delete impact summaries and recovery actions for unavailable Drive-backed tracks without changing the dedicated entity views.
  - Keep root-folder and connection-first guardrails explicit, and leave broader automated coverage to task `2.5.4` after this behavior lands.
- [x] 2.5.4 Add focused coverage for Files menus, hard-link copy semantics, `Delete from folder` messaging (including explicit last-link confirmation), root-folder default visibility invariants, and recovery actions, then manually verify entity-specific explorer menus and naming or move guardrails (`6.1.6`, `6.2.7.1`, and `6.2.7.2` scoped to Files actions).
  - Extend the existing Files row/action and explorer model specs rather than adding a broader UI harness.
  - Cover operation-level copy/delete impact behavior with a lightweight repository fake so hard-link semantics and root-folder invariants are observable.
  - Automated coverage added and focused specs pass.
  - PASS: Live Files explorer verification in the VS Code integrated browser showed root-folder default visibility, disabled root back navigation, folder/track/loop-specific menu contents, destination picker current-folder labeling, self-move prevention for folders, duplicate-name conflict messaging, explicit last-link delete confirmation, folder-delete impact summaries, and the floating Files create menu without mutating the library.
  - PASS: Additional live folder flow verification moved a unique track into a folder, saved a loop from that folder-resident track into the same folder, and created a playlist from the folder create menu. Playlist creation initially left an extra root link; the Files create flow now moves the default root playlist link into the current folder, matching the loop save behavior, and focused coverage plus live verification confirm the fixed playlist appears in the folder without appearing at root.
- [x] 2.5.5 Implement and test always-available track-level `Remove from library`: keep the action available during active playback and while track dependencies exist, show an explicit impact summary of affected loops/folder links/playlist entries, require explicit confirmation, and verify cascade cleanup across library entities and active playback context (`5.3.3.1`, `6.1.6`, and `6.2.7.2`).
  - Enabled Files track-level `Remove from library` for available tracks, added dependency-aware confirmation copy for loops, folder links, and playlist entries, and refreshed playlist state after source removal so active playback sync receives pruned library context.
  - Added focused runtime and mobile coverage for source deletion cascade, Files removal impact counts, and enabled Remove-from-library menu behavior.
- [x] 2.5.6 Replace the temporary flat destination list for `Move to folder` and `Create a copy` with a proper folder-browser picker that supports navigating the Files hierarchy, selecting the target folder in context, preserving root/current-folder guardrails, and scaling beyond short folder lists (`5.3.3` and `6.2.7.1`).
- [x] 2.5.7 Add lightweight success feedback for Files mutations: move, copy, rename, delete-link, folder creation, playlist creation, Drive import staging, and loop saves launched from Files. Keep feedback brief, include destination context when useful, and offer a folder-navigation action such as `View in folder` only when the user is not already looking at the destination.
  - Add one shared Files success-feedback model/controller at the Library Files boundary so row actions, create controls, and Files-launched loop saves use the same compact copy and dismissal behavior.
  - Emit success feedback after completed move, copy, rename, delete-link/folder, create-folder, create-playlist, Drive import staging, and Files-context loop-save operations without changing storage semantics.
  - Keep destination context in the message and only expose a folder-navigation action when feedback points to a folder the user is not already viewing.
- [x] 2.5.8 Make duplicate-name guardrails more actionable across Files create, move, copy, and rename flows. When a case-insensitive conflict occurs, preserve the clear error copy and add a low-friction recovery path such as suggested `Copy` naming, rename-and-retry, or returning directly to the relevant name field without restarting the whole flow.
  - Promote duplicate-name conflicts from plain issue strings to structured Files issue metadata so create, rename, move, and copy surfaces can recognize sibling-name collisions without changing repository guard behavior.
  - Add inline recovery affordances to the existing create and rename dialogs that let users accept a suggested unique name and retry from the same field instead of cancelling and reopening the flow.
  - Keep move/copy destination state open on conflict and offer a rename-first handoff that seeds the existing rename dialog with a suggested unique name, then resumes the user's original context without restarting folder navigation.
  - Backfill focused specs around duplicate-name issue formatting, rename/create recovery metadata, and destination-picker conflict recovery before checking this task complete.
- [x] 2.5.9 Improve folder row metadata so mixed-content folders are easier to scan in music-library browsing. Replace or augment generic item counts with compact type-aware summaries such as track, loop, and playlist counts while preserving a concise fallback for narrow rows and unknown item mixes.
  - Move Files folder supporting-label generation into a direct child-type summary helper in the explorer model so folder rows can distinguish tracks, loops, playlists, and child folders without changing the row shell.
  - Prefer music-entity counts in mixed folders, keep concise folder-aware fallback copy when only one music type is present alongside subfolders, and retain total-item fallback when the mix cannot be summarized clearly.
  - Add focused model coverage for mixed-content, folder-only, and compact fallback summaries before checking this slice complete.
- [x] 2.5.10 Improve empty playlist detail when opened from Files. Keep the existing playback-first playlist detail hierarchy, but make folder-origin context legible, update empty-state copy so it does not imply users must leave the current folder, and add an `Add items` path that can start from the originating Files folder.
  - Capture the originating Files folder name alongside the existing Files back-path state so empty playlist detail can reference the current folder explicitly.
  - Replace the generic empty-playlist copy with a Files-aware variant when playlist detail was opened from Files, and expose an `Add items` action that opens a Files item-picking mode rooted in the originating folder instead of dropping users into generic browse.
  - Keep the picker mode inside the existing Files explorer by surfacing direct `Add` actions on nearby track and loop rows plus a clear return path back to playlist detail, while preserving the existing row overflow `Add to playlist` path as the fallback elsewhere.
  - Cover the empty-state copy and Files-origin action mapping with a focused playlist-origin spec before checking this slice complete.
- [x] 2.5.11 Add a low-friction set-building path after creating a playlist inside Files. Users should be able to add nearby folder tracks or loops to the newly created playlist without hunting through row overflow menus one item at a time, while preserving existing per-row `Add to playlist` behavior as a fallback.
  - Keep the Files add-items picker reachable after the first item is added by showing a persistent `Add items` control in playlist detail, not only in the empty state.
  - Route playlist overflow affordances into the same Files add-items picker from playlist detail, playlist cards, and Files playlist rows, with current-folder context when available and root-folder fallback otherwise.
  - Add focused coverage for shared playlist overflow action ordering plus Files playlist-row menu ordering after the new `Add items` entry is introduced.

### 2.6 Add Files Search, Sort, Restoration, and Loop-Result Integration

- [x] 2.6.1 Add Files search behavior that defaults to the current folder subtree, supports an explicit `All Files` option, and shows containing-path metadata when results come from outside the currently visible folder list (`5.1.1` and `5.1.1.1`).
  - Keep Files search inside the Files explorer shell instead of falling back to the grouped Library search results when `selectedView === 'files'`.
  - Add Files-specific search scope state (`This folder` by default with an explicit `All Files` option) without changing the existing track, loop, and playlist search flow.
  - Surface containing-path metadata on matching rows that come from outside the current visible folder list, and cover current-folder-vs-all-files behavior with focused Files model tests before checking this slice complete.
- [x] 2.6.2 Add Files sort controls for `Name`, `Type`, `Date added`, and `Date opened`, including case-insensitive name sorting, folder-first grouping across modes, defined type/date ordering, and search results that continue to follow the active sort mode after filtering (`5.1.2` and `5.1.2.1`).
  - Extend the Files model with an explicit sort mode so both browse rows and Files search results share one ordering path with folders grouped first.
  - Use canonical entity metadata for `Date added`, session-scoped Files interaction timestamps for `Date opened`, and stable name-based fallback ordering where the current file-tree schema has no timestamp for folder nodes.
  - Expose the sort choices through the existing Library Files filter/search controls, then add focused Files model coverage for name, type, date, and search-order behavior before checking this slice complete.
- [x] 2.6.3 Restore Files path, breadcrumb stack, search scope/query, selected sort, and scroll position when users leave Files for another Library subview or top-level tab and return in the same session (`5.3.4.1` and the remaining state-restoration portion of `5.3.4`).
  - Capture a Files-only session snapshot at the Library screen boundary so Files folder path, search query/scope, selected sort, and screen scroll offset are not overwritten by non-Files Library browsing.
  - Restore that snapshot when users re-enter the Files view by reapplying the saved folder/search state and then scrolling the Library screen back to the prior Files offset after the view settles.
  - Cover the capture and restore boundary rules with a focused colocated spec before checking this slice complete.
- [x] 2.6.4 Finish parent-track context presentation in Files, search, tag, and folder results while keeping dedicated track-context loop browsing fast and clearly parent-linked (`5.4` and `5.4.1` UI integration).
  - Route standalone loop-result supporting copy through one shared parent-track label formatter so Files rows and saved-loop cards use the same parent-track wording.
  - Make parent-track identity explicit in standalone Files/search/tag/folder results so containing-path metadata and source context stay distinguishable at a glance.
  - Keep track-scoped loop browsing on the existing detail surface while backfilling focused assertions for standalone parent-track context before checking this slice complete.
- [x] 2.6.5 Add focused coverage for Files search, sort, restoration, and result-group behavior, then manually verify the full Files organization/search contract end to end (`6.1.6`, `6.2.7`, and `6.2.7.3`).
  - Keep the existing focused model specs for Files search and sort as the base, then add the missing focused coverage for mixed search-result grouping and Files session restoration orchestration without widening into a broader UI harness.
  - Extract only the smallest pure restoration-transition helper needed so the Files session capture and restore contract can be asserted deterministically with folder, query, sort, and scroll state preserved.
  - Run the narrow mobile Files spec slice after the coverage lands, then use the integrated browser session to manually verify Files search scope switching, sort persistence, breadcrumb or folder restoration, and scroll restoration before checking this slice complete.
  - Automated coverage added and focused specs pass for Files search result grouping and Files session restoration orchestration.
  - PASS: Integrated browser verification from Browser Test Folder confirmed the active `twi` query, `All Files` scope label, containing-path metadata for out-of-folder results, Browser Test Folder breadcrumb or current-folder restoration, and `Type`-ordered Files results persisted after switching `Files -> Tracks -> Files` and `Library -> Add -> Library`.

### 2.7 Simplify Copy, Metadata, and Visual Prominence

- [x] 2.7.1 Simplify Recents steady-state copy so the screen prioritizes the recent rehearsal rows over explanatory text: demote or remove `Resume recent rehearsal` helper copy when recent items exist, shorten `Last played` timestamps to music-app-friendly relative or date-only labels, and move shortcut explanation copy to empty or first-use states.
  - Reduce Recents intro copy to a simple section label while recent rows are present, and keep explanatory fallback guidance only for empty or first-use states.
  - Replace exact `Last played` timestamps with compact relative-or-date labels in the Recents helper, then backfill the existing focused Recents helper specs before manual browser verification.
  - PASS: Focused Recents helper specs passed via `npx tsx --test src/app/screens/recents/history.spec.ts src/app/screens/recents/screen-copy.spec.ts`, and `npm exec -- nx run mobile-rehearsal-player:typecheck` passed after the copy and label changes.
  - PASS: A fresh integrated browser page verified the empty/first-use Recents branch now keeps shortcut explanation copy in the shortcut module while preserving the concise `No recent rehearsal yet. Start in Add or Library.` guidance.
  - NOTE: The pre-existing authenticated browser tab surfaced Metro resolver errors unrelated to this slice, and the fresh browser session had no persisted recent-history rows available, so steady-state row copy and relative/date label behavior were validated through the focused helper coverage in this checkpoint.
- [x] 2.7.2 Simplify playlist card presentation: collapse empty playlist metadata from repeated `0 items • 0 tracks • 0 loops` plus `No items yet` into one concise empty state, keep richer track/loop counts for non-empty playlists, and replace the prominent `Open playlist` text button with a quieter detail affordance or row/card-body open behavior while preserving explicit play and overflow controls.
  - Treat the existing row-body press behavior as the playlist-detail affordance and keep the explicit play and overflow controls visually primary.
  - Move empty-playlist card copy to one concise secondary label and suppress the extra preview line so empty cards do not stack redundant zero-state text.
  - Backfill focused playlist-card view-model coverage for the empty and non-empty card summaries before checking this slice complete.
  - PASS: Focused coverage passed via `npx tsx --test src/app/library/playlists/utils/saved-playlist-card-view-model.spec.ts`, and `npm exec -- nx run mobile-rehearsal-player:typecheck` plus `npm exec -- nx run mobile-rehearsal-player:lint --outputStyle=static` passed for the project.
  - PASS: Integrated browser verification in the shared localhost session showed an empty playlist card rendering as one concise `Empty playlist` secondary label with the row body opening detail and the explicit play plus overflow controls preserved.
- [x] 2.7.3 Reduce playlist-detail label and empty-state noise: demote or remove the `Playlist detail` eyebrow where the playlist title and back button already provide context, shorten empty-playlist copy, and align it with the folder-aware add-items work from task `2.5.10`.
  - Make the shared detail-card eyebrow optional, then remove the redundant playlist-detail eyebrow while preserving the title, metadata, close action, and playback controls hierarchy.
  - Tighten both the generic and Files-origin empty-playlist copy so the screen points users back to adding nearby items without the longer explanatory sentence used before.
  - Backfill the existing focused playlist-detail-origin spec for the revised empty-copy contract and run a narrow playlist detail or origin test slice before checking this task complete.
- [x] 2.7.4 Make Add Drive browse metadata quieter by hiding exact `Updated` dates in default browsing unless the user is searching, sorting by date, or inspecting item details; keep item type, saved/playable state, shared-folder status, preview, and save affordances more prominent than file-system timestamps.
  - Make updated-date inclusion explicit in the Drive metadata formatter so ordinary browse rows omit timestamps without removing format, duration, location, or availability context.
  - Opt active Drive search results into updated-date metadata through the existing Add explorer search state; leave future date-sort and item-detail callers able to opt in without introducing new controls in this slice.
  - Backfill focused metadata-helper coverage for browse-hidden and search-visible dates, then manually verify both Add states in the integrated browser before checking this task complete.
  - PASS: Focused Drive metadata coverage passed via `npm exec -- nx run mobile-rehearsal-player:test-file -- --file=src/app/library/drive/utils/drive-library-view-model.spec.ts`; project typecheck and lint also passed with only pre-existing warnings outside the touched files.
  - PASS: Integrated browser verification at 393 by 852 showed default My Drive browsing with concise folder metadata, no redundant format labels beneath filenames that already include their extension, and preserved play/save actions but no exact updated dates. Running the `Masquerade` Drive search placed exact `Updated YYYY-MM-DD` metadata directly beneath each filename while preserving playable and save/overflow actions.
  - PASS: Follow-up browser verification confirmed sources without useful secondary metadata render no `Metadata unavailable` subtitle or empty metadata node, while their playback and overflow actions remain available.
  - PASS: Follow-up browser verification confirmed ordinary folder rows rely on their folder icon and name without a redundant `Folder` subtitle, while shared folders retain the meaningful `Shared folder` state label.
- [x] 2.7.5 Tighten Files explorer chrome hierarchy by reducing duplicate current-folder naming between the navigation title and breadcrumb bar, for example by making breadcrumbs smaller or omitting the disabled final breadcrumb segment while preserving parent-folder jump targets.
  - Keep the current folder name in the Files navigation bar and omit that disabled terminal segment from the Files breadcrumb row so the chrome has one clear location title.
  - Preserve every ancestor breadcrumb as a direct folder-jump target, and hide the breadcrumb row at Files root where no ancestor path remains.
  - Extend the focused Files view-model coverage, then manually verify root and nested-folder hierarchy plus ancestor navigation in the integrated browser.
  - PASS: Focused Files view-model coverage, app typecheck, project lint, and touched-file Problems checks passed. The broader `typecheck-tests` target remains blocked by pre-existing errors in unrelated specs and reports no errors in the touched Files files.
  - PASS: Integrated browser verification showed no breadcrumb row at Files root, a single `Browser Test Folder` navigation title in the nested folder, and an enabled `Library` ancestor breadcrumb that returned directly to root.
- [x] 2.7.6 Quiet shared options-sheet titles so menu surfaces emphasize the affected item and actions rather than a repeated `More options` label. Preserve accessible labels and sheet context, but make the generic menu title smaller, removable, or consistently secondary across Files, Recents, playlist, source, and loop menus.
  - Remove the shared visible `More options` eyebrow from the options-sheet shell while retaining each caller's affected-item title as the sheet's visual context and preserving accessible labels on overflow triggers and actions.
  - Verify representative Files, Recents, playlist, source, and loop callers provide meaningful item titles, and add the narrowest rendered-shell coverage available for the revised title hierarchy.
  - Run focused options-sheet coverage, project typecheck and lint, then manually verify representative overflow menus in the integrated browser before checking this task complete.
- [x] 2.7.7 Simplify mini-player text hierarchy during track, loop, and playlist playback. Avoid repeating the active title across adjacent lines, reserve secondary text for playback state/source/queue context, and shorten long loop context so the mini-player remains scannable while preserving accessibility labels.
  - Collapse the mini-player's visible copy to the active item title plus one compact context line: state and progress for standalone tracks, state and parent source for loops, and state plus queue position for playlist or transient-queue playback.
  - Keep complete playback state, progress, collection, queue-mode, and loop-range context in the mini-player body's accessibility label even when those details are intentionally omitted from the compact visual line.
  - Backfill focused shell-model coverage for standalone track, loop, and playlist summaries, then manually verify the three playback contexts in the integrated browser before checking this task complete.
  - PASS: Focused shell summary coverage passed with 14 tests; mobile typecheck passed; project lint passed with only 10 pre-existing warnings outside the touched slice; all touched-file Problems checks are clean.
  - PASS: Integrated browser verification showed standalone track copy as title plus `Playing • <progress>`, auto-named loop copy as title plus `Playing • Saved loop` without repeating its long parent source, paused loop state in the same hierarchy, and playlist-backed copy as title plus `Playing • Twi • 1 of 1`. Accessible mini-player labels retained full progress, source, queue mode, repeat mode, and loop-range context.
- [x] 2.7.8 Move playback status messaging out of layout-shifting Library content cards and into standard non-disruptive surfaces. Route paused, ready, preparing/loading, and success feedback through the mini-player, snackbar/toast-style feedback, or a notification/status menu as appropriate; reserve prominent inline cards for blocked or error states that require user action; and ensure transient playback status never pushes Library browsing content down or changes row positions during normal playback transitions.
  - Extract a pure Library status-card visibility model so routine saved-library loading/readiness and playback preparing/paused/ready/success states cannot insert cards above browse content, while playback errors and saved-library warning/error recovery states remain prominent.
  - Preserve settled empty-library guidance as an actual empty state, keep per-row playback failures attached to their affected item, and rely on the persistent mini-player for routine active playback state instead of duplicating it in Library content.
  - Add focused visibility-model coverage for loading, ready, paused, empty, warning, and error states, then run the narrow spec, mobile typecheck and lint before manually verifying stable Library row placement through playback transitions in the integrated browser.
  - Checkpoint revision: keep long loop names at intrinsic width inside the mini-player marquee and calculate the scroll cycle from measured title width so the full name reaches the viewport before the duplicate title begins.
- [x] 2.7.9 Make the loop builder feel isolated from Library browse content while it is open. Avoid injecting or expanding a full Saved loops section behind the builder during Files-launched loop creation, preserve the originating Files folder context for save placement, and keep creation focus on range selection, preview, and save.
  - Add an explicit builder-focused presentation state at the Library composition boundary when Files launches loop creation, without changing the loop editor or modal lifecycle.
  - Suppress the Saved loops heading, status, and list while that focused builder is open, while keeping the Files explorer mounted behind the modal and preserving normal loop browsing in dedicated views.
  - Extend focused visibility and Files save-location coverage, then manually verify that a loop launched from a nested Files folder saves back into that folder without expanding Saved loops behind the builder.
  - PASS: Focused loop-section visibility and Files save-location specs passed; mobile typecheck passed; project lint passed with only pre-existing warnings in untouched files; touched-file Problems checks were clean.
  - PASS: Integrated browser verification launched `Make loop` from a track in `Browser Test Folder`, showed range/preview/save content with no visible Saved loops heading or list behind the modal, and saved the temporary loop back into `Browser Test Folder` with destination feedback. The temporary loop was removed after verification.
- [x] 2.7.10 Add musician-friendly loop creation shortcuts after the baseline loop builder is stable: set start or end from current playback position, nudge start/end boundaries, and expose quick range-adjustment controls without making the default builder visually heavier.
  - UX decisions confirmed with user: nudge step is 250ms per tap (finer than the existing 1s slider step); "Set start/end to current position" is enabled only while the in-builder draft-range preview is actively playing (`previewTimeline.canScrub`), not from the full source track playing elsewhere, and applies identically whether creating a new loop or editing an existing one.
  - Add pure helpers in `saved-loop-builder-view-model.ts`: `LOOP_BUILDER_NUDGE_STEP_MS`, `nudgeLoopBuilderBoundary` (clamped independent start/end nudge preserving a minimum gap), and `resolveLoopBuilderBoundaryFromPlaybackPosition` (captures the live preview position into a boundary with the same clamping).
  - Wire new `handleNudgeBoundary` / `handleSetBoundaryToCurrentPosition` actions through `use-saved-loop-section-state.ts`, reusing `updateLoopBuilderDraftRange` so suggested-name syncing stays consistent with slider-driven changes.
  - Render compact nudge (−/+) controls flanking the Start/End chips in `loop-range-selector-range-card.tsx`, and add "Set start here" / "Set end here" quick actions in `loop-range-selector-preview-card.tsx`, enabled only while `previewTimeline.canScrub` is true, so the default builder gains controls without new top-level rows.
  - Add focused pure-function coverage in `saved-loop-builder-view-model.spec.ts` for nudge clamping at both boundaries and position-capture clamping.
- [x] 2.7.11 Group long overflow menus into lightweight sections or visual separators where action count is high, especially Files track menus. Preserve the canonical ordering, but make playback, rehearsal, organization, and destructive actions easier to scan without hiding frequently used queue, loop, playlist, copy, rename, move, and remove actions.
  - Added an optional `section` grouping key to the shared `OptionsMenuAction` contract and a pure `resolveOptionsMenuSheetSectionDividers` helper in `options-menu-sheet/model.ts` that only inserts a divider where the declared section changes, so unsectioned menus keep rendering as one unbroken list.
  - `OptionsMenuSheet` renders a lightweight hairline divider before the first action of each new section, with no new text labels, keeping canonical action order untouched.
  - Wired Files track, loop, playlist, and folder menu resolvers through a shared `attachRowActionSections` helper (new `options-menu-sheet/row-action-sections.ts`) mapping each canonical label to `rehearsal` / `organize` / `destructive`, matching the design.md row-action ordering contract (rehearsal actions first, file-management next, destructive last).
  - Extended the same shared helper to the dedicated Library Tracks view (`drive-library-source-group.tsx`, via `BrowseSourceGroup`) and dedicated Library Loops view (`saved-loop-list-row.tsx`), since both already reuse `resolveSavedTrackRowActions`/`resolveSavedLoopRowActions` and their menus overlap Files' rehearsal/organize/destructive vocabulary — this keeps Tracks and Loops menu grouping visually consistent with Files instead of Files-only. Playlist cards/detail rows, Add/Drive rows, and Recents rows were reviewed and left flat/unsectioned: each is a short (2-4 item), bespoke action set with no shared vocabulary, so dividers wouldn't add scanability there.
  - Added focused coverage: `options-menu-sheet/model.spec.ts` for the divider helper, `options-menu-sheet/row-action-sections.spec.ts` for the shared label-to-section map, and a `section` assertion on the Files track-menu spec in `files-row-actions.spec.ts`. Live manual verification (auth-gated) is deferred to task `2.7.15`/`2.8.1`.
- [x] 2.7.12 Reduce default playlist-detail row management density. Keep direct play available, but consider moving drag handles and up/down controls behind an explicit reorder affordance or visually quieting them so playlist detail reads as a listening surface before it reads as an editor.
  - UX decision confirmed with user: visually quiet the drag handle and up/down controls rather than gating them behind a new toggle, keeping design.md's "no separate edit mode" decision intact (`design.md` §10).
  - Reordered the playlist-detail row control strip to play-button, spacer, then a tightly grouped drag-handle + up/down cluster, then the overflow trigger, so play and overflow read as the primary actions. Dropped the drag handle/step-button pill chrome (border + background) and shrank their icons, reusing `PLAYLIST_SECONDARY_TEXT` for a muted icon color instead of the prior full-contrast colors, while keeping their touch targets and enabled/disabled/pressed behavior unchanged.
  - Live browser follow-up feedback: removed the redundant `READY` row status label (the default state for most rows, so it read as noise) while preserving `Current`/`Playing`/`Unavailable` labels, which still carry real state information.
  - No model/logic changes were needed (`playlist-detail-row-controls-model.ts` and its 3 passing specs are unchanged); this slice is a pure layout/style/copy change with no practical automated render coverage available in this codebase (no component-render test harness exists anywhere in the mobile app). Verified manually in the integrated browser: reorder controls are visually quiet and still functional (up/down swap confirmed), overflow menu still opens with `Move to position`/`Remove from playlist`, and row status labels correctly show nothing for `Ready`, and still show `Current`/`Playing` during active/paused playback.
  - Follow-up redesign after live user feedback with YouTube/YouTube Music queue references (drag handle was in a non-standard mid-row position and rows were too tall — only ~3 fit on screen): confirmed with user to keep a compact combined up/down stepper (not drop it) rather than matching the references exactly. Collapsed the two-block row (control row + copy block) into one single-line row: leading drag handle, compact `SurfaceIconButton` play/pause, flexible title+metadata column (title truncated to 1 line, status text inlined into the metadata line instead of a separate header row), a compact bordered two-cell up/down stepper, then the overflow trigger — all in `playlist-detail-row-controls.tsx`. Shrank `itemCard` padding and removed the now-dead `itemHeaderRow`/`itemStatusReady` styles in `section-card-styles.ts`. Re-ran typecheck, lint (clean on touched files), and the model spec (3/3 passing, logic untouched), then manually verified in the integrated browser against the user's actual 3-item playlist: all 3 items now fit comfortably with room to spare, up/down reorder and the overflow menu still work, and disabled-state dimming on the stepper is correct at both list boundaries. Reverted the reorder used for verification back to the original order afterward.
  - Second follow-up per live feedback: play/overflow controls were still visibly larger than needed and the drag handle sat too far from the card edge. Shrank `playlistRowPlayButton` (38→30px) and the `OverflowMenuTrigger` override (added explicit smaller `minWidth`/`minHeight`/`paddingHorizontal` via `playlistRowOverflowTrigger`, plus a smaller `iconSize` prop), shrank the drag handle (26→20px) and stepper (26→22px) with correspondingly smaller icon-size constants, tightened `playlistRowShell` gap (8→4), and reduced `itemCard` padding (12/8→6/6 horizontal/vertical) so the whole row hugs the card edges. Typecheck, lint (clean on touched files), and the model spec (3/3) re-verified; manually re-verified reorder and overflow menu still work at the smaller sizes against the same real playlist, reverting the test reorder afterward.
- [x] 2.7.13 Clarify Now Playing repeat controls by presenting repeat state as one mode control or a clearly selected segmented control instead of separate adjacent `Repeat off` and `Repeat one` actions that can read as independent buttons.
  - Replaced the per-mode `ModeButton` row for repeat options with a new `RepeatModeSegmentedControl` in `playback-session-mode-card.tsx`: one shared bordered/rounded pill container (`accessibilityRole="radiogroup"`, label `Repeat mode`) with segments separated by hairline dividers instead of gaps, each segment using `accessibilityRole="radio"` with `selected` state, so it reads as one control both visually and to assistive tech instead of two independent buttons.
  - No logic change to `resolveVisibleRepeatModes`/`REPEAT_MODE_OPTIONS`; only the repeat-row rendering and styles changed. The shuffle `ModeButton` and its group row are unchanged.
  - No automated render coverage exists for this component (no component-render test harness exists anywhere in this app, per the same finding recorded in task `2.7.12`), and this slice has no new pure logic beyond the already-tested `resolveVisibleRepeatModes` filtering, so this was verified manually instead of via a new spec.
  - PASS: `npm exec -- nx run mobile-rehearsal-player:typecheck` and `npm exec -- nx run mobile-rehearsal-player:lint` both passed clean on the touched file (only pre-existing unrelated warnings elsewhere). Formatted via `nx format:write` scoped to the touched file.
  - PASS: Integrated browser verification on the Now Playing sheet (standalone playback, `off`/`one` modes visible) showed the repeat control as one continuous bordered pill with a hairline divider and no gap between segments; tapping `Repeat one` moved the highlighted fill to that segment and back; the accessibility tree reported `radio "Repeat off"` / `radio "Repeat one"` confirming the mode-control semantics.
- [x] 2.7.14 Add destination context to Files-launched playlist creation dialogs. When the create flow starts from a folder, show the target folder in the dialog title or secondary line, for example `Create a playlist in Browser Test Folder`, while preserving the generic Library Playlists create dialog outside Files.
  - `getSavedPlaylistCreateDialogCopy` in `saved-playlist-view-model.ts` now takes an optional `{ destinationFolderName }` and renders `Create a playlist in ${destinationFolderName}` as the title when given, falling back to the original `Create playlist` title otherwise.
  - `SavedPlaylistCreateDialog` gained an optional `destinationFolderName` prop threaded into that copy call; `library-files-create-controls.tsx` passes its existing `filesFolderLabel` (already used for the create-menu title and toasts) into the dialog, while the generic Library Playlists create control (`library-playlist-create-controls.tsx`) is unchanged and keeps the plain `Create playlist` title.
  - PASS: Added focused cases to `saved-playlist-view-model.spec.ts` for the destination-folder title and the no-destination fallback; `npm exec -- nx run mobile-rehearsal-player:test-file -- --file=src/app/library/playlists/utils/saved-playlist-view-model.spec.ts` (9/9), project typecheck, and project lint (clean on touched files) all passed.
  - PASS: Integrated browser verification showed `Create a playlist in Library` from the Files `+` menu at Files root, and `Create a playlist in Verify 2714` inside a temporary nested test folder (folder removed after verification); the dedicated Library Playlists view `+` still opens the unchanged generic `Create playlist` dialog.
- [x] 2.7.14.1 Fix a pre-existing race condition, surfaced during `2.7.14` manual verification, where a Files-launched playlist creation inside a nested folder appeared at Library Files root in the UI instead of the target folder, even though the underlying storage write was already correct.
  - Root cause: `useLibraryFiles`'s `canonicalIdsKey`-triggered `refresh()` effect (fired whenever the saved-playlists list changes, including right after a new playlist is created) and the explicit Files create-in-folder move (`saveLibraryFileLink`) both read-then-conditionally-write the same owner's library file tree in `practice-repository-storage.ts` without any serialization. The refresh's read could return a pre-move snapshot and its `setTree(...)` call could still apply after the move's own `setTree(...)`, silently reverting the in-memory/UI tree even though AsyncStorage itself was never corrupted (confirmed via direct localStorage inspection and a full-reload repro that showed the correct nested placement).
  - Fix: added a per-owner async serialization queue (`runSerializedLibraryFileTreeOperation`) in `practice-repository-storage.ts`, applied to the two shared entry points for all library-file-tree reads/writes (`persistSynchronizedLibraryFileTree` and `writeStoredLibraryFileTree`), with `persistSynchronizedLibraryFileTree`'s internal write routed through a private raw writer to avoid self-reentrant deadlock. This guarantees a `refresh()`-style read can never interleave with a concurrent explicit mutation for the same owner, regardless of dispatch order.
  - PASS: Added `practice-repository-storage.spec.ts` with two focused tests — mutual-exclusion (max concurrent library-tree storage calls per owner is 1) and a deterministic repro of the exact reported race (a "move" write dispatched immediately before a "refresh" read, both unawaited) — both verified to fail without the fix (via a temporary revert) and pass with it. Full `audio-library-runtime` suite (28/28) and `mobile-rehearsal-player` suite (364/364) pass; project typecheck and lint clean on touched files (only pre-existing unrelated warnings elsewhere).
  - PASS: Live browser re-verification of the exact reported scenario (create a folder, create a playlist inside it via the Files `+` menu) showed the new playlist appearing correctly inside the target folder immediately, with no page reload needed and no stray duplicate at root.
- [x] 2.7.15 Add focused UI/model coverage for the copy and hierarchy simplifications where behavior or rendered labels change, then manually verify Recents, Add browsing, Library Files, playlist cards, playlist detail, Now Playing, loop creation, playlist creation, and representative overflow menus for cleaner hierarchy and cross-screen consistency.
  - Audited every `2.7.x` subtask's own completion notes: each already added focused automated coverage in its own slice (confirmed via full `mobile-rehearsal-player:test` run: 364/364 passing, and typecheck clean) except `2.7.12` and `2.7.13`, which explicitly documented no component-render test harness exists anywhere in this app and were verified manually within their own tasks already. No further automated-coverage gap was found, so this slice's remaining scope was the cross-screen live-verification pass explicitly deferred from `2.7.11` ("deferred to task `2.7.15`/`2.8.1`").
  - PASS: Live browser verification (authenticated session) confirmed, in one pass: `2.7.11`/`2.7.6` Files track overflow menu renders `rehearsal` (Play next, Add to queue once a queue context exists, Make loop, Add to playlist) / `organize` (Create a copy, Edit tags, Rename, Move to folder) / `destructive` (Delete from folder, Remove from library) sections with hairline dividers and the item name (not `More options`) as the sheet title; `2.7.7` mini-player shows title plus one compact `Playing • progress` line with full context preserved in the accessibility label; `2.7.13` Now Playing repeat control renders as one bordered segmented pill exposing `radio "Repeat off"` / `radio "Repeat one"`; `2.7.1`/`3.3.1`/`3.3.4` Recents shows concise `Recent rehearsal` labeling, relative `Last played` timestamps, icon-only play, and a `Play next` / `Add to queue` / `View in library` overflow; `2.1.1`/`2.7.4` Add header keeps leading `Refresh` before `Search`/session-menu and hides exact dates on default folder browsing; `2.7.14` playlist creation from the dedicated Library Playlists `+` still opens the generic `Create playlist` title; `2.7.2` empty playlist cards show one concise `Empty playlist` label; `2.7.3` playlist detail has no `Playlist detail` eyebrow and icon-first `Ordered`/`Shuffle` controls; `2.5.10`/`2.5.11` `Add items` opens the Files picker with root-folder fallback context and stays reachable after the first item is added; `2.7.12` playlist-detail rows render as one quiet compact line with no redundant `READY` status noise; the playlist-card/detail overflow's `Remove playlist` shows an explicit impact-confirmation dialog; and `2.7.9`/`2.7.10` the loop builder opens isolated (no Saved loops list behind the modal) with nudge and `Set start/end here` controls. All test artifacts created during verification (a temporary playlist and its item) were removed afterward; no defects or cross-screen inconsistencies were found.

### 2.8 Run Final Regression and Close Out the Change

- [x] 2.8.1 Run the full rehearsal-critical manual regression across Recents, Add, Library, playlist detail, now-playing, queue, Files organization, and loop browsing after all prior steps have landed together (`6.2` through `6.2.7.3` as a final sweep).
  - PASS: Live browser verification (authenticated session, real Google Drive library) covered all seven surfaces end to end: Recents (shortcut card, relative `Last played` labels, icon-only play, `Play next`/`Add to queue`/`View in library` overflow), Add (My Drive/Shared root switching, folder drill-down, breadcrumbs, preview-without-save playback, save, Drive session menu), Library Files/Tracks/Loops/Playlists views with filters and sort, playlist detail (icon-first Ordered/Shuffle, button-based and multi-step drag reorder confirmed via a granular synthetic pointer sequence, `Move to position` modal, active-session copy), Now Playing/Queue (mini-player, `RepeatModeSegmentedControl` exposing `radio "Repeat off/one/all"`, prev/next transport, row play/drag/overflow, `Create new playlist`/`Update current playlist` confirmation), Files organization (folder create, move with folder-browser picker, `Create a copy` hard-link with unique-suffix naming, rename with inline duplicate-name recovery), and loop browsing (isolated builder, 250ms nudge controls, `Set start/end here` from live preview position, track-scoped loop view with parent-track linkage).
  - FAIL (4 confirmed defects, reproduced live, not present in any prior checkpoint's PASS notes):
    1. **Drive search scope silently drops on root switch.** Typing a query under My Drive then switching to Shared folders (or back) leaves the query text in the input but the results fall back to unfiltered browsing with no highlighting. Root cause traced to `selectRoot()` in `use-drive-library.ts`, which unconditionally calls `deactivateSearch()` (clearing `activeSearchQuery`) without clearing or reapplying the still-visible input text. This is pre-existing baseline behavior (predates this change, commit `824611e`) but directly contradicts task `3.6`'s "keep root-level search at selected-root scope," which is checked off as complete.
    2. **Last-link `Delete from folder` does not remove the item from the library.** The confirmation dialog correctly identifies the last-link case ("will also remove the saved item from your rehearsal library," `Delete item from library` button), but after confirming, the track remains fully present, `PLAYABLE`, and still listed in the playlist that referenced it. Contradicts task `2.5.5`'s cascade-cleanup verification notes.
    3. **Saved-loop `Remove` is a silent no-op on web.** Reproduced 3+ times from both the top-level Loops view and the track-scoped loop view: tapping `Remove` in a saved loop's overflow menu closes the menu with no confirmation, no error, and no deletion. Root cause confirmed by source trace: `confirmRemoveLoop` (`use-saved-rehearsal-library-removal-actions.ts:32-48`) calls `Alert.alert(...)` and only invokes `deleteLoop` from the destructive button's `onPress`. On the web target, `react-native-web`'s `Alert.alert` is a complete no-op stub (`static alert() {}`), so the dialog never renders and `onPress` — and therefore `deleteLoop` — is never called. The web build has no working loop-removal path at all.
    4. **Playlist-card `Remove playlist` is a silent no-op on web, same root cause as finding 3.** Tapping it closes the menu with no confirmation dialog and no removal, contradicting task `2.7.15`'s verification note that this action "shows an explicit impact-confirmation dialog." Source trace: `handleDeletePlaylist` (`use-saved-rehearsal-library-playlist-state.ts:136-160`) has the identical `Alert.alert(...)` → destructive-button-only `deletePlaylist` pattern, which is unreachable on web for the same reason as finding 3. (The underlying storage write path — `runSerializedLibraryFileTreeOperation` in `practice-repository-storage.ts:37-54`, added by task `2.7.14.1` — was independently reviewed and ruled out; `deleteLoop`/`deletePlaylist` are never even called, so the serialization queue is never reached.) Both would work correctly on native iOS/Android, where `Alert.alert` is functional; this is a web-target-only regression. A workspace grep for `Alert.alert` found exactly three call sites, all with the identical destructive-only-`onPress` pattern: the two above, plus `confirmRemoveSource`/a second `confirmRemoveLoop` in `use-drive-library-saved-library-actions.ts:34-49,50-83` (saved-source/track removal from the Add-tab library actions). That third site was not independently live-reproduced in this sweep, but shares the exact same code pattern and is very likely equally broken on web. Fix direction: replace `Alert.alert` with a web-compatible confirm modal (the app already has a shared dialog-card/options-menu-sheet primitive from task `4.7`/`4.7.2` that could host this) across all three call sites, rather than patching one at a time.
  - NOTE (lower-severity, not blocking): the dedicated Library Tracks-view saved-track overflow menu uses a different action order and a highlighted primary `Add to playlist` button, unlike the Files-view menu's `Play next`/`Add to queue`/`Make loop`/`Add to playlist` ordering with rehearsal/organize/destructive sections from task `2.7.11`. Worth a follow-up consistency pass but does not block any capability.
- [x] 2.8.2 Explicitly verify that no critical capability from the baseline mapping in task `1.1` regressed and record pass/fail by capability (`6.3` final pass).
  - PASS — Recents: hero/status summary, continue-rehearsal shortcut, recent-item history, shortcut chips.
  - PASS — Add: Drive search query/submit/clear, Drive search result list (playable + unavailable), search status/loading messaging, save action. Remove action shares the `Alert.alert` web defect below and is not confirmed working.
  - PASS — Library: saved tracks list and playback-first rows, saved loops list and loop playback controls, loop creation entry point, playlist browse cards and detail entry, playlist playback start/toggle, add-to-playlist from row overflow.
  - PASS — Cross-surface: top-level tab switching, Drive session menu, mini-player persistence across tabs, Now Playing entry point, Queue/Up Next entry point, global transport controls.
  - PASS (per `1.3` acceptance criteria) — Drive root switching, folder navigation, explorer row primary actions, breadcrumb navigation, track playback, loop creation and editing (creation/edit only — see FAIL below), track/loop row parity, playlist playback, queue-to-playlist persistence from Up Next, active rehearsal queue at long lengths.
  - FAIL — **Source save/remove**: save is intact; the last-link `Delete from folder` removal path is broken (see `2.8.1` finding 2), and the Add-tab saved-source `Remove` action shares the `Alert.alert` web defect from findings 3/4 (not independently live-reproduced, but same code pattern in `use-drive-library-saved-library-actions.ts`). Capability is not fully preserved.
  - FAIL — **Loop creation and editing**: creation, editing, nudge, and preview-position capture all pass; loop removal is broken (see `2.8.1` finding 3). Capability is only partially preserved.
  - FAIL (not in the original `1.1`/`1.3` list, but a Library-baseline playlist-management capability) — **Playlist removal**: `Remove playlist` is broken (see `2.8.1` finding 4).
  - Everything else in the `1.1` baseline mapping and `1.3` acceptance criteria passed without regression.
- [x] 2.8.3 Compare the final app surfaces against the proposal, design, and delta specs, and capture any intentional implementation deltas before considering the change complete (`6.4`).
  - The implemented IA, shell, search, playlist/queue, Files-organization, and copy/hierarchy surfaces match the proposal, design, and delta specs closely — no intentional deltas beyond what earlier checkpoints already recorded (e.g. the `2.7.12`/`2.7.13` compact-row and segmented-control redesigns, both confirmed against design.md language).
  - The four defects recorded in `2.8.1`/`2.8.2` are **not** intentional deltas — they are regressions against explicit design.md acceptance criteria (`Source save/remove`, `Loop creation and editing`) and against other tasks' own completion claims (`2.5.5`, `2.7.15`, `3.6`). Per design.md's Acceptance Gate ("Any failed capability blocks final sign-off until either fixed or documented as an intentional, approved delta"), none of the four qualify as an approved delta, so final sign-off/archive remains blocked pending a fix-or-accept decision from the user.
  - The Tracks-view vs. Files-view menu-ordering inconsistency noted in `2.8.1` is a minor, non-blocking cosmetic delta worth a follow-up pass but does not gate sign-off.
- [x] 2.8.4 Create the final tested checkpoint commit or release-candidate handoff after the full regression sweep.
  - This checkpoint commits the regression-sweep documentation above. It is a **documentation checkpoint, not a clean release-candidate sign-off**: four defects are open (drive-search-scope-on-root-switch, last-link delete not removing from library, loop removal no-op, playlist removal no-op) and require an explicit fix-or-accept decision before the change can be archived per design.md's Acceptance Gate.

### 2.9 Fix Defects Found During the Final Regression Sweep

Each `2.9.x` checkbox is one checkpoint. Fix the underlying behavior, add or update automated coverage for the corrected path, and re-verify manually where the existing surface has no practical automated coverage, consistent with the rest of this file's execution discipline.

- [x] 2.9.1 Replace `Alert.alert` with a web-functional confirm flow for saved-loop removal, playlist removal, and saved-source removal. `react-native-web`'s `Alert.alert` is a no-op stub, so the destructive-confirm button whose `onPress` calls `deleteLoop`/`deletePlaylist`/`removeSource` never fires on web, making all three removal actions silent no-ops on that platform. Cover `confirmRemoveLoop` in `use-saved-rehearsal-library-removal-actions.ts`, `handleDeletePlaylist` in `use-saved-rehearsal-library-playlist-state.ts`, and `confirmRemoveLoop`/`confirmRemoveSource` in `use-drive-library-saved-library-actions.ts`. Reuse the existing shared dialog-card/options-menu-sheet primitive (task `4.7`/`4.7.2`) for the confirmation UI rather than three separate implementations, and confirm native iOS/Android behavior is unaffected (native `Alert.alert` already works there).
  - Reused the existing `useLibraryFilesConfirmationFlow`/`LibraryFilesConfirmationDialog` pair (a `CenteredDialogCard`-based confirm flow already built on the shared dialog-card primitive) in all three hooks instead of adding a fourth confirmation implementation, following the precedent already set by `use-saved-playlist-section-state.ts`'s playlist-detail deletion/item-removal flow, which reuses the same hook outside Library Files.
  - Each hook now returns a `confirmationDialog` element alongside its existing actions: `use-saved-rehearsal-library-removal-actions.ts` and `use-drive-library-saved-library-actions.ts` for loop/source removal, and `use-saved-rehearsal-library-playlist-state.ts` for playlist-card removal.
  - Wired rendering: `use-rehearsal-library-controller.ts` now exposes a top-level `confirmationDialog` (from the removal-actions hook), threaded through `app-router.tsx` into a new `savedLibraryConfirmationDialog` prop on `MobileShell`, rendered once at the shell level so it covers both the Add and Library tabs that share the one controller instance. The playlist-card removal dialog is rendered directly in `SavedRehearsalLibrarySection` alongside the other section-level dialogs.
  - `use-drive-library-saved-library-actions.ts` and its owning `DriveLibrarySection` component are dead code (nothing in the app imports `DriveLibrarySection`); fixed for correctness/consistency per the task's explicit file list, but there is no live surface to render its `confirmationDialog` on today.
  - No automated coverage added: these are hook-level UI wiring changes reusing an existing, already-tested dialog primitive, and this codebase has no component-render test harness (documented repeatedly in prior `2.7.x` checkpoints). Verified manually instead.
  - Native `Alert.alert` was not preserved as a platform branch; the same `CenteredDialogCard`-based flow now runs on both platforms, matching the already-shipped cross-platform pattern for Files `Delete from folder` and playlist-detail removal (neither of which special-cases native).
  - PASS: Live browser verification (web) confirmed all three previously-broken flows: saved-loop `Remove` (Loops view) now shows "Remove saved loop?" and successfully deletes; playlist-card `Remove playlist` now shows "Remove saved playlist?" with impact copy and successfully deletes; saved-track `Remove` (Tracks view) now shows "Remove saved track?" (cancelled after confirming the dialog renders, to leave a fixture track in place).
  - NOTE: `npm exec -- nx run mobile-rehearsal-player:typecheck` and `lint` both pass with 0 errors. Two touched files (`saved-rehearsal-library-section/index.tsx`, `app-router.tsx`) trip the `max-lines` warning, but both were already over the 300-line limit before this change (303 and 338 lines respectively); this change added one line to each. Left as pre-existing debt rather than triggering an unrelated extraction refactor; worth a follow-up cleanup pass.
- [x] 2.9.2 Fix last-link `Delete from folder` so it actually removes the underlying entity from the library. The confirmation dialog and success toast both currently claim full removal ("will also remove the saved item from your rehearsal library"), but the track remains fully present, `PLAYABLE`, and still referenced by any playlist that used it. Trace the last-link deletion path in the Files mutation/storage layer (`5.3.3`/`5.3.3.2` implementation from task `2.5.3`) to find where it diverges from the explicit `Remove from library` action's cascade cleanup (task `2.5.5`), and align the two paths.
  - Root cause: the storage-layer cascade (`deleteLibraryFileLinkFromRepository` in `async-storage-practice-repository-helpers.ts`, calling `deleteSource`/`deleteLoop`/`deletePlaylist` on last-link deletion) was already correct — AsyncStorage was genuinely mutated. The bug was one layer up: `useLibraryFiles`'s `deleteFileLink` only updated its own `tree` state after a successful delete, never refreshing the sibling `savedSources`/`savedLoops`/`savedPlaylists` React caches owned by `useSavedRehearsalLibrary`/`useSavedLoops`/`useSavedPlaylists`, unlike `confirmRemoveSource` (task `2.5.5`'s working path), which explicitly awaits `refreshLoops()`/`refreshPlaylists()` after removal.
  - Fix: added `refreshSources` to `useSavedRehearsalLibrary` (mirroring the existing `refreshLoops`/`refreshPlaylists` pattern), added a `LibraryFilesEntityRefreshCallbacks` type (`refreshSavedSources`/`refreshSavedLoops`/`refreshSavedPlaylists`) threaded through `useLibraryFiles`'s options, and wired `use-rehearsal-library-controller.ts` to supply the three sibling hooks' refresh functions. `deleteFileLink` in `library-files-operations.ts` now looks up the target file link's `entityKind` and last-link status before deleting, then — only when it was the last link — refreshes the appropriate sibling cache(s): `savedSources`+`savedLoops`+`savedPlaylists` for a track (matching `deleteSource`'s full cascade), `savedLoops` only for a loop, `savedPlaylists` only for a playlist.
  - Also fixed the same `useLibraryFiles` call in the dead/unimported `drive-library-section/saved-library-panel.tsx` (found via typecheck) so the whole app typechecks; left the file otherwise untouched as it has no live import path (per task `2.9.1`'s same finding).
  - PASS: Added 2 focused tests to `library-files-operations.spec.ts` — last-link track delete refreshes all three sibling caches, and non-last-link delete refreshes none. Updated the file's other 6 `createLibraryFilesOperations` call sites with no-op refresh stubs. Full suite: `library-files-operations.spec.ts` 10/10, full `mobile-rehearsal-player:test` 366/366, `typecheck` and `lint` clean (0 errors; only pre-existing `max-lines` warnings on files that were already over 300 lines before this change — `library-files-operations.ts` (438→443), its spec (411→489), and `drive-library-section/index.tsx` (309→311) — left as pre-existing debt per the same precedent `2.9.1` already established).
  - PASS: Live browser verification (authenticated session) reproduced the exact reported bug on a real last-link track (`This Masquerade.m4a`), confirmed the "Delete last link from folder?" dialog, confirmed the fix: the track disappeared from Files, the Tracks view correctly showed "No saved tracks yet" instead of the stale `PLAYABLE` track, and the removal persisted across a full page reload (confirming AsyncStorage was correctly synced, not just in-memory state).
- [x] 2.9.3 Fix Drive discovery search losing its active filter when the user switches Drive roots (My Drive ↔ Shared folders) mid-query. `selectRoot()` in `use-drive-library.ts` unconditionally calls `deactivateSearch()`, clearing `activeSearchQuery` while leaving the input text visible, so results silently fall back to unfiltered root browsing. Either clear the input text along with the search state on root switch, or re-run the query against the newly selected root, so behavior matches task `3.6`'s "keep root-level search at selected-root scope."
  - Removed the `deactivateSearch()` call from `selectRoot()` in `use-drive-library.ts`. `activeSearchQuery` now survives the root switch, and the existing discovery `useEffect` (already keyed on `currentLocation.rootKind`/`.id`/`.kind` alongside `activeSearchQuery`) naturally re-runs `searchDriveAudioFiles` against the newly selected root, matching design.md's "at root level, search runs across the selected Drive root" scope contract. `openFolder`/`goToLocation` still call `deactivateSearch()` unchanged, since drill-down scoping is a separate, already-shipped behavior (task `3.6`) not affected by this defect.
  - No automated coverage added: this hook has no render/effect test harness in the codebase (same precedent recorded in `2.9.1`/prior `2.7.x` checkpoints — no component-render or hook-render harness exists), and the fix is a one-line removal with no new pure logic to extract. Verified manually instead.
  - PASS: `npm exec -- nx run mobile-rehearsal-player:typecheck` and `npm exec -- nx run mobile-rehearsal-player:lint --outputStyle=static` both passed clean on the touched file (only pre-existing unrelated `max-lines` warnings elsewhere).
  - PASS: Live browser verification (authenticated session) reproduced the reported bug path: searched `masquerade` under My Drive (5 results), switched to Shared folders — search stayed active, the header/scope label updated to `Shared folders`, and results correctly showed `No search results` for that root (no stray unfiltered browse). Switching back to My Drive re-ran the same query and correctly showed all 5 matches again.
- [ ] 2.9.4 Align the dedicated Library Tracks-view saved-track overflow menu with the Files-view shared overflow ordering and rehearsal/organize/destructive sectioning from task `2.7.11`, instead of its current distinct order and highlighted-primary-action (`Add to playlist`) styling. Lower priority than `2.9.1`–`2.9.3`; does not block archiving on its own.
- [ ] 2.9.5 Restrict the dedicated Library Tracks view to saved tracks only, removing its "Saved loops" section so it matches how the Loops and Playlists dedicated views already behave (each shows exactly one entity type; only the unified Files view mixes entity types, per the `1.2`/`3.1` IA plan). Keep the saved-track `View track loops` overflow action as the track-scoped loop entry point. This reverses design.md's `4.4.1` guidance ("Keep the default top-level Saved loops Library section available for cross-track access"), so update that language in design.md to reflect the narrowed decision as part of this task, before or alongside the implementation change.
- [ ] 2.9.6 Hide or disable the Library `Files`/`Tracks`/`Loops`/`Playlists` view-switcher while a playlist create/rename dialog, the loop builder, or a tag editor is open, so users cannot switch views out from under an active edit. Extend the builder-focused isolation state already used to suppress the Saved loops section behind the loop builder (task `2.7.9`) to also cover the view-switcher row, and apply the same treatment to playlist and tag edit surfaces.
- [ ] 2.9.7 Re-run the rehearsal-critical manual regression sweep (repeat the scope of `2.8.1`–`2.8.3`) after `2.9.1`–`2.9.5` land, to confirm the fixes and re-verify no new regressions were introduced, before considering the change ready to archive.
